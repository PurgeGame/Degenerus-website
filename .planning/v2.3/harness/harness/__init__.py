"""Degenerus v2.3 expected-value derivation harness.

Populated by phases 18-01 (constants + archetypes), 18-02 (regimes + derive),
18-03 (schema + api + render). Top-level intentionally avoids importing
submodules to prevent cycles before downstream plans land.
"""

__version__ = "0.1.0"
