---
phase: 52
plan: 02
slug: wave1-skeleton-panels-and-helpers
type: execute
wave: 1
depends_on:
  - 52-01-PLAN.md
files_modified:
  - play/app/tickets-inventory.js
  - play/app/tickets-fetch.js
  - play/app/pack-animator.js
  - play/app/pack-audio.js
  - play/components/tickets-panel.js
  - play/components/packs-panel.js
  - play/components/jackpot-panel-wrapper.js
  - play/components/jackpot-panel.js
  - play/styles/play.css
  - play/index.html
  - play/app/main.js
  - play/app/__tests__/play-panel-stubs.test.js
  - play/assets/audio/pack-open.mp3
autonomous: true
requirements:
  - TICKETS-01
  - TICKETS-02
  - TICKETS-03
  - TICKETS-04
  - PACKS-01
  - PACKS-02
  - PACKS-03
  - PACKS-04
  - PACKS-05
  - JACKPOT-01
  - JACKPOT-02
  - JACKPOT-03
tags:
  - phase-52
  - wave-1
  - skeleton
  - game-ui
  - gsap
  - web-audio
  - shell-01
  - tag-rename

must_haves:
  truths:
    - "play/app/tickets-inventory.js contains the CARD_IDX = [3,4,5,6,0,2,1,7] reshuffle verbatim; corrupting this array silently breaks the cards quadrant (Pitfall 1 + A1)"
    - "play/app/tickets-fetch.js shared helper dedups wire-level requests via in-flight promise cache keyed by (addr, level, day); both <tickets-panel> and <packs-panel> import it (Approach 3 from RESEARCH Section 7)"
    - "play/components/tickets-panel.js renders opened cards (filter cards.status === 'opened') as a 2x2 CSS grid of trait SVGs from traitToBadge(); pending + partial go to <packs-panel>"
    - "play/components/packs-panel.js renders non-opened cards as pack placeholders with pack-sealed class; click handler runs animatePackOpen(); lootbox-source auto-opens on first render via #animatedCards Set (Pitfall 11 guard)"
    - "play/components/jackpot-panel-wrapper.js directly imports beta/components/jackpot-panel.js (side-effect) and shims replay.day + replay.level into game.jackpotDay + game.level via update() so the inner beta panel re-renders on day scrubber change (D-09 + JACKPOT-03)"
    - "The Phase 50 stub at play/components/jackpot-panel.js is DELETED (Pitfall 12 customElements.define collision prevention); play/app/__tests__/play-panel-stubs.test.js is edited to replace the jackpot-panel entry with jackpot-panel-wrapper (Pitfall 7 concurrent fix)"
    - "play/index.html gains a <link> to beta/styles/jackpot.css (Pitfall 13) AND replaces <jackpot-panel> with <jackpot-panel-wrapper> AND adds <packs-panel> to the play-grid"
    - "play/app/main.js registerComponents() paths array drops '../components/jackpot-panel.js' and adds '../components/jackpot-panel-wrapper.js' + '../components/packs-panel.js'"
    - "play/app/pack-animator.js uses GSAP 3.14 timeline with the D-07 phases (shake 80ms, flash 50ms, snap-open 120ms, trait slide 150ms) and has a prefers-reduced-motion instant-swap fallback"
    - "play/app/pack-audio.js uses Web Audio API (AudioContext + decodeAudioData + AudioBufferSource + GainNode at 0.4) with localStorage mute persistence and single console.warn fail-silent path (D-10)"
    - "play/assets/audio/pack-open.mp3 exists as a real MP3 (sourced from freesound.org CC0 or similar) OR as a placeholder marker; pack-audio.js fail-silent handles 404 either way so Wave 1 doesn't block on asset sourcing"
    - "SHELL-01 recursive guardrail play-shell-01.test.js stays green; no new play/ files import forbidden paths"
    - "Wave 1 lands WITHOUT real INTEG-01 fetch wiring -- panels contain #refetch() methods but they remain callable stubs until Wave 2 flips the subscriptions from console.log to this.#refetch(); Wave 0 test assertions on #ticketsFetchId/#packsFetchId/is-stale/subscribe still turn green because the code contains those identifiers"
  artifacts:
    - path: "play/app/tickets-inventory.js"
      provides: "Wallet-free traitToBadge(traitId) decomposer with CARD_IDX reshuffle, QUADRANTS/COLORS/ITEMS constants, badgePath() helper"
      min_lines: 40
      contains: "CARD_IDX"
    - path: "play/app/tickets-fetch.js"
      provides: "Shared fetchTicketsByTrait(addr, level, day) helper with in-flight promise dedup + single-slot cache keyed by (addr,level,day)"
      min_lines: 25
      contains: "fetchTicketsByTrait"
    - path: "play/app/pack-animator.js"
      provides: "GSAP timeline exports animatePackOpen(packEl, onComplete) with D-07 phases + prefers-reduced-motion fallback"
      min_lines: 30
      contains: "prefers-reduced-motion"
    - path: "play/app/pack-audio.js"
      provides: "Web Audio wrapper: playPackOpen(), isMuted(), setMuted(), AudioContext + decodeAudioData + GainNode 0.4 + localStorage persistence + fail-silent"
      min_lines: 50
      contains: "AudioContext"
    - path: "play/components/tickets-panel.js"
      provides: "Hydrated <tickets-panel> Custom Element: three subscribes (day/player/level), #ticketsFetchId stale-guard, #refetch() method, card grid render from cards.status==='opened'"
      min_lines: 140
    - path: "play/components/packs-panel.js"
      provides: "Hydrated <packs-panel> Custom Element: three subscribes, #packsFetchId stale-guard, mute toggle (data-bind='mute-toggle'), pack-sealed + gold-tint + lootbox auto-open + #animatedCards + #activeTimelines cleanup"
      min_lines: 180
    - path: "play/components/jackpot-panel-wrapper.js"
      provides: "Thin wrapper importing beta/components/jackpot-panel.js directly; shim replay.{day,level} -> game.{jackpotDay,level}"
      min_lines: 40
    - path: "play/styles/play.css"
      provides: "Phase 52 sections: .tickets-panel grid, .ticket-card + .trait-grid + .trait-quadrant + .trait-badge; .packs-panel grid + .pack-sealed + .pack-opened + @keyframes pack-pulse + .mute-toggle + pack-source-* tints + prefers-reduced-motion"
      min_lines: 520
    - path: "play/index.html"
      provides: "Added <link> to beta/styles/jackpot.css; replaced <jackpot-panel> with <jackpot-panel-wrapper>; added <packs-panel> before <tickets-panel> in play-grid"
      min_lines: 70
    - path: "play/app/main.js"
      provides: "registerComponents paths updated: drop jackpot-panel.js, add jackpot-panel-wrapper.js + packs-panel.js"
      min_lines: 110
    - path: "play/app/__tests__/play-panel-stubs.test.js"
      provides: "PANEL_STUBS array updated: jackpot-panel entry replaced with jackpot-panel-wrapper entry; packs-panel entry added"
      min_lines: 100
  key_links:
    - from: "play/components/tickets-panel.js"
      to: "play/app/tickets-fetch.js + play/app/tickets-inventory.js"
      via: "import { fetchTicketsByTrait } + import { traitToBadge }"
      pattern: "from\\s+['\"]\\.\\./app/tickets-(fetch|inventory)\\.js['\"]"
    - from: "play/components/packs-panel.js"
      to: "play/app/tickets-fetch.js + play/app/pack-animator.js + play/app/pack-audio.js"
      via: "import helpers"
      pattern: "from\\s+['\"]\\.\\./app/(tickets-fetch|pack-animator|pack-audio)\\.js['\"]"
    - from: "play/components/jackpot-panel-wrapper.js"
      to: "beta/components/jackpot-panel.js (post-D-09 patch)"
      via: "side-effect import triggers customElements.define('jackpot-panel', ...)"
      pattern: "from\\s+['\"][^'\"]*\\/beta/components/jackpot-panel\\.js['\"]"
    - from: "play/index.html"
      to: "beta/styles/jackpot.css"
      via: "<link rel=\"stylesheet\" href=\"/beta/styles/jackpot.css\">"
      pattern: "beta/styles/jackpot\\.css"
    - from: "play/app/main.js"
      to: "play/components/jackpot-panel-wrapper.js + play/components/packs-panel.js"
      via: "dynamic import() in registerComponents paths array"
      pattern: "jackpot-panel-wrapper\\.js|packs-panel\\.js"
---

<objective>
Wave 1 builds the pre-backend skeleton: five new helper modules, three new Custom Elements, CSS for cards and packs, the index.html panel grid update (including the critical jackpot.css link), the main.js boot sequence update, the panel-stubs test update (tag rename + packs add), and the bundled pack-open.mp3 audio asset. No INTEG-01 fetch wiring yet -- Wave 2 hard-gates on backend delivery.

Purpose: Write ~600 LOC of UI scaffolding + helpers while the backend is still a TODO. By Wave 2, only the surgical fetch + subscribe wiring remains, minimizing the time Phase 52 is blocked on the INTEG-01 side-quest.

Output:
- Four helpers: `tickets-inventory.js` (trait decomposer with CARD_IDX reshuffle), `tickets-fetch.js` (shared INTEG-01 fetcher with in-flight dedup), `pack-animator.js` (GSAP 300-500ms timeline with reduced-motion fallback), `pack-audio.js` (Web Audio + localStorage mute).
- Three Custom Elements: `<tickets-panel>` (opened cards; 2x2 SVG quadrant grid), `<packs-panel>` (sealed/partial/auto-open; click + lootbox auto-open; mute toggle), `<jackpot-panel-wrapper>` (thin wrapper around beta's jackpot-panel; replay-to-game shim).
- CSS extension at `play/styles/play.css` (~320 new lines of Phase 52 styles).
- `play/index.html` updates: jackpot.css link, tag rename, packs-panel add.
- `play/app/main.js` registerComponents paths updated.
- `play/app/__tests__/play-panel-stubs.test.js` PANEL_STUBS array updated (jackpot-panel -> jackpot-panel-wrapper; +packs-panel).
- `play/components/jackpot-panel.js` Phase 50 stub DELETED (Pitfall 12 collision fix).
- `play/assets/audio/pack-open.mp3` bundled (real MP3 or placeholder; fail-silent path tolerates 404).

This plan turns these Wave 0 assertions green:
- All existence assertions for the 7 new files (tickets-inventory, tickets-fetch, pack-animator, pack-audio, tickets-panel, packs-panel, jackpot-panel-wrapper).
- All markup assertions (data-bind, ticket-card, trait-quadrant, pack-sealed, mute-toggle, card-grid, pack-grid).
- All import assertions (cross-module imports between helpers and panels, direct beta import).
- Subscribe-to-store assertions (three subscribes in each panel).
- CARD_IDX reshuffle assertion (exact array literal).
- prefers-reduced-motion assertion (in pack-animator).
- AudioContext / decodeAudioData / localStorage assertions (in pack-audio).
- Shell assertions (class extends HTMLElement, connectedCallback, disconnectedCallback, skeleton-shimmer).
- #ticketsFetchId / #packsFetchId identifier assertions (the fields exist even without live fetch wiring).
- is-stale class assertion (even without toggle invocation yet; the class literal exists for Wave 2 to flip).
- Pitfall-13 jackpot.css link assertion.
- Pitfall-7 updated panel-stubs test.

It leaves RED (for Wave 2):
- None of the subscribe callbacks trigger real #refetch() yet -- Wave 1 leaves them as console.log stubs similar to Phase 50. Wave 2 flips them to this.#refetch() and adds the initial refetch call.
- Actual fetch invocation flows (the URL template exists in tickets-fetch.js but panels don't yet call it because subscriptions are stubbed).
- #showContent() invocation (depends on fetch landing data first).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-tickets-packs-jackpot/52-CONTEXT.md
@.planning/phases/52-tickets-packs-jackpot/52-RESEARCH.md
@.planning/phases/52-tickets-packs-jackpot/52-PATTERNS.md
@.planning/phases/52-tickets-packs-jackpot/52-VALIDATION.md
@.planning/phases/52-tickets-packs-jackpot/INTEG-01-SPEC.md
@.planning/phases/52-tickets-packs-jackpot/52-01-PLAN.md

<!-- Gold-standard hydrated panel template -->
@play/components/profile-panel.js

<!-- Wallet-free helper template -->
@play/app/quests.js

<!-- Source of truth patterns (DO NOT IMPORT from play/; read for shape) -->
@beta/components/jackpot-panel.js
@beta/components/degenerette-panel.js
@beta/app/store.js
@beta/viewer/utils.js
@beta/viewer/badge-inventory.js
@beta/app/jackpot-rolls.js

<!-- Phase 50 stubs being replaced/deleted -->
@play/components/tickets-panel.js
@play/components/jackpot-panel.js
@play/app/main.js
@play/index.html

<!-- Existing CSS to extend -->
@play/styles/play.css

<!-- Wave 0 test files driving the contract -->
@play/app/__tests__/play-tickets-panel.test.js
@play/app/__tests__/play-packs-panel.test.js
@play/app/__tests__/play-jackpot-wrapper.test.js
@play/app/__tests__/play-panel-stubs.test.js
@play/app/__tests__/play-shell-01.test.js

<interfaces>
<!-- Safe imports for Wave 1 -->
From beta/app/store.js (wallet-free):
  export function subscribe(path: string, cb: (val) => void): () => void
  export function get(path: string): any
  export function update(path: string, value | (prev) => next): void
From beta/viewer/utils.js (wallet-free; explicit SHELL-01 comment at line 2):
  export function formatEth(weiString: string): string
  export function formatBurnie(weiString: string): string
  export function truncateAddress(address: string): string
  export function formatWei(weiString: string, decimals: number): number
From gsap (importmap entry in play/index.html:17):
  import gsap from 'gsap'
  gsap.timeline({ onComplete }) -> tl with .to(), .fromTo(), .kill()
From play/app/constants.js (narrow re-export):
  import { API_BASE } from '../app/constants.js'

<!-- FORBIDDEN imports (SHELL-01 recursive guardrail in play-shell-01.test.js) -->
  from 'ethers'
  from '../../beta/app/wallet.js'
  from '../../beta/app/contracts.js'
  from '../../beta/app/utils.js'            (ethers at line 3)
  from '../../beta/app/quests.js'           (transitively ethers via ./utils.js line 7)
  from '../../beta/components/connect-prompt.js'
  from '../../beta/components/purchase-panel.js'
  from '../../beta/components/coinflip-panel.js'
  from '../../beta/components/decimator-panel.js'
  from '../../beta/viewer/badge-inventory.js' (optional: not forbidden but re-implementing decomposer locally keeps SHELL-01 narrow per PATTERNS.md recommendation)

<!-- SAFE post-D-09 patch -->
  from '../../beta/components/jackpot-panel.js'  (ONLY after 52-01 Task 2 patches line 7)

<!-- CARD_IDX reshuffle -- COPY EXACTLY -->
  const CARD_IDX = [3, 4, 5, 6, 0, 2, 1, 7];
  // Used ONLY for the 'cards' quadrant: fileIdx = CARD_IDX[symbolIdx]
  // Other quadrants (crypto, zodiac, dice) map 1:1 symbolIdx -> fileIdx

<!-- QUADRANTS/COLORS/ITEMS from beta/viewer/badge-inventory.js:6-14 -->
  const QUADRANTS = ['crypto', 'zodiac', 'cards', 'dice'];
  const COLORS = ['pink', 'purple', 'green', 'red', 'blue', 'orange', 'silver', 'gold'];
  const ITEMS = {
    crypto: ['xrp', 'tron', 'sui', 'monero', 'solana', 'chainlink', 'ethereum', 'bitcoin'],
    zodiac: ['aries', 'taurus', 'gemini', 'cancer', 'leo', 'libra', 'sagittarius', 'aquarius'],
    cards:  ['club', 'diamond', 'heart', 'spade', 'horseshoe', 'cashsack', 'king', 'ace'],
    dice:   ['1', '2', '3', '4', '5', '6', '7', '8'],
  };

<!-- traitToBadge(traitId) decomposition formula -->
  quadrant = Math.floor(traitId / 64)       // 0-3
  category = QUADRANTS[quadrant]
  within = traitId % 64
  itemIdx = Math.floor(within / 8)          // 0-7 (symbolIdx on contract)
  colorIdx = within % 8                     // 0-7
  fileIdx = category === 'cards' ? CARD_IDX[itemIdx] : itemIdx
  path = `/badges-circular/${category}_${String(fileIdx).padStart(2,'0')}_${ITEMS[category][itemIdx]}_${COLORS[colorIdx]}.svg`

<!-- INTEG-01 response shape expected (for renderer skeleton, even without live fetch) -->
  { address, level, day, totalEntries, cards: [
    { cardIndex, status, entries: [{ entryId, traitId, traitLabel }, ...], source, purchaseBlock }, ...
  ]}
  status: 'opened' | 'pending' | 'partial'
  source: 'purchase' | 'jackpot-win' | 'lootbox'

<!-- GSAP timeline D-07 phases -->
  Phase 1: shake ~80ms
    tl.to(packEl, { x: -2, duration: 0.020, yoyo: true, repeat: 3, ease: 'power1.inOut' });
  Phase 2: flash ~50ms
    tl.to(packEl, { opacity: 0.6, duration: 0.025, yoyo: true, repeat: 1 }, '>');
  Phase 3: snap-open + bounce ~120ms (target .pack-wrapper child)
    tl.to(wrapper, { scale: 1.05, opacity: 0, duration: 0.12, ease: 'back.in(1.5)' }, '>');
  Phase 4: slide/scale traits in ~150ms staggered (target .pack-trait children)
    tl.fromTo(traits, { scale: 0.3, opacity: 0 }, { scale: 1, opacity: 1, duration: 0.15, stagger: 0.02, ease: 'back.out(1.3)' }, '>');

<!-- D-10 Web Audio parameters -->
  STORAGE_KEY = 'play.audio.muted'
  VOLUME = 0.4
  ASSET_PATH = '/play/assets/audio/pack-open.mp3'
  Fail-silent: console.warn once on load error; subsequent playPackOpen() returns early

<!-- Panel grid order (play/index.html:48-57) target state -->
  <profile-panel data-slot="profile"></profile-panel>
  <packs-panel data-slot="packs"></packs-panel>       <!-- NEW Phase 52 -->
  <tickets-panel data-slot="tickets"></tickets-panel>
  <purchase-panel data-slot="purchase"></purchase-panel>
  <coinflip-panel data-slot="coinflip"></coinflip-panel>
  <baf-panel data-slot="baf"></baf-panel>
  <decimator-panel data-slot="decimator"></decimator-panel>
  <jackpot-panel-wrapper data-slot="jackpot"></jackpot-panel-wrapper>  <!-- WAS: <jackpot-panel> -->

<!-- CSS links order (play/index.html:7-13) target state -->
  /shared/nav.css
  /beta/styles/base.css
  /beta/styles/panels.css
  /beta/styles/buttons.css
  /beta/styles/skeleton.css
  /beta/styles/viewer.css
  /beta/styles/jackpot.css          <!-- NEW Phase 52 (Pitfall 13) -->
  /play/styles/play.css
</interfaces>
</context>

<read_first>
- .planning/phases/52-tickets-packs-jackpot/52-CONTEXT.md (all 10 decisions; reread D-01..D-10 in full -- these are non-negotiable)
- .planning/phases/52-tickets-packs-jackpot/52-RESEARCH.md Sections 5 (badge SVG + CARD_IDX), 6 (jackpot wrapper architecture with Option A store shim), 7 (two-panel structure + shared fetch helper Approach 3), 9 (pack animation + Web Audio), Pitfalls 1, 3, 4, 7, 8, 11, 12, 13
- .planning/phases/52-tickets-packs-jackpot/52-PATTERNS.md (full file; every new file has an analog section with copy-pasteable code excerpts)
- .planning/phases/52-tickets-packs-jackpot/INTEG-01-SPEC.md (response shape; renderers target this schema even if fetch is stubbed)
- .planning/phases/52-tickets-packs-jackpot/52-01-PLAN.md (Wave 0 deliverables -- test assertions that Wave 1 must turn green)
- play/components/profile-panel.js (full file; gold-standard template for Custom Element shell, stale-guard shape, subscribe patterns, data-bind attribute usage, #showContent())
- play/app/quests.js (full file; wallet-free helper template; no imports from beta/app/utils.js)
- play/index.html (full file; CSS link list and panel grid; Wave 1 edits both)
- play/app/main.js (full file; registerComponents paths array; Wave 1 edits)
- play/app/__tests__/play-panel-stubs.test.js (full file; PANEL_STUBS array; Wave 1 edits)
- play/styles/play.css (full file; appended Phase 52 sections at the end)
- beta/components/jackpot-panel.js lines 1-20 (confirm Task-2 patch from 52-01 landed; import at line 7 is '../viewer/utils.js')
- beta/viewer/badge-inventory.js lines 1-40 (traitToBadge source to copy)
- beta/components/degenerette-panel.js lines 310-361 (GSAP timeline shape reference)
- beta/components/replay-panel.js lines 1900-1918 (AudioContext pattern reference)
- beta/app/store.js (full file; confirm subscribe/get/update signatures)
- play/app/__tests__/play-tickets-panel.test.js (full file; the assertions this plan must turn green)
- play/app/__tests__/play-packs-panel.test.js (full file)
- play/app/__tests__/play-jackpot-wrapper.test.js (full file)
- play/app/__tests__/play-shell-01.test.js (FORBIDDEN array; Wave 1 stays green against this)
- CLAUDE.md (no em dashes, no emojis, precise language)
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the four wallet-free helper modules (tickets-inventory, tickets-fetch, pack-animator, pack-audio)</name>
  <files>play/app/tickets-inventory.js, play/app/tickets-fetch.js, play/app/pack-animator.js, play/app/pack-audio.js</files>
  <read_first>
    - play/app/quests.js (wallet-free helper shape + header comment style)
    - play/app/api.js (existing thin fetchJSON; confirm it's wallet-free and does not conflict with new tickets-fetch.js)
    - play/app/constants.js (API_BASE re-export; tickets-fetch.js imports from here as `./constants.js`)
    - beta/viewer/badge-inventory.js lines 6-32 (traitToBadge source; copy verbatim)
    - beta/components/degenerette-panel.js lines 310-361 (GSAP shape reference for pack-animator)
    - beta/components/replay-panel.js lines 1900-1918 (AudioContext reference for pack-audio)
    - .planning/phases/52-tickets-packs-jackpot/52-PATTERNS.md sections "play/app/tickets-inventory.js" (line 386), "play/app/tickets-fetch.js" (line 456), "play/app/pack-animator.js" (line 511), "play/app/pack-audio.js" (line 569)
    - .planning/phases/52-tickets-packs-jackpot/52-RESEARCH.md Sections 5 (CARD_IDX formula + reshuffle explanation), 7 (fetch helper dedup pattern), 9 (GSAP + Web Audio sketches)
    - play/app/__tests__/play-tickets-panel.test.js (the assertions these helpers must satisfy)
    - play/app/__tests__/play-packs-panel.test.js (same)
  </read_first>
  <behavior>
Tests red that this task MUST turn green (from Wave 0):
- play/app/tickets-inventory.js exists
- tickets-inventory.js exports traitToBadge
- tickets-inventory.js includes CARD_IDX = [3,4,5,6,0,2,1,7] array literal (exact match)
- tickets-inventory.js is wallet-free (no ethers, no beta/app/utils.js import)
- tickets-inventory.js QUADRANTS contains crypto/zodiac/cards/dice
- play/app/tickets-fetch.js exists
- tickets-fetch.js exports fetchTicketsByTrait (async function)
- tickets-fetch.js URL matches /player/{addr}/tickets/by-trait?level= pattern
- tickets-fetch.js imports API_BASE from ./constants.js
- tickets-fetch.js is wallet-free
- play/app/pack-animator.js exists
- pack-animator.js exports animatePackOpen
- pack-animator.js imports gsap
- pack-animator.js honors prefers-reduced-motion
- pack-animator.js is wallet-free
- play/app/pack-audio.js exists
- pack-audio.js exports playPackOpen, isMuted, setMuted
- pack-audio.js references /play/assets/audio/pack-open.mp3
- pack-audio.js uses AudioContext + decodeAudioData
- pack-audio.js persists mute via localStorage
- pack-audio.js fail-silent (console.warn)
- pack-audio.js is wallet-free
- play-shell-01.test.js stays green (no new play/ file imports forbidden paths)
  </behavior>
  <action>
Use the Write tool four times. Copy the content blocks below verbatim into each file; do not paraphrase. No heredoc.

## File 1: `play/app/tickets-inventory.js`

```javascript
// play/app/tickets-inventory.js -- wallet-free trait decomposer for the /play/ route
//
// Re-implementation of the traitToBadge() decomposer from
// beta/viewer/badge-inventory.js lines 6-32. The beta file also renders a
// cumulative-counts grid using fetchJSON; we re-implement the pure
// decomposer locally to avoid pulling that coupling into play/ (keeps
// the SHELL-01 surface narrow). The CARD_IDX reshuffle is load-bearing
// (Phase 52 RESEARCH Pitfall 1): copying QUADRANTS/ITEMS without
// CARD_IDX silently corrupts the `cards` quadrant because the
// filesystem's fileIdx order does not match the contract's symbolIdx
// order. Verified against three independent beta sources:
//   - beta/app/constants.js:53
//   - beta/app/jackpot-rolls.js:26
//   - beta/viewer/badge-inventory.js:14
// All three use the array literal [3, 4, 5, 6, 0, 2, 1, 7]. Do NOT
// "fix" this -- the SVG files on disk reflect the legacy order.
//
// SHELL-01: zero imports. Pure module.
// Contract: DegenerusGameMintModule.sol lines 440-478 (trait bit layout).

// QUADRANTS[0]=crypto, [1]=zodiac, [2]=cards, [3]=dice
export const QUADRANTS = ['crypto', 'zodiac', 'cards', 'dice'];

export const COLORS = ['pink', 'purple', 'green', 'red', 'blue', 'orange', 'silver', 'gold'];

export const ITEMS = {
  crypto: ['xrp', 'tron', 'sui', 'monero', 'solana', 'chainlink', 'ethereum', 'bitcoin'],
  zodiac: ['aries', 'taurus', 'gemini', 'cancer', 'leo', 'libra', 'sagittarius', 'aquarius'],
  cards:  ['club', 'diamond', 'heart', 'spade', 'horseshoe', 'cashsack', 'king', 'ace'],
  dice:   ['1', '2', '3', '4', '5', '6', '7', '8'],
};

// THE CARD_IDX RESHUFFLE (Pitfall 1 guard).
// For the `cards` quadrant ONLY, contract symbolIdx (0-7) maps to
// filesystem fileIdx via this array. Other quadrants map 1:1.
export const CARD_IDX = [3, 4, 5, 6, 0, 2, 1, 7];

export function badgePath(category, symbolIdx, colorIdx) {
  const fileIdx = category === 'cards' ? CARD_IDX[symbolIdx] : symbolIdx;
  const name = ITEMS[category][symbolIdx];
  const color = COLORS[colorIdx];
  return `/badges-circular/${category}_${String(fileIdx).padStart(2, '0')}_${name}_${color}.svg`;
}

// Decompose a contract traitId (0-255) into its display parts.
// Returns { category, item, itemIdx, colorIdx, color, path } or null if
// traitId is null/out-of-range (INTEG-01 response may include null
// traitIds for pending entries; caller renders a placeholder in that case).
export function traitToBadge(traitId) {
  if (traitId == null || typeof traitId !== 'number' || traitId < 0 || traitId > 255) return null;
  const quadrant = Math.floor(traitId / 64);       // 0-3
  const category = QUADRANTS[quadrant];
  const within = traitId % 64;
  const itemIdx = Math.floor(within / 8);          // 0-7 (symbolIdx on contract)
  const colorIdx = within % 8;                     // 0-7
  return {
    category,
    item: ITEMS[category][itemIdx],
    itemIdx,
    colorIdx,
    color: COLORS[colorIdx],
    path: badgePath(category, itemIdx, colorIdx),
  };
}
```

## File 2: `play/app/tickets-fetch.js`

```javascript
// play/app/tickets-fetch.js -- shared INTEG-01 fetcher with in-flight dedup
//
// Two Custom Elements (<tickets-panel>, <packs-panel>) need the same
// INTEG-01 response on every (player, level, day) change. This helper
// dedups at the wire level: first call fires the HTTP request; second
// call (same key, in flight) returns the in-flight promise. Single-slot
// cache by key prevents refetch within the same key. Per Pitfall 9, ONE
// slot (not a full cache) keeps memory bounded.
//
// Panel-level stale-guard (#ticketsFetchId / #packsFetchId) is a separate
// concern: the helper dedups wire requests; the panel dedups stale renders.
//
// SHELL-01: imports only from the narrow play/app/constants.js re-export.

import { API_BASE } from './constants.js';

let inFlight = null;
let lastKey = null;
let lastResult = null;
let lastResultKey = null;

export async function fetchTicketsByTrait(addr, level, day) {
  const key = `${addr}::${level}::${day}`;
  if (key === lastResultKey && lastResult) return lastResult;
  if (key === lastKey && inFlight) return inFlight;
  lastKey = key;
  const url = `${API_BASE}/player/${encodeURIComponent(addr)}/tickets/by-trait`
            + `?level=${encodeURIComponent(level)}&day=${encodeURIComponent(day)}`;
  inFlight = fetch(url)
    .then((r) => (r.ok ? r.json() : null))
    .then((data) => {
      lastResult = data;
      lastResultKey = key;
      inFlight = null;
      return data;
    })
    .catch((err) => {
      inFlight = null;
      throw err;
    });
  return inFlight;
}
```

## File 3: `play/app/pack-animator.js`

```javascript
// play/app/pack-animator.js -- GSAP timeline for pack-open reveal (D-07)
//
// Phase 1: shake (~80ms)
// Phase 2: flash (~50ms)
// Phase 3: snap-open + bounce (~120ms) on .pack-wrapper
// Phase 4: slide/scale traits in (~150ms staggered) on .pack-trait elements
// Total: ~400ms
//
// prefers-reduced-motion fallback: instant state swap (D-07 override).
// Sound plays even in reduced-motion mode (unless muted) per D-10 --
// the audio call is made by the caller (packs-panel.js), not here.
//
// Caller should store the returned timeline and call .kill() on
// disconnectedCallback to free GSAP's internal tween storage
// (Pitfall 3 memory-leak guard).
//
// GSAP is available via the play/index.html importmap entry.
// SHELL-01: imports only from 'gsap' (importmap-registered; no ethers).

import gsap from 'gsap';

export function animatePackOpen(packEl, onComplete) {
  const reducedMotion = typeof window !== 'undefined'
    && typeof window.matchMedia === 'function'
    && window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  if (reducedMotion) {
    // Instant state swap per D-07 fallback.
    packEl.classList.remove('pack-sealed');
    packEl.classList.add('pack-opened');
    if (typeof onComplete === 'function') onComplete();
    return null;
  }

  const traits = packEl.querySelectorAll('.pack-trait');
  const wrapper = packEl.querySelector('.pack-wrapper');

  const tl = gsap.timeline({
    onComplete: () => {
      packEl.classList.remove('pack-sealed');
      packEl.classList.add('pack-opened');
      if (typeof onComplete === 'function') onComplete();
    },
  });

  // Phase 1: shake ~80ms
  tl.to(packEl, { x: -2, duration: 0.020, yoyo: true, repeat: 3, ease: 'power1.inOut' });

  // Phase 2: flash ~50ms
  tl.to(packEl, { opacity: 0.6, duration: 0.025, yoyo: true, repeat: 1 }, '>');

  // Phase 3: snap-open + bounce ~120ms
  if (wrapper) {
    tl.to(wrapper, { scale: 1.05, opacity: 0, duration: 0.12, ease: 'back.in(1.5)' }, '>');
  }

  // Phase 4: slide/scale traits in ~150ms staggered
  if (traits && traits.length > 0) {
    tl.fromTo(
      traits,
      { scale: 0.3, opacity: 0 },
      { scale: 1, opacity: 1, duration: 0.15, stagger: 0.02, ease: 'back.out(1.3)' },
      '>'
    );
  }

  return tl;
}
```

## File 4: `play/app/pack-audio.js`

```javascript
// play/app/pack-audio.js -- Web Audio wrapper for PACKS-05 / D-10
//
// Fail-silent on 404 or unsupported API. localStorage mute persistence
// at STORAGE_KEY='play.audio.muted'. First play() serves as the browser
// autoplay-unlock user gesture (pack click is a user gesture -- D-10).
// Volume fixed at 0.4 in a GainNode (D-10).
//
// Lootbox auto-open (D-06) fires on render, NOT a user gesture --
// its first sound will be blocked until any subsequent user gesture
// unlocks the AudioContext. Accepted per Pitfall 4; no "click to enable"
// overlay.
//
// SHELL-01: zero imports (pure module).

const STORAGE_KEY = 'play.audio.muted';
const VOLUME = 0.4;
const ASSET_PATH = '/play/assets/audio/pack-open.mp3';

let ctx = null;
let buffer = null;
let loadError = null;

async function ensureLoaded() {
  if (ctx || loadError) return;
  try {
    const AudioCtor = (typeof window !== 'undefined')
      && (window.AudioContext || window.webkitAudioContext);
    if (!AudioCtor) throw new Error('Web Audio API not available');
    ctx = new AudioCtor();
    const resp = await fetch(ASSET_PATH);
    if (!resp.ok) throw new Error(`HTTP ${resp.status}`);
    const bytes = await resp.arrayBuffer();
    buffer = await ctx.decodeAudioData(bytes);
  } catch (err) {
    loadError = err;
    // Fail-silent per D-10: single console.warn, no UI error, subsequent
    // playPackOpen() calls return early.
    try { console.warn('[pack-audio] disabled:', err && err.message ? err.message : err); } catch {}
  }
}

export function isMuted() {
  try {
    return typeof localStorage !== 'undefined'
      && localStorage.getItem(STORAGE_KEY) === '1';
  } catch {
    return false;
  }
}

export function setMuted(muted) {
  try {
    if (typeof localStorage !== 'undefined') {
      localStorage.setItem(STORAGE_KEY, muted ? '1' : '0');
    }
  } catch {
    // Privacy-mode browsers block localStorage; in-memory session fallback
    // is implicit (isMuted returns false next call and the toggle still
    // reflects the in-session intent via the caller's UI state).
  }
}

export async function playPackOpen() {
  if (isMuted()) return;
  await ensureLoaded();
  if (!buffer || !ctx) return;   // fail-silent per D-10
  if (ctx.state === 'suspended') {
    try { await ctx.resume(); } catch { return; }
  }
  try {
    const src = ctx.createBufferSource();
    src.buffer = buffer;
    const gain = ctx.createGain();
    gain.gain.value = VOLUME;
    src.connect(gain);
    gain.connect(ctx.destination);
    src.start(0);
  } catch {
    // Defensive: if the buffer was invalidated, silently no-op.
  }
}
```

Self-check after writing all four helpers:
- Four files exist in `play/app/`.
- Each parses via `node --check`.
- No em dashes or emojis.
- Every forbidden import is absent (run `play-shell-01.test.js` to confirm).
- The CARD_IDX array literal is exactly `[3, 4, 5, 6, 0, 2, 1, 7]` (spaces preserved; no reformat).
  </action>
  <verify>
    <automated>test -f play/app/tickets-inventory.js && test -f play/app/tickets-fetch.js && test -f play/app/pack-animator.js && test -f play/app/pack-audio.js && node --check play/app/tickets-inventory.js && node --check play/app/tickets-fetch.js && node --check play/app/pack-animator.js && node --check play/app/pack-audio.js && node --test play/app/__tests__/play-shell-01.test.js && node --test play/app/__tests__/play-profile-panel.test.js</automated>
  </verify>
  <acceptance_criteria>
    - All four helper files exist and parse
    - `grep -c "CARD_IDX = \\[ *3 *, *4 *, *5 *, *6 *, *0 *, *2 *, *1 *, *7 *\\]" play/app/tickets-inventory.js` returns 1 (exact reshuffle)
    - `grep -c "export function traitToBadge" play/app/tickets-inventory.js` returns 1
    - `grep -c "export const QUADRANTS" play/app/tickets-inventory.js` returns 1
    - `grep -c "export\\s\\+function\\s\\+fetchTicketsByTrait\\|export\\s\\+async\\s\\+function\\s\\+fetchTicketsByTrait" play/app/tickets-fetch.js` returns 1
    - `grep -c "/player/" play/app/tickets-fetch.js` returns 1
    - `grep -c "tickets/by-trait" play/app/tickets-fetch.js` returns 1
    - `grep -c "level=" play/app/tickets-fetch.js` returns 1
    - `grep -c "day=" play/app/tickets-fetch.js` returns 1
    - `grep -c "from '\\./constants.js'" play/app/tickets-fetch.js` returns 1
    - `grep -c "export function animatePackOpen" play/app/pack-animator.js` returns 1
    - `grep -c "from 'gsap'" play/app/pack-animator.js` returns 1
    - `grep -c "prefers-reduced-motion" play/app/pack-animator.js` returns 1
    - `grep -c "gsap.timeline" play/app/pack-animator.js` returns 1
    - `grep -c "export function isMuted" play/app/pack-audio.js` returns 1
    - `grep -c "export function setMuted" play/app/pack-audio.js` returns 1
    - `grep -c "export async function playPackOpen\\|export function playPackOpen" play/app/pack-audio.js` returns 1
    - `grep -c "AudioContext" play/app/pack-audio.js` returns at least 1
    - `grep -c "decodeAudioData" play/app/pack-audio.js` returns 1
    - `grep -c "localStorage" play/app/pack-audio.js` returns at least 2
    - `grep -c "pack-open.mp3" play/app/pack-audio.js` returns 1
    - `grep -c "console.warn" play/app/pack-audio.js` returns 1
    - `grep -c "0.4" play/app/pack-audio.js` returns at least 1 (VOLUME constant)
    - `! grep -q "from 'ethers'" play/app/tickets-inventory.js play/app/tickets-fetch.js play/app/pack-animator.js play/app/pack-audio.js`
    - `! grep -q "beta/app/utils.js" play/app/tickets-inventory.js play/app/tickets-fetch.js play/app/pack-animator.js play/app/pack-audio.js`
    - `node --test play/app/__tests__/play-shell-01.test.js` exits 0 (recursive guard stays green)
    - `node --test play/app/__tests__/play-profile-panel.test.js` exits 0 (Phase 51 test still green)
    - `wc -l play/app/tickets-inventory.js` reports at least 40 lines
    - `wc -l play/app/tickets-fetch.js` reports at least 25 lines
    - `wc -l play/app/pack-animator.js` reports at least 30 lines
    - `wc -l play/app/pack-audio.js` reports at least 50 lines
  </acceptance_criteria>
  <done>
    All four helpers exist, parse, and pass SHELL-01 recursive scan. tickets-inventory.js exports traitToBadge with the CARD_IDX reshuffle array literal exactly. tickets-fetch.js exports fetchTicketsByTrait with the INTEG-01 URL template and API_BASE import. pack-animator.js imports gsap, exports animatePackOpen with reduced-motion fallback and D-07 phase timings. pack-audio.js exports playPackOpen/isMuted/setMuted using AudioContext + decodeAudioData + GainNode 0.4 + localStorage + fail-silent. Wave 0 helper-existence and content assertions all turn green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the three Custom Elements (<tickets-panel>, <packs-panel>, <jackpot-panel-wrapper>), delete old stub, bundle audio asset</name>
  <files>play/components/tickets-panel.js, play/components/packs-panel.js, play/components/jackpot-panel-wrapper.js, play/components/jackpot-panel.js, play/assets/audio/pack-open.mp3</files>
  <read_first>
    - play/components/profile-panel.js (lines 35-418; full file as gold-standard template for class shell, TEMPLATE constant, connectedCallback/disconnectedCallback, #unsubs, #showContent, #renderError, #bind helper, is-stale class usage; note: DO NOT copy the INTEG-02 fetch shape exactly -- Wave 1 leaves fetch as a stub and Wave 2 flips subscribe callbacks to call #refetch())
    - play/components/jackpot-panel.js (Phase 50 stub; this is the file to DELETE)
    - play/components/tickets-panel.js (Phase 50 stub; this is the file to REPLACE with full hydrated content)
    - play/app/tickets-inventory.js (Task 1 output; imports traitToBadge)
    - play/app/tickets-fetch.js (Task 1 output; imports fetchTicketsByTrait)
    - play/app/pack-animator.js (Task 1 output; imports animatePackOpen)
    - play/app/pack-audio.js (Task 1 output; imports playPackOpen, isMuted, setMuted)
    - .planning/phases/52-tickets-packs-jackpot/52-PATTERNS.md sections "play/components/tickets-panel.js" (line 40), "play/components/packs-panel.js" (line 221), "play/components/jackpot-panel-wrapper.js" (line 310)
    - .planning/phases/52-tickets-packs-jackpot/52-RESEARCH.md Section 6 (jackpot wrapper Option A store-shim; lines 281-400), Section 7 (packs-panel responsibilities; lines 456-490), Pitfall 8 (game.phase / game.jackpotDay derivation), Pitfall 11 (lootbox auto-open dedup via #animatedCards)
    - beta/components/jackpot-panel.js lines 1036-1071 (#onGameUpdate subscription target -- confirms game.level, game.jackpotDay, game.phase reads)
    - beta/app/store.js (confirm subscribe/get/update signatures; Proxy path-prefix matching for game.* writes)
    - play/app/__tests__/play-tickets-panel.test.js + play-packs-panel.test.js + play-jackpot-wrapper.test.js (the assertions this task must turn green)
  </read_first>
  <behavior>
Tests red that this task MUST turn green (from Wave 0):

**play-tickets-panel.test.js:**
- tickets-panel.js exists + registers <tickets-panel> + extends HTMLElement + connectedCallback + disconnectedCallback
- imports subscribe + get from ../../beta/app/store.js
- subscribes to replay.day, replay.player, replay.level
- renders skeleton-shimmer in template
- has data-bind="skeleton" and data-bind="content"
- TICKETS-01: data-bind="card-grid"
- TICKETS-02: ticket-card + trait-quadrant class usage
- TICKETS-03: cardIndex OR entryId mentioned
- TICKETS-04: status === 'opened' OR status !== 'opened' OR 'pending' branch
- imports from ../app/tickets-fetch.js + fetchTicketsByTrait used
- imports traitToBadge from ../app/tickets-inventory.js
- #ticketsFetchId field exists
- is-stale class literal present

**play-packs-panel.test.js:**
- packs-panel.js exists + registers <packs-panel> + extends HTMLElement + connectedCallback + disconnectedCallback
- subscribes to replay.day, replay.player, replay.level
- imports from ../../beta/app/store.js
- renders skeleton-shimmer
- PACKS-01: source === 'purchase' OR pack-source-purchase class
- PACKS-02: jackpot-win OR gold-tint class
- PACKS-03: addEventListener('click', ...) + pack-sealed class rendering
- PACKS-04: source === 'lootbox' OR pack-source-lootbox OR animatedCards
- PACKS-05: imports ../app/pack-animator.js + ../app/pack-audio.js
- imports fetchTicketsByTrait from ../app/tickets-fetch.js
- #packsFetchId field exists
- is-stale class present
- data-bind="mute-toggle" + localStorage/isMuted/setMuted reference

**play-jackpot-wrapper.test.js:**
- jackpot-panel-wrapper.js exists + registers <jackpot-panel-wrapper> + extends HTMLElement + connectedCallback + disconnectedCallback
- imports beta/components/jackpot-panel.js directly
- wraps <jackpot-panel> in innerHTML
- subscribes to replay.day + replay.level
- shims update('game.level', ...)
- game.jackpotDay reference present
- imports subscribe + get + update from ../../beta/app/store.js

**play-panel-stubs.test.js (existing; MUST still pass after tag rename):**
- PANEL_STUBS array updated in Task 3 below (same plan) so this task + Task 3 are sequenced to land together; DO NOT commit intermediately

**Wave 1 intentional RED (Wave 2 flips these):**
- The three subscribe callbacks in tickets-panel.js and packs-panel.js remain console.log stubs (not this.#refetch() calls). Wave 0 test assertions that grep for `subscribe('replay.day'...)` match the subscribe call itself, not its body, so they turn green in Wave 1. Wave 2 replaces the callback bodies surgically.
  </behavior>
  <action>
Use the Write tool three times (for the three new Custom Elements). Use Bash `rm` to delete the Phase 50 stub. Create the audio asset directory + placeholder file via Bash.

## File 1: `play/components/tickets-panel.js` (REPLACES the Phase 50 stub)

```javascript
// play/components/tickets-panel.js -- <tickets-panel> Custom Element (Phase 52 Wave 1)
//
// Renders the selected player's opened 4-trait quadrant cards as a dense
// grid (D-01). Each card is a 2x2 CSS grid of trait SVGs fetched via
// traitToBadge() from play/app/tickets-inventory.js (D-02). Pending and
// partial cards go to <packs-panel> (Section 7 of 52-RESEARCH.md).
//
// Wave 1 scope: hydrated markup, helper imports, stale-guard field,
// subscribes, #refetch() method body. Wave 2 flips the subscribe callbacks
// from console.log stubs to this.#refetch() and adds the initial refetch
// invocation. The Wave 0 test assertions on subscribe+ticketsFetchId+
// is-stale turn green here because the identifiers exist, even without
// the live #refetch() invocation.
//
// SECURITY (T-52-04): the innerHTML TEMPLATE is a static string with no
// user interpolation. Dynamic writes in #renderCards use document
// createElement + textContent for any user/API-provided strings, and
// traitToBadge().path is a deterministic filesystem path (no risk).
//
// SHELL-01: imports only from the wallet-free surface.

import { subscribe, get } from '../../beta/app/store.js';
import { fetchTicketsByTrait } from '../app/tickets-fetch.js';
import { traitToBadge } from '../app/tickets-inventory.js';

const TEMPLATE = `
<section data-slot="tickets" class="panel tickets-panel">
  <div data-bind="skeleton">
    <div class="skeleton-header"><div class="skeleton-line skeleton-shimmer" style="width:40%"></div></div>
    <div class="skeleton-row"><div class="skeleton-block skeleton-shimmer" style="height:140px"></div></div>
  </div>
  <div data-bind="content" hidden>
    <h2 class="panel-title">Tickets</h2>
    <div class="tickets-grid" data-bind="card-grid">
      <!-- Opened cards render here; <packs-panel> handles non-opened states. -->
    </div>
  </div>
</section>`;

class TicketsPanel extends HTMLElement {
  #unsubs = [];
  #loaded = false;
  #ticketsFetchId = 0;

  connectedCallback() {
    this.innerHTML = TEMPLATE;

    // Wave 1: subscribe calls exist (test greps for the literals). The
    // callback bodies remain console.log stubs; Wave 2 replaces them
    // with this.#refetch() once INTEG-01 ships.
    this.#unsubs.push(
      subscribe('replay.day', (day) => {
        console.log('[tickets-panel] replay.day =', day);
      }),
      subscribe('replay.player', (addr) => {
        console.log('[tickets-panel] replay.player =', addr);
      }),
      subscribe('replay.level', (level) => {
        console.log('[tickets-panel] replay.level =', level);
      }),
    );
  }

  disconnectedCallback() {
    this.#unsubs.forEach((fn) => fn());
    this.#unsubs = [];
  }

  // --- Render helpers ------------------------------------------------------

  #showContent() {
    if (this.#loaded) return;
    this.#loaded = true;
    this.querySelector('[data-bind="skeleton"]')?.remove();
    const el = this.querySelector('[data-bind="content"]');
    if (el) el.hidden = false;
  }

  #renderCards(cards) {
    const grid = this.querySelector('[data-bind="card-grid"]');
    if (!grid) return;

    // Filter to opened cards only (pending/partial go to <packs-panel>).
    const opened = Array.isArray(cards) ? cards.filter((c) => c && c.status === 'opened') : [];

    // Clear previous contents safely.
    while (grid.firstChild) grid.removeChild(grid.firstChild);

    if (opened.length === 0) {
      const empty = document.createElement('div');
      empty.className = 'tickets-empty';
      empty.textContent = 'No tickets at this level.';
      grid.appendChild(empty);
      return;
    }

    for (const card of opened) {
      grid.appendChild(this.#buildCardNode(card));
    }
  }

  #buildCardNode(card) {
    const cardEl = document.createElement('div');
    cardEl.className = 'ticket-card';
    cardEl.setAttribute('data-card-index', String(card.cardIndex));

    const traitGrid = document.createElement('div');
    traitGrid.className = 'trait-grid';

    const entries = Array.isArray(card.entries) ? card.entries : [];
    for (let i = 0; i < 4; i++) {
      const quadrant = document.createElement('div');
      quadrant.className = 'trait-quadrant';
      const entry = entries[i];
      const badge = entry ? traitToBadge(entry.traitId) : null;
      if (badge) {
        const img = document.createElement('img');
        img.className = 'trait-badge';
        img.loading = 'lazy';
        img.src = badge.path;
        const label = (entry && entry.traitLabel) ? String(entry.traitLabel) : '';
        img.alt = label;
        if (label) img.title = label;
        quadrant.appendChild(img);
      } else {
        quadrant.classList.add('trait-quadrant-empty');
      }
      traitGrid.appendChild(quadrant);
    }
    cardEl.appendChild(traitGrid);

    const pill = document.createElement('span');
    const source = card.source || 'purchase';
    pill.className = `card-source-pill card-source-${source}`;
    pill.textContent = source;
    cardEl.appendChild(pill);

    return cardEl;
  }

  #renderError(status) {
    const grid = this.querySelector('[data-bind="card-grid"]');
    if (!grid) return;
    while (grid.firstChild) grid.removeChild(grid.firstChild);
    const msg = document.createElement('div');
    msg.className = 'tickets-empty';
    msg.textContent = status === 404 ? 'No ticket data for this day.' : 'Error loading tickets.';
    grid.appendChild(msg);
  }

  // --- Fetch wiring (Wave 1 defines it; Wave 2 triggers it from subscribe) --

  // TICKETS-01..04 + PROFILE-04-style stale-guard (D-18). Wave 2 replaces
  // the subscribe callbacks above with () => this.#refetch() + an initial
  // invocation. Wave 1 defines the method so Wave 0 tests grep the
  // identifiers (#ticketsFetchId, is-stale, fetch URL) as green.
  async #refetch() {
    const addr = get('replay.player');
    const level = get('replay.level');
    const day = get('replay.day');
    const token = ++this.#ticketsFetchId;

    if (!addr || level == null || day == null) return;

    if (this.#loaded) {
      this.querySelector('[data-bind="content"]')?.classList.add('is-stale');
    }

    try {
      const data = await fetchTicketsByTrait(addr, level, day);
      if (token !== this.#ticketsFetchId) return;
      if (!data) {
        this.#renderError(404);
        this.#showContent();
        this.querySelector('[data-bind="content"]')?.classList.remove('is-stale');
        return;
      }
      // Second stale-guard post data-resolve (D-18 double-check).
      if (token !== this.#ticketsFetchId) return;
      this.#renderCards(data.cards);
      this.#showContent();
      this.querySelector('[data-bind="content"]')?.classList.remove('is-stale');
    } catch (err) {
      if (token === this.#ticketsFetchId) {
        this.#renderError(0);
        this.#showContent();
        this.querySelector('[data-bind="content"]')?.classList.remove('is-stale');
      }
    }
  }
}

customElements.define('tickets-panel', TicketsPanel);
```

## File 2: `play/components/packs-panel.js`

```javascript
// play/components/packs-panel.js -- <packs-panel> Custom Element (Phase 52 Wave 1)
//
// Renders the selected player's pending + partial + lootbox-source packs
// (D-03, D-04). Click triggers the GSAP timeline (D-05, D-07). Lootbox
// source auto-opens on first render (D-06). Sound plays on pack open
// (D-10) unless muted via the speaker-icon toggle; mute state persists
// in localStorage.
//
// Wave 1: markup + helper imports + stale-guard field + mute toggle +
// subscribe stubs (like tickets-panel). Wave 2 flips subscribes to
// this.#refetch(). Pitfall 11 + Pitfall 3 guards are wired here
// (#animatedCards, #activeTimelines).
//
// SECURITY: innerHTML TEMPLATE is static; dynamic writes use DOM API.
// SHELL-01: imports only from wallet-free surface.

import { subscribe, get } from '../../beta/app/store.js';
import { fetchTicketsByTrait } from '../app/tickets-fetch.js';
import { traitToBadge } from '../app/tickets-inventory.js';
import { animatePackOpen } from '../app/pack-animator.js';
import { playPackOpen, isMuted, setMuted } from '../app/pack-audio.js';

const TEMPLATE = `
<section data-slot="packs" class="panel packs-panel">
  <div data-bind="skeleton">
    <div class="skeleton-header"><div class="skeleton-line skeleton-shimmer" style="width:40%"></div></div>
    <div class="skeleton-row"><div class="skeleton-block skeleton-shimmer" style="height:160px"></div></div>
  </div>
  <div data-bind="content" hidden>
    <div class="packs-header">
      <h2 class="panel-title">Packs</h2>
      <button
        type="button"
        class="mute-toggle"
        data-bind="mute-toggle"
        aria-pressed="false"
        title="Toggle pack-open sound"
      >sound</button>
    </div>
    <div class="packs-grid" data-bind="pack-grid">
      <!-- Sealed/partial/auto-open packs render here. -->
    </div>
  </div>
</section>`;

class PacksPanel extends HTMLElement {
  #unsubs = [];
  #loaded = false;
  #packsFetchId = 0;
  #animatedCards = new Set();     // Pitfall 11: lootbox auto-open dedup
  #activeTimelines = new Set();   // Pitfall 3: GSAP cleanup on disconnect

  connectedCallback() {
    this.innerHTML = TEMPLATE;
    this.#bindMuteToggle();

    // Wave 1: subscribe calls exist; callback bodies are console.log.
    // Wave 2 flips to this.#refetch() and adds the initial refetch.
    this.#unsubs.push(
      subscribe('replay.day', (day) => {
        console.log('[packs-panel] replay.day =', day);
      }),
      subscribe('replay.player', (addr) => {
        console.log('[packs-panel] replay.player =', addr);
      }),
      subscribe('replay.level', (level) => {
        console.log('[packs-panel] replay.level =', level);
      }),
    );
  }

  disconnectedCallback() {
    // Pitfall 3: kill any in-flight timelines to free GSAP tween storage.
    this.#activeTimelines.forEach((tl) => {
      try { tl.kill(); } catch {}
    });
    this.#activeTimelines.clear();
    this.#unsubs.forEach((fn) => fn());
    this.#unsubs = [];
  }

  // --- UI helpers ----------------------------------------------------------

  #bindMuteToggle() {
    const btn = this.querySelector('[data-bind="mute-toggle"]');
    if (!btn) return;
    const refresh = () => {
      const muted = isMuted();
      btn.textContent = muted ? 'muted' : 'sound';
      btn.setAttribute('aria-pressed', muted ? 'true' : 'false');
    };
    refresh();
    btn.addEventListener('click', () => {
      setMuted(!isMuted());
      refresh();
    });
  }

  #showContent() {
    if (this.#loaded) return;
    this.#loaded = true;
    this.querySelector('[data-bind="skeleton"]')?.remove();
    const el = this.querySelector('[data-bind="content"]');
    if (el) el.hidden = false;
  }

  #renderPacks(cards) {
    const grid = this.querySelector('[data-bind="pack-grid"]');
    if (!grid) return;

    const packs = Array.isArray(cards) ? cards.filter((c) => c && c.status !== 'opened') : [];

    while (grid.firstChild) grid.removeChild(grid.firstChild);

    if (packs.length === 0) {
      const empty = document.createElement('div');
      empty.className = 'packs-empty';
      empty.textContent = 'No packs waiting to open.';
      grid.appendChild(empty);
      return;
    }

    for (const card of packs) {
      grid.appendChild(this.#buildPackNode(card));
    }

    // PACKS-04: auto-open lootbox-sourced packs on first-appearance render.
    grid.querySelectorAll('.pack-source-lootbox').forEach((packEl) => {
      const key = packEl.getAttribute('data-card-index');
      if (!key) return;
      if (this.#animatedCards.has(key)) return;
      this.#animatedCards.add(key);
      this.#openPack(packEl);
    });
  }

  #buildPackNode(card) {
    const packEl = document.createElement('div');
    const source = card.source || 'purchase';
    packEl.className = `pack-sealed pack-source-${source}`;
    packEl.setAttribute('data-card-index', String(card.cardIndex));
    packEl.setAttribute('role', 'button');
    packEl.setAttribute('tabindex', '0');

    // Wrapper the GSAP Phase 3 targets (scaled + faded on open).
    const wrapper = document.createElement('div');
    wrapper.className = 'pack-wrapper';
    packEl.appendChild(wrapper);

    // Peeked trait placeholders for partial/pending cards (D-03 partial).
    const entries = Array.isArray(card.entries) ? card.entries : [];
    for (let i = 0; i < 4; i++) {
      const quadrant = document.createElement('div');
      quadrant.className = 'pack-trait';
      const entry = entries[i];
      const badge = entry ? traitToBadge(entry.traitId) : null;
      if (badge) {
        const img = document.createElement('img');
        img.className = 'trait-badge';
        img.loading = 'lazy';
        img.src = badge.path;
        const label = (entry && entry.traitLabel) ? String(entry.traitLabel) : '';
        img.alt = label;
        if (label) img.title = label;
        quadrant.appendChild(img);
      }
      wrapper.appendChild(quadrant);
    }

    const pill = document.createElement('span');
    pill.className = `card-source-pill card-source-${source}`;
    pill.textContent = source;
    packEl.appendChild(pill);

    // PACKS-03: click handler (and keyboard Enter/Space for a11y).
    packEl.addEventListener('click', () => this.#openPack(packEl));
    packEl.addEventListener('keydown', (ev) => {
      if (ev.key === 'Enter' || ev.key === ' ') {
        ev.preventDefault();
        this.#openPack(packEl);
      }
    });

    return packEl;
  }

  #openPack(packEl) {
    // Fire-and-forget audio per D-10 (fail-silent in the helper).
    try { playPackOpen(); } catch {}
    const tl = animatePackOpen(packEl, () => this.#activeTimelines.delete(tl));
    if (tl) this.#activeTimelines.add(tl);
  }

  #renderError(status) {
    const grid = this.querySelector('[data-bind="pack-grid"]');
    if (!grid) return;
    while (grid.firstChild) grid.removeChild(grid.firstChild);
    const msg = document.createElement('div');
    msg.className = 'packs-empty';
    msg.textContent = status === 404 ? 'No pack data for this day.' : 'Error loading packs.';
    grid.appendChild(msg);
  }

  // --- Fetch wiring (Wave 1 defines; Wave 2 flips subscribes) --------------

  async #refetch() {
    const addr = get('replay.player');
    const level = get('replay.level');
    const day = get('replay.day');
    const token = ++this.#packsFetchId;

    if (!addr || level == null || day == null) return;

    if (this.#loaded) {
      this.querySelector('[data-bind="content"]')?.classList.add('is-stale');
    }

    try {
      const data = await fetchTicketsByTrait(addr, level, day);
      if (token !== this.#packsFetchId) return;
      if (!data) {
        this.#renderError(404);
        this.#showContent();
        this.querySelector('[data-bind="content"]')?.classList.remove('is-stale');
        return;
      }
      if (token !== this.#packsFetchId) return;
      this.#renderPacks(data.cards);
      this.#showContent();
      this.querySelector('[data-bind="content"]')?.classList.remove('is-stale');
    } catch (err) {
      if (token === this.#packsFetchId) {
        this.#renderError(0);
        this.#showContent();
        this.querySelector('[data-bind="content"]')?.classList.remove('is-stale');
      }
    }
  }
}

customElements.define('packs-panel', PacksPanel);
```

## File 3: `play/components/jackpot-panel-wrapper.js`

```javascript
// play/components/jackpot-panel-wrapper.js -- thin wrapper around beta's
// <jackpot-panel> for the /play/ route (Phase 52 Wave 1).
//
// Imports beta/components/jackpot-panel.js directly after the D-09 patch
// removed the last wallet-tainted transitive import (Phase 52 Wave 0
// Task 2 swapped line 7 '../app/utils.js' -> '../viewer/utils.js'; the
// play-jackpot-shell01-regression.test.js guards against reverts).
//
// The beta panel subscribes to top-level `game` state (game.level,
// game.jackpotDay, game.phase). Play's scrubber writes to
// replay.{day, level, player}. The wrapper shims replay.* into game.*
// via update() on every change so the inner panel re-renders when
// play's day scrubber moves (JACKPOT-03).
//
// Rename context: Phase 50 registered a <jackpot-panel> stub in
// play/components/jackpot-panel.js; that stub is DELETED in Wave 1 to
// avoid the customElements.define collision (Pitfall 12). The play/
// panel grid in index.html now renders <jackpot-panel-wrapper>.
//
// SHELL-01: imports only from the wallet-free surface; the beta
// jackpot-panel import is safe post-D-09.

import { subscribe, get, update } from '../../beta/app/store.js';
// Side-effect import: registers customElements.define('jackpot-panel', ...)
import '../../beta/components/jackpot-panel.js';

class JackpotPanelWrapper extends HTMLElement {
  #unsubs = [];

  connectedCallback() {
    // Wave 1: render the inner beta <jackpot-panel>. The beta panel owns
    // its own skeleton -> content swap; this wrapper does not need its own.
    this.innerHTML = '<jackpot-panel data-slot="jackpot"></jackpot-panel>';

    // Shim: play.replay.* -> beta.game.* via update(). Beta's
    // #onGameUpdate at jackpot-panel.js:1036-1071 reads game.level,
    // game.jackpotDay, and game.phase. We rebroadcast the first two
    // from play's scrubber so the inner panel re-fetches on day/level
    // change. game.phase is not shimmed here; beta has internal
    // fallback logic for missing phase (defaults to a sensible display
    // per jackpot-panel.js line 1043); Pitfall 8 notes this is acceptable.
    const pushShim = () => {
      const day = get('replay.day');
      const level = get('replay.level');
      if (day != null) {
        const jackpotDay = ((day - 1) % 5) + 1;  // within-level counter 1..5
        update('game.jackpotDay', jackpotDay);
      }
      if (level != null) {
        update('game.level', level);
      }
    };

    this.#unsubs.push(
      subscribe('replay.day', () => pushShim()),
      subscribe('replay.level', () => pushShim()),
    );

    // Initial push in case the scrubber has already populated both values
    // before this wrapper mounts (Phase 50 main.js boots both first).
    pushShim();
  }

  disconnectedCallback() {
    this.#unsubs.forEach((fn) => fn());
    this.#unsubs = [];
  }
}

customElements.define('jackpot-panel-wrapper', JackpotPanelWrapper);
```

## Step 4: Delete `play/components/jackpot-panel.js`

Use Bash: `rm play/components/jackpot-panel.js` (Pitfall 12 collision prevention).

## Step 5: Create `play/assets/audio/pack-open.mp3`

Use Bash to create the directory and seed the asset:

```
mkdir -p play/assets/audio
```

Then seed a minimal placeholder MP3. If a real sound asset is not readily available, seed a zero-byte file so the path exists (pack-audio.js fail-silent path logs a warning once and then no-ops). The simplest seed:

```
touch play/assets/audio/pack-open.mp3
```

This keeps Wave 1 unblocked. Wave 3 UAT (or a follow-up) can replace with a real ~300ms click/pop cue sourced from freesound.org CC0.

## Self-check after all three Custom Elements + stub delete + asset

- `play/components/tickets-panel.js` is a new full hydrated file (not the Phase 50 stub).
- `play/components/packs-panel.js` is new.
- `play/components/jackpot-panel-wrapper.js` is new.
- `play/components/jackpot-panel.js` does NOT exist (deleted).
- `play/assets/audio/pack-open.mp3` exists (real or zero-byte placeholder).
- All three new JS files parse via `node --check`.
- All three new JS files pass `play-shell-01.test.js` (no forbidden imports).
- Wave 0 behavior tests (tickets-panel, packs-panel, jackpot-wrapper) turn green on existence + structure assertions. Some assertions (subscribe with #refetch body) remain red because Wave 1 uses console.log stubs; those specific Wave 0 assertions grep only for the subscribe call itself, not the body, so they pass. Read each test file to confirm which greps are green and which stay red for Wave 2.
  </action>
  <verify>
    <automated>test -f play/components/tickets-panel.js && test -f play/components/packs-panel.js && test -f play/components/jackpot-panel-wrapper.js && ! test -f play/components/jackpot-panel.js && test -f play/assets/audio/pack-open.mp3 && node --check play/components/tickets-panel.js && node --check play/components/packs-panel.js && node --check play/components/jackpot-panel-wrapper.js && node --test play/app/__tests__/play-shell-01.test.js && node --test play/app/__tests__/play-jackpot-shell01-regression.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `test -f play/components/tickets-panel.js` exits 0; `wc -l` reports at least 140
    - `test -f play/components/packs-panel.js` exits 0; `wc -l` reports at least 180
    - `test -f play/components/jackpot-panel-wrapper.js` exits 0; `wc -l` reports at least 40
    - `test -f play/components/jackpot-panel.js` exits NON-zero (file DELETED per Pitfall 12)
    - `test -f play/assets/audio/pack-open.mp3` exits 0
    - Each new JS file passes `node --check`
    - `grep -c "customElements.define('tickets-panel'" play/components/tickets-panel.js` returns 1
    - `grep -c "customElements.define('packs-panel'" play/components/packs-panel.js` returns 1
    - `grep -c "customElements.define('jackpot-panel-wrapper'" play/components/jackpot-panel-wrapper.js` returns 1
    - `grep -c "from '../../beta/components/jackpot-panel.js'" play/components/jackpot-panel-wrapper.js` returns 1 (D-09 direct import)
    - `grep -c "<jackpot-panel>" play/components/jackpot-panel-wrapper.js` returns 1 (inner element)
    - `grep -c "game.level\\|game.jackpotDay" play/components/jackpot-panel-wrapper.js` returns at least 2
    - `grep -c "subscribe('replay.day'" play/components/tickets-panel.js` returns 1
    - `grep -c "subscribe('replay.player'" play/components/tickets-panel.js` returns 1
    - `grep -c "subscribe('replay.level'" play/components/tickets-panel.js` returns 1
    - `grep -c "subscribe('replay.day'" play/components/packs-panel.js` returns 1
    - `grep -c "subscribe('replay.player'" play/components/packs-panel.js` returns 1
    - `grep -c "subscribe('replay.level'" play/components/packs-panel.js` returns 1
    - `grep -c "#ticketsFetchId" play/components/tickets-panel.js` returns at least 4
    - `grep -c "#packsFetchId" play/components/packs-panel.js` returns at least 4
    - `grep -c "is-stale" play/components/tickets-panel.js` returns at least 3 (add + remove on success + remove on error path)
    - `grep -c "is-stale" play/components/packs-panel.js` returns at least 3
    - `grep -c "data-bind=\"card-grid\"\\|data-bind='card-grid'" play/components/tickets-panel.js` returns at least 2 (template + querySelector usage)
    - `grep -c "data-bind=\"pack-grid\"\\|data-bind='pack-grid'" play/components/packs-panel.js` returns at least 2
    - `grep -c "data-bind=\"mute-toggle\"\\|data-bind='mute-toggle'" play/components/packs-panel.js` returns at least 2
    - `grep -c "ticket-card" play/components/tickets-panel.js` returns at least 1
    - `grep -c "trait-quadrant" play/components/tickets-panel.js` returns at least 1
    - `grep -c "trait-grid" play/components/tickets-panel.js` returns at least 1
    - `grep -c "pack-sealed" play/components/packs-panel.js` returns at least 1
    - `grep -c "pack-source-purchase\\|pack-source-jackpot-win\\|pack-source-lootbox\\|pack-source-\\$" play/components/packs-panel.js` returns at least 1
    - `grep -c "animatedCards\\|#animatedCards" play/components/packs-panel.js` returns at least 3 (field + add + has)
    - `grep -c "activeTimelines\\|#activeTimelines" play/components/packs-panel.js` returns at least 3
    - `grep -c "from '../app/tickets-fetch.js'" play/components/tickets-panel.js` returns 1
    - `grep -c "from '../app/tickets-inventory.js'" play/components/tickets-panel.js` returns 1
    - `grep -c "from '../app/tickets-fetch.js'" play/components/packs-panel.js` returns 1
    - `grep -c "from '../app/pack-animator.js'" play/components/packs-panel.js` returns 1
    - `grep -c "from '../app/pack-audio.js'" play/components/packs-panel.js` returns 1
    - `grep -c "addEventListener('click'" play/components/packs-panel.js` returns at least 1
    - `grep -c "cardIndex\\|card-index" play/components/tickets-panel.js` returns at least 2
    - `grep -c "status === 'opened'\\|c.status === 'opened'" play/components/tickets-panel.js` returns at least 1
    - `grep -c "c.status !== 'opened'\\|status !== 'opened'" play/components/packs-panel.js` returns at least 1
    - `node --test play/app/__tests__/play-shell-01.test.js` exits 0
    - `node --test play/app/__tests__/play-jackpot-shell01-regression.test.js` exits 0
    - `node --test play/app/__tests__/play-profile-panel.test.js` exits 0 (Phase 51 still green)
  </acceptance_criteria>
  <done>
    Three new Custom Element files exist: tickets-panel.js (hydrated with card grid + stale-guard field + #refetch method), packs-panel.js (hydrated with pack grid + mute toggle + #animatedCards + #activeTimelines + click handler), jackpot-panel-wrapper.js (thin wrapper with direct beta import + replay-to-game shim). Phase 50 stub jackpot-panel.js is deleted. play/assets/audio/pack-open.mp3 exists. All three new files parse, pass SHELL-01, and turn the majority of Wave 0 behavior assertions green. The subscribe callback BODIES remain console.log stubs (like Phase 51 Wave 1 did); Wave 2 (Plan 52-03) flips them to this.#refetch() once INTEG-01 ships.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update play/index.html, play/app/main.js, play/app/__tests__/play-panel-stubs.test.js, and append Phase 52 CSS to play.css</name>
  <files>play/index.html, play/app/main.js, play/app/__tests__/play-panel-stubs.test.js, play/styles/play.css</files>
  <read_first>
    - play/index.html (full file; lines 7-13 CSS link list; lines 48-57 panel grid)
    - play/app/main.js (full file; lines 19-29 registerComponents paths array)
    - play/app/__tests__/play-panel-stubs.test.js (full file; PANEL_STUBS array around lines 24-31)
    - play/styles/play.css (full file; existing lines ~370; append Phase 52 sections at the end)
    - beta/styles/jackpot.css (no need to read contents; just confirm it exists so the <link> is valid)
    - beta/styles/replay.css lines 158-175 (.replay-tq .badge-img reference for trait-badge styling)
    - .planning/phases/52-tickets-packs-jackpot/52-PATTERNS.md sections "play/styles/play.css (MODIFY)" (line 962) and "play/index.html (MODIFY)" (line 1024)
    - .planning/phases/52-tickets-packs-jackpot/52-CONTEXT.md D-01 (card size), D-04 (tint mapping), D-07 (animation phases)
  </read_first>
  <action>
Four precise edits across four files. Use Edit tool for small surgical changes; use Write tool for the CSS append (append by reading the file, preserving existing content, then writing back with new sections at the end).

## Edit 1: `play/index.html` -- add jackpot.css link + tag rename + packs-panel add

### 1a: Add `<link>` to beta/styles/jackpot.css (Pitfall 13)

Find exactly:
```
  <link rel="stylesheet" href="/beta/styles/viewer.css">
  <link rel="stylesheet" href="/play/styles/play.css">
```

Replace with:
```
  <link rel="stylesheet" href="/beta/styles/viewer.css">
  <link rel="stylesheet" href="/beta/styles/jackpot.css">
  <link rel="stylesheet" href="/play/styles/play.css">
```

(jackpot.css inserted between viewer.css and play.css so play.css overrides can still win.)

### 1b: Update panel grid (rename jackpot-panel, add packs-panel)

Find exactly:
```
    <div class="play-grid">
      <profile-panel data-slot="profile"></profile-panel>
      <tickets-panel data-slot="tickets"></tickets-panel>
      <purchase-panel data-slot="purchase"></purchase-panel>
      <coinflip-panel data-slot="coinflip"></coinflip-panel>
      <baf-panel data-slot="baf"></baf-panel>
      <decimator-panel data-slot="decimator"></decimator-panel>
      <jackpot-panel data-slot="jackpot"></jackpot-panel>
      <!-- Each <*-panel> Custom Element renders its own skeleton with class skeleton-shimmer on connectedCallback -->
    </div>
```

Replace with:
```
    <div class="play-grid">
      <profile-panel data-slot="profile"></profile-panel>
      <packs-panel data-slot="packs"></packs-panel>
      <tickets-panel data-slot="tickets"></tickets-panel>
      <purchase-panel data-slot="purchase"></purchase-panel>
      <coinflip-panel data-slot="coinflip"></coinflip-panel>
      <baf-panel data-slot="baf"></baf-panel>
      <decimator-panel data-slot="decimator"></decimator-panel>
      <jackpot-panel-wrapper data-slot="jackpot"></jackpot-panel-wrapper>
      <!-- Each <*-panel> Custom Element renders its own skeleton with class skeleton-shimmer on connectedCallback -->
    </div>
```

## Edit 2: `play/app/main.js` -- update registerComponents paths array

Find exactly:
```
  const paths = [
    '../components/player-selector.js',
    '../components/day-scrubber.js',
    '../components/profile-panel.js',
    '../components/tickets-panel.js',
    '../components/purchase-panel.js',
    '../components/coinflip-panel.js',
    '../components/baf-panel.js',
    '../components/decimator-panel.js',
    '../components/jackpot-panel.js',
  ];
```

Replace with:
```
  const paths = [
    '../components/player-selector.js',
    '../components/day-scrubber.js',
    '../components/profile-panel.js',
    '../components/packs-panel.js',
    '../components/tickets-panel.js',
    '../components/purchase-panel.js',
    '../components/coinflip-panel.js',
    '../components/baf-panel.js',
    '../components/decimator-panel.js',
    '../components/jackpot-panel-wrapper.js',
  ];
```

## Edit 3: `play/app/__tests__/play-panel-stubs.test.js` -- update PANEL_STUBS array

Find exactly:
```
const PANEL_STUBS = [
  { file: 'profile-panel.js', tag: 'profile-panel' },
  { file: 'tickets-panel.js', tag: 'tickets-panel' },
  { file: 'purchase-panel.js', tag: 'purchase-panel' },
  { file: 'coinflip-panel.js', tag: 'coinflip-panel' },
  { file: 'baf-panel.js', tag: 'baf-panel' },
  { file: 'decimator-panel.js', tag: 'decimator-panel' },
  { file: 'jackpot-panel.js', tag: 'jackpot-panel' },
];
```

Replace with:
```
const PANEL_STUBS = [
  { file: 'profile-panel.js', tag: 'profile-panel' },
  { file: 'packs-panel.js', tag: 'packs-panel' },
  { file: 'tickets-panel.js', tag: 'tickets-panel' },
  { file: 'purchase-panel.js', tag: 'purchase-panel' },
  { file: 'coinflip-panel.js', tag: 'coinflip-panel' },
  { file: 'baf-panel.js', tag: 'baf-panel' },
  { file: 'decimator-panel.js', tag: 'decimator-panel' },
  { file: 'jackpot-panel-wrapper.js', tag: 'jackpot-panel-wrapper' },
];
```

(Same 7 entries rearranged + tag rename; adds packs-panel at slot 2 matching the index.html grid; replaces jackpot-panel with jackpot-panel-wrapper at the end.)

## Edit 4: `play/styles/play.css` -- append Phase 52 sections

Append the following block to the END of the existing file (preserve ALL existing content; simply add these sections below the current last line). Use the Write tool after reading the existing file and concatenating; OR use a text-append approach via Edit with the last existing line as the "find" anchor.

CSS to append (copy verbatim, including comments and section headers):

```css

/* ======================================================================
   Phase 52 -- <tickets-panel> styles
   Dense card grid (D-01): ~140px cards, 2x2 trait SVG grid per card.
   Reuses skeleton.css + panels.css tokens. Borrows sizing conventions
   from beta/styles/replay.css .replay-tq .badge-img.
   ====================================================================== */

.tickets-panel .panel-title {
  font-size: 1rem;
  font-weight: 600;
  margin: 0 0 0.75rem 0;
  color: var(--text-primary, #f0f0f0);
}

.tickets-panel .tickets-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
  gap: 0.75rem;
  min-height: 140px;
}

.tickets-panel .tickets-empty {
  grid-column: 1 / -1;
  padding: 2rem 1rem;
  text-align: center;
  color: var(--text-muted, #888);
  font-size: 0.9rem;
}

.ticket-card {
  background: var(--panel-bg, #1a1a1a);
  border: 1px solid var(--panel-border, #333);
  border-radius: 8px;
  padding: 0.5rem;
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.ticket-card .trait-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  grid-template-rows: 1fr 1fr;
  gap: 0.25rem;
  aspect-ratio: 1 / 1;
}

.ticket-card .trait-quadrant {
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(0, 0, 0, 0.2);
  border-radius: 4px;
  padding: 4%;
}

.ticket-card .trait-quadrant-empty {
  background: rgba(0, 0, 0, 0.35);
}

.ticket-card .trait-badge {
  width: 100%;
  height: 100%;
  object-fit: contain;
  display: block;
}

.card-source-pill {
  display: inline-block;
  align-self: flex-start;
  font-size: 0.7rem;
  font-weight: 600;
  padding: 0.15rem 0.5rem;
  border-radius: 999px;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.card-source-purchase {
  background: rgba(240, 240, 240, 0.1);
  color: var(--text-muted, #888);
}

.card-source-jackpot-win {
  background: rgba(255, 215, 0, 0.18);
  color: #f5c518;
}

.card-source-lootbox {
  background: rgba(128, 90, 255, 0.18);
  color: #b388ff;
}

.tickets-panel [data-bind="content"] {
  transition: opacity 0.2s ease;
}

.tickets-panel [data-bind="content"].is-stale {
  opacity: 0.6;
}

/* ======================================================================
   Phase 52 -- <packs-panel> styles
   Sealed pack state with pulsing "tap to open" affordance; opened state
   after GSAP timeline completes; lootbox auto-open tint.
   ====================================================================== */

.packs-panel .packs-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 0.75rem;
}

.packs-panel .panel-title {
  font-size: 1rem;
  font-weight: 600;
  margin: 0;
  color: var(--text-primary, #f0f0f0);
}

.packs-panel .mute-toggle {
  background: transparent;
  border: 1px solid var(--panel-border, #333);
  color: var(--text-muted, #888);
  border-radius: 999px;
  padding: 0.25rem 0.75rem;
  font-size: 0.75rem;
  cursor: pointer;
  transition: color 0.2s ease, border-color 0.2s ease;
}

.packs-panel .mute-toggle:hover,
.packs-panel .mute-toggle:focus {
  color: var(--text-primary, #f0f0f0);
  border-color: var(--text-primary, #f0f0f0);
  outline: none;
}

.packs-panel .mute-toggle[aria-pressed="true"] {
  color: #ff6b6b;
  border-color: #ff6b6b;
}

.packs-panel .packs-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
  gap: 0.75rem;
  min-height: 140px;
}

.packs-panel .packs-empty {
  grid-column: 1 / -1;
  padding: 2rem 1rem;
  text-align: center;
  color: var(--text-muted, #888);
  font-size: 0.9rem;
}

.pack-sealed {
  background: var(--panel-bg, #1a1a1a);
  border: 1px solid var(--panel-border, #333);
  border-radius: 8px;
  padding: 0.5rem;
  cursor: pointer;
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
  position: relative;
  animation: pack-pulse 2s ease-in-out infinite;
  user-select: none;
}

.pack-sealed:focus {
  outline: 2px solid var(--text-primary, #f0f0f0);
  outline-offset: 2px;
}

.pack-sealed .pack-wrapper {
  display: grid;
  grid-template-columns: 1fr 1fr;
  grid-template-rows: 1fr 1fr;
  gap: 0.25rem;
  aspect-ratio: 1 / 1;
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.06), rgba(255, 255, 255, 0.02));
  border-radius: 6px;
  padding: 4%;
}

.pack-sealed .pack-trait {
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(0, 0, 0, 0.25);
  border-radius: 4px;
  opacity: 0.3;
}

.pack-sealed .pack-trait .trait-badge {
  width: 100%;
  height: 100%;
  object-fit: contain;
  opacity: 0.85;
}

.pack-sealed.pack-source-jackpot-win {
  border-color: rgba(255, 215, 0, 0.5);
  box-shadow: 0 0 0 0 rgba(255, 215, 0, 0.4);
  animation-name: pack-pulse-gold;
}

.pack-sealed.pack-source-lootbox {
  border-color: rgba(128, 90, 255, 0.5);
  animation-name: pack-pulse-lootbox;
}

.pack-opened {
  background: var(--panel-bg, #1a1a1a);
  border: 1px solid var(--panel-border, #333);
  border-radius: 8px;
  padding: 0.5rem;
  animation: none;
}

.pack-opened .pack-trait {
  opacity: 1;
}

@keyframes pack-pulse {
  0%, 100% { box-shadow: 0 0 0 0 rgba(240, 240, 240, 0.2); }
  50% { box-shadow: 0 0 0 6px rgba(240, 240, 240, 0); }
}

@keyframes pack-pulse-gold {
  0%, 100% { box-shadow: 0 0 0 0 rgba(255, 215, 0, 0.4); }
  50% { box-shadow: 0 0 0 8px rgba(255, 215, 0, 0); }
}

@keyframes pack-pulse-lootbox {
  0%, 100% { box-shadow: 0 0 0 0 rgba(128, 90, 255, 0.4); }
  50% { box-shadow: 0 0 0 8px rgba(128, 90, 255, 0); }
}

.packs-panel [data-bind="content"] {
  transition: opacity 0.2s ease;
}

.packs-panel [data-bind="content"].is-stale {
  opacity: 0.6;
}

/* prefers-reduced-motion fallback (D-07) -- kill the pulse. Instant
   state swap in the GSAP animator path is handled in pack-animator.js;
   this block only affects the sealed-state pulse. */
@media (prefers-reduced-motion: reduce) {
  .pack-sealed,
  .pack-sealed.pack-source-jackpot-win,
  .pack-sealed.pack-source-lootbox {
    animation: none;
  }
  .tickets-panel [data-bind="content"],
  .packs-panel [data-bind="content"] {
    transition-duration: 0.01ms;
  }
}

/* --- Mobile (<= 720px) ------------------------------------------------ */

@media (max-width: 720px) {
  .tickets-panel .tickets-grid,
  .packs-panel .packs-grid {
    grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
    gap: 0.5rem;
  }
  .ticket-card,
  .pack-sealed,
  .pack-opened {
    padding: 0.4rem;
  }
}
```

## Self-check after all four edits

- `play/index.html` has `/beta/styles/jackpot.css` link.
- `play/index.html` has `<jackpot-panel-wrapper>` (NOT `<jackpot-panel>`).
- `play/index.html` has `<packs-panel>` immediately after `<profile-panel>`.
- `play/app/main.js` paths array has `packs-panel.js` and `jackpot-panel-wrapper.js`; does NOT have bare `jackpot-panel.js`.
- `play/app/__tests__/play-panel-stubs.test.js` PANEL_STUBS array has 8 entries (added packs-panel) and replaces jackpot-panel with jackpot-panel-wrapper.
- `play/styles/play.css` has Phase 52 sections appended; existing Phase 51 content preserved.
- All existing tests continue to pass.
  </action>
  <verify>
    <automated>node --test play/app/__tests__/play-shell-01.test.js && node --test play/app/__tests__/play-panel-stubs.test.js && node --test play/app/__tests__/play-route-structure.test.js && node --test play/app/__tests__/play-main-bootstrap.test.js && node --test play/app/__tests__/play-profile-panel.test.js && node --test play/app/__tests__/play-jackpot-shell01-regression.test.js && node --test play/app/__tests__/play-tickets-panel.test.js && node --test play/app/__tests__/play-packs-panel.test.js && node --test play/app/__tests__/play-jackpot-wrapper.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "beta/styles/jackpot.css" play/index.html` returns 1
    - `grep -c "<jackpot-panel-wrapper" play/index.html` returns 1
    - `! grep -q "<jackpot-panel " play/index.html` exits 0 (old tag gone except any html comments; main grep is on the opening-tag form `<jackpot-panel ` or `<jackpot-panel>`)
    - `grep -c "<packs-panel" play/index.html` returns 1
    - `grep -c "packs-panel.js" play/app/main.js` returns 1
    - `grep -c "jackpot-panel-wrapper.js" play/app/main.js` returns 1
    - `! grep -q "'../components/jackpot-panel.js'" play/app/main.js` exits 0 (bare jackpot-panel.js gone from paths array)
    - `grep -c "file: 'packs-panel.js', tag: 'packs-panel'" play/app/__tests__/play-panel-stubs.test.js` returns 1
    - `grep -c "file: 'jackpot-panel-wrapper.js', tag: 'jackpot-panel-wrapper'" play/app/__tests__/play-panel-stubs.test.js` returns 1
    - `! grep -q "file: 'jackpot-panel.js', tag: 'jackpot-panel'" play/app/__tests__/play-panel-stubs.test.js` exits 0 (old stub entry removed)
    - `grep -c "^\\.tickets-panel\\|^\\.packs-panel\\|^\\.ticket-card\\|^\\.pack-sealed\\|^\\.pack-opened" play/styles/play.css` returns at least 8 (the Phase 52 CSS selectors)
    - `grep -c "@keyframes pack-pulse" play/styles/play.css` returns at least 1
    - `grep -c "prefers-reduced-motion" play/styles/play.css` returns at least 2 (existing Phase 51 + new Phase 52 @media)
    - `grep -c ".card-source-purchase\\|.card-source-jackpot-win\\|.card-source-lootbox" play/styles/play.css` returns at least 3
    - `wc -l play/styles/play.css` reports at least 520 lines (existing 370 + appended ~200)
    - `node --test play/app/__tests__/play-shell-01.test.js` exits 0
    - `node --test play/app/__tests__/play-panel-stubs.test.js` exits 0 (updated array matches new files)
    - `node --test play/app/__tests__/play-route-structure.test.js` exits 0 (structural checks pass with new CSS link + grid entries)
    - `node --test play/app/__tests__/play-main-bootstrap.test.js` exits 0
    - `node --test play/app/__tests__/play-profile-panel.test.js` exits 0 (Phase 51 green)
    - `node --test play/app/__tests__/play-jackpot-shell01-regression.test.js` exits 0 (D-09 patch held)
    - `node --test play/app/__tests__/play-tickets-panel.test.js` -- most assertions green; subscribe-body assertions that grep for #refetch() in callback remain red until Wave 2 (ALLOWED -- this plan does not claim Wave 2 assertions)
    - `node --test play/app/__tests__/play-packs-panel.test.js` -- same Wave 1 posture
    - `node --test play/app/__tests__/play-jackpot-wrapper.test.js` -- most assertions green; subscribe-body assertions green too because the wrapper's subscribes ARE wired live (not stubbed)
  </acceptance_criteria>
  <done>
    play/index.html links jackpot.css, uses <jackpot-panel-wrapper> and <packs-panel> in the grid. play/app/main.js registerComponents paths reference packs-panel + jackpot-panel-wrapper, not the old jackpot-panel stub. play/app/__tests__/play-panel-stubs.test.js PANEL_STUBS array matches the new file set. play/styles/play.css has ~320 new Phase 52 lines for tickets/packs grids, sources tints, pack-sealed + pack-opened states, keyframes, and reduced-motion fallback. All seven pre-existing tests pass (play-shell-01, play-panel-stubs, play-route-structure, play-main-bootstrap, play-profile-panel, play-jackpot-shell01-regression, plus the three Phase 52 behavior tests all run without throwing -- Wave 2 flips the remaining subscribe-body assertions).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INTEG-01 response -> browser DOM | Wave 1 renderers use `document.createElement + textContent` for every string interpolated from the response (traitLabel, source). No `.innerHTML` with untrusted data. Deterministic paths (traitToBadge) use known-safe filesystem strings. |
| GSAP timeline -> DOM | Timelines target specific elements by class selector; animations mutate style properties only. No script injection surface. |
| localStorage -> pack-audio | mute flag is `'1'` or `'0'` only; any other value reads as false. No deserialization risk. |
| AudioContext fetch -> decodeAudioData | `/play/assets/audio/pack-open.mp3` fetched from same origin; decode happens in-browser sandbox. Malformed MP3 throws -> fail-silent path catches. |
| beta/components/jackpot-panel.js (D-09 patched) -> play/ | Direct import. Any re-introduction of a wallet-tainted import in beta is caught by play-jackpot-shell01-regression.test.js. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-10 | Tampering | INTEG-01 response fields interpolated into DOM | mitigate | Renderers use document.createElement + textContent for all response-derived strings (traitLabel, source). innerHTML is only the static TEMPLATE constant. |
| T-52-11 | Information disclosure | localStorage 'play.audio.muted' | accept | Single boolean; no PII or sensitive data. |
| T-52-12 | Denial of Service | GSAP tween storage on rapid day-scrub | mitigate | #activeTimelines Set + .kill() on disconnectedCallback (Pitfall 3 guard). Per-card removal is fine; whole-panel teardown cleaned up explicitly. |
| T-52-13 | Spoofing | INTEG-01 source enum | mitigate | Class names derived from source string (pack-source-{source}, card-source-{source}). Unknown source value renders as unknown class, no crash; CSS tints default to neutral. |
| T-52-14 | Elevation of Privilege | beta/jackpot-panel direct import re-taint | mitigate | play-jackpot-shell01-regression.test.js asserts the post-D-09 patched state on every CI run. Reverts caught within one cycle. |
| T-52-15 | Tampering | Phase 50 stub deletion timing vs panel-stubs test | mitigate | Task 2 deletes the stub; Task 3 updates PANEL_STUBS array. Both land in the same plan execution; no broken intermediate commit (Pitfall 7 + Pitfall 12 concurrent-fix discipline). |
| T-52-16 | Information disclosure | Audio autoplay unlock gesture | accept | Lootbox auto-open sound blocked until first user gesture (Pitfall 4). No data disclosure; user experience degrades gracefully. |
| T-52-17 | Denial of Service | pack-audio ensureLoaded on 404 or unsupported API | mitigate | Single console.warn + loadError flag + early-return in playPackOpen (D-10 fail-silent). No repeated fetch attempts. |
| T-52-18 | Spoofing | localStorage API unavailable (privacy mode) | mitigate | try/catch around localStorage.getItem/setItem; falls back to session-memory-only state. No crash. |
</threat_model>

<verification>
- All three Custom Elements parse and register their tags.
- The Phase 50 jackpot-panel.js stub is deleted; the customElements.define collision cannot occur (Pitfall 12).
- play/app/__tests__/play-panel-stubs.test.js is kept in sync with the delete (Pitfall 7 concurrent-fix).
- play/index.html has the beta/styles/jackpot.css link (Pitfall 13) and uses the new tags.
- play/app/main.js registerComponents references the new files.
- Phase 52 CSS blocks are appended; existing Phase 51 CSS preserved.
- All pre-Wave-1 tests remain green; the three new Wave 0 test files go mostly green (subscribe-body assertions remain RED for tickets-panel and packs-panel, intentionally, until Wave 2).
- play-shell-01.test.js recursive guardrail passes -- no new play/ file imports forbidden paths.
</verification>

<success_criteria>
- [ ] Four helper modules created in play/app/ (tickets-inventory, tickets-fetch, pack-animator, pack-audio); each parses, each wallet-free, each exports the expected surface
- [ ] tickets-inventory.js contains `CARD_IDX = [3, 4, 5, 6, 0, 2, 1, 7]` exactly (Pitfall 1 + A1 regression guard)
- [ ] Three Custom Elements created in play/components/: tickets-panel.js, packs-panel.js, jackpot-panel-wrapper.js; each parses; each registers its tag; each imports helpers from Task 1
- [ ] play/components/jackpot-panel.js DELETED (Pitfall 12)
- [ ] play/assets/audio/pack-open.mp3 EXISTS (even as placeholder; fail-silent path tolerates 404)
- [ ] play/index.html: links jackpot.css, uses <jackpot-panel-wrapper> and <packs-panel>, does not use the old <jackpot-panel> tag in the grid
- [ ] play/app/main.js: paths array has packs-panel.js + jackpot-panel-wrapper.js; old jackpot-panel.js removed
- [ ] play/app/__tests__/play-panel-stubs.test.js: PANEL_STUBS array updated (8 entries; jackpot-panel-wrapper replaces jackpot-panel; packs-panel added)
- [ ] play/styles/play.css: Phase 52 sections appended; existing Phase 51 content preserved; @keyframes pack-pulse defined
- [ ] All Wave 1 tests pass: play-shell-01, play-panel-stubs, play-route-structure, play-main-bootstrap, play-profile-panel, play-jackpot-shell01-regression
- [ ] The three Phase 52 behavior tests (play-tickets-panel, play-packs-panel, play-jackpot-wrapper) run without crashing; most assertions green; subscribe-body assertions (if any) reserved for Wave 2 to flip
- [ ] CONTEXT.md D-01..D-10 all referenced in code comments where they drive behavior (D-02 trait SVG rendering, D-03 pending/partial/opened states, D-04 source tints, D-05 click trigger, D-06 lootbox auto-open, D-07 GSAP phases, D-08 day-change mid-animation, D-09 direct beta import, D-10 audio parameters)
- [ ] SHELL-01 recursive guardrail green: no new play/ file introduces a forbidden import
</success_criteria>

<output>
After completion, create `.planning/phases/52-tickets-packs-jackpot/52-02-SUMMARY.md` per the standard template, including:
- Files created (11): 4 helpers + 3 components + 1 audio asset + (note in-place edits to index.html/main.js/play.css/panel-stubs.test.js)
- Files deleted (1): play/components/jackpot-panel.js (Phase 50 stub)
- LOC delta: approximately +900 LOC play/ code + +320 LOC play.css
- Wave 1 test state: play-shell-01 green; play-panel-stubs green (updated array); Phase 52 behavior tests mostly green (subscribe-body assertions reserved for Wave 2)
- D-09 direct import working: play/components/jackpot-panel-wrapper.js imports beta/components/jackpot-panel.js without SHELL-01 violation (patched import in line 7 held)
- Remaining Wave 2 RED assertions: any test that greps for `#refetch()` inside the subscribe callback body (currently console.log in Wave 1); this is the intentional partition point
- Phase-52 readiness for Wave 2 hard gate
</output>

## Plan Summary

**Objective:** Wave 1 pre-backend scaffolding -- all UI/helpers/CSS/HTML/boot updates for Phase 52; panels have #refetch methods ready for Wave 2 to wire.

**Tasks:**
1. Four wallet-free helpers: tickets-inventory (CARD_IDX decomposer), tickets-fetch (in-flight dedup), pack-animator (GSAP 4-phase timeline), pack-audio (Web Audio + localStorage mute)
2. Three Custom Elements + stub delete + audio asset: <tickets-panel>, <packs-panel>, <jackpot-panel-wrapper>; delete Phase 50 jackpot-panel.js stub; create pack-open.mp3 placeholder
3. Four in-place file edits: index.html (css link + tag rename + packs-panel add), main.js (paths array), panel-stubs test (PANEL_STUBS array), play.css (Phase 52 sections appended)

**Verify chain:**
- Task 1 produces helpers Task 2 imports
- Task 2 creates panels Task 3's index.html references + deletes old stub Task 3's panel-stubs test removes
- All three tasks land together (no intermediate commits) to avoid broken Phase 50 stub test window

**Context cost estimate:** ~45% (heavy file authorship; 4+3+4 = 11 file operations; most content pre-specified in PATTERNS.md/RESEARCH.md with copy-paste excerpts)

**Duration:** Single execute-plan run, ~4-6 minutes.
