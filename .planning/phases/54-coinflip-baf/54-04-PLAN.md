---
phase: 54
plan: 04
slug: wave3-uat-and-polish
type: execute
wave: 3
depends_on:
  - 54-03-PLAN.md
files_modified:
  - .planning/phases/54-coinflip-baf/54-UAT.md
autonomous: false
requirements:
  - COINFLIP-01
  - COINFLIP-02
  - COINFLIP-03
  - BAF-01
  - BAF-02
  - BAF-03
tags:
  - phase-54
  - wave-3
  - uat
  - manual-verify
  - optional

must_haves:
  truths:
    - "A human has clicked through the manual-only verifications from 54-VALIDATION.md against real data AND recorded pass/fail per scenario OR the UAT is formally deferred with a written rationale mirroring Phase 50+51+52+53 deferral precedent"
    - "BAF prominence tier colors (rank 1 gold #FFD700, rank 2 silver #C0C0C0, rank 3 bronze #CD7F32, rank 4 regular per D-06) visually confirmed on desktop against a level with >= 4 BAF participants (level 20 is known-populated per 54-RESEARCH)"
    - "Panel-level data-prominence border (high accent 2px at levelsUntilBaf <= 3 OR isBafLevel; medium secondary at 4-7 levels away; low 80% opacity at 8+) visually confirmed across level range 1-21"
    - "Round-status pill color-coding distinct for 4 states: open green (default BAF-active), closed dim gray (post-jackpot), skipped red (coinflip lost), not_eligible dim (non-multiple-of-10 level)"
    - "'Your rank: N of M' row un-hides for non-top-4 players at BAF-active levels after INTEG-05 ships; stays hidden when rank is null"
    - "Coinflip bounty armed-pulse animation plays when currentBounty > 0; static 'NOT ARMED' when zero; biggestFlipPlayer null renders '--' placeholder gracefully (no 'null' literal)"
    - "Coinflip leaderboard integer-BURNIE rendering verified (score '52875' shows as '52875 BURNIE', NOT '0.000000000000052875 BURNIE' -- Pitfall 8 regression check)"
    - "Selected-player highlighting (aria-current=true + CSS bold + underline) works on both panels when the chosen player is in their respective top-4/top-10"
    - "Rapid level-scrub (BAF) and rapid day-scrub (coinflip) stability: is-stale 0.6 opacity dim applied, latest data always wins, no flashing between stale states"
    - "Any issues found are recorded in 54-UAT.md with enough detail (what, where, expected, observed, severity) that a follow-up gap-closure plan could address them without re-investigation"
  artifacts:
    - path: ".planning/phases/54-coinflip-baf/54-UAT.md"
      provides: "UAT record: scenarios tested, player/day/level tuples used, device/browser, pass/fail per scenario, notes on issues OR formal deferral note with rationale citing Phase 50/51/52/53 precedent chain"
      min_lines: 15
  key_links:
    - from: ".planning/phases/54-coinflip-baf/54-UAT.md"
      to: "54-VALIDATION.md Manual-Only Verifications table (lines 94-106)"
      via: "each row gets a pass/fail entry OR a deferral acknowledgment"
      pattern: "Manual-Only|UAT"
---

<objective>
Wave 3 closes Phase 54 with a manual UAT pass (or formal deferral, per Phase 50/51/52/53 precedent chain). The scenarios in 54-VALIDATION.md Manual-Only Verifications table cannot be automated (prominence tier color fidelity, round-status pill visual distinction, selected-player highlight visibility, rapid-scrub stability, armed-pulse animation feel). A human (the user) clicks through each scenario against the live stack and records pass/fail, OR formally defers UAT as per all four prior Phase-5x plans.

Purpose: Automated tests prove the code contract is satisfied (~58 new + pre-existing ~230 = ~288 play/ test assertions green after Wave 2). UAT proves the code ACTUALLY WORKS end-to-end visually on a real device. This is the final gate before marking Phase 54 complete.

This plan is OPTIONAL. If the user prefers to defer UAT to a later pass (as happened in Phase 50 -> 51 -> 52 -> 53 consecutively), the plan can be short-circuited and the phase marked complete after Plan 03 plus a 54-UAT.md note of deferral.

Output:
- `.planning/phases/54-coinflip-baf/54-UAT.md` -- a record of the UAT session (scenarios exercised, device/browser, player+level+day tuples, pass/fail per scenario, issue notes) OR a short deferral note with rationale mirroring Phase 51+52+53.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-coinflip-baf/54-CONTEXT.md
@.planning/phases/54-coinflip-baf/54-VALIDATION.md
@.planning/phases/54-coinflip-baf/54-RESEARCH.md
@.planning/phases/54-coinflip-baf/54-01-PLAN.md
@.planning/phases/54-coinflip-baf/54-02-PLAN.md
@.planning/phases/54-coinflip-baf/54-03-PLAN.md

<!-- Deferral precedent chain -->
@.planning/phases/50-route-foundation-day-aware-store/50-03-SUMMARY.md
@.planning/phases/51-profile-quests/51-UAT.md
@.planning/phases/52-tickets-packs-jackpot/52-UAT.md
@.planning/phases/53-purchase-flow/53-01-SUMMARY.md

<interfaces>
<!-- Manual-Only Verifications from 54-VALIDATION.md lines 94-106 -->

| # | Behavior | Requirement | Why Manual | Instructions |
|---|----------|-------------|------------|--------------|
| 1 | Prominence tier colors visual fidelity (rank 1 gold, 2 silver, 3 bronze, 4 regular) | BAF-02 / D-06 | Visual color accuracy; CSS var resolution on real theme | Load /play/; scrub to level 20 (known-populated). Verify rank 1 row uses #FFD700 or similar gold-tone; rank 2 silver; rank 3 bronze; rank 4 default theme color. |
| 2 | Panel-level data-prominence border transitions | BAF-03 (feel) | Visual feedback over level range | Scrub levels 1 -> 21. At levels 7-10 border turns accent-primary 2px (high). At levels 3-7 border shows text-secondary (medium). At levels >= 8 from next BAF, panel dims to 80% opacity (low). At level 10 or 20 BAF-active, border stays high. |
| 3 | Round-status pill visual states | BAF-03 / D-09 | 4 distinct colors need human eye | Scrub to levels in each state: (a) current open BAF level -> pill green "OPEN"; (b) past closed BAF level with eth_baf distribution -> pill gray "CLOSED"; (c) past skipped BAF level (baf_skipped row) -> pill red "SKIPPED"; (d) non-multiple-of-10 level -> pill dim "NOT ELIGIBLE". |
| 4 | 'Your rank: N of M' row rendering for non-top-4 | BAF-01 | INTEG-05 wiring + hidden-vs-visible toggle | Pick a player whose rank > 4 at a BAF-active level (level 20). Below the top-4 leaderboard, a row un-hides showing "You: rank 7 of 42 -- {score} BURNIE". Selecting a player NOT in the BAF at that level keeps the row hidden. |
| 5 | Selected-player highlighting in both leaderboards | COINFLIP-02 + BAF-02 | aria-current + CSS bold + underline visual inspection | Pick a player known to be in top-10 coinflip at a given day (e.g., day 64 rank 1 = 0xfabb0ac9d68b0b445fb7357272ff202c5651694a per RESEARCH). Row should be bold + underlined. Pick a player in top-4 BAF at level 20. Row should be highlighted. |
| 6 | Coinflip bounty armed-pulse animation | COINFLIP-03 | CSS keyframe animation feel | Scrub to a day where currentBounty > 0 in the /player/:addr?day= coinflip block. The "ARMED" indicator pill should pulse (opacity 1 -> 0.5 -> 1 over 2s). When currentBounty = 0, static "NOT ARMED" with gray background. |
| 7 | Coinflip leaderboard integer-BURNIE rendering (Pitfall 8 regression) | COINFLIP-02 | Score unit is CRITICAL bug to catch | Scrub to day 64 (known-populated). Leaderboard scores should render as raw integers: '52875 BURNIE' (rank 1), '43590 BURNIE' (rank 2), etc. If any score appears as '0.000000000000052875 BURNIE' that is the Pitfall 8 regression (integer misinterpreted as wei) -- file as P0. |
| 8 | BAF leaderboard wei-BURNIE rendering | BAF-02 | Score unit (opposite of coinflip) | Scrub to level 20. Leaderboard scores should render as '475,215 BURNIE' (rank 1), '393,272 BURNIE' (rank 2), etc -- all via formatBurnie. If any score appears as '475215212469240581904067 BURNIE' (raw wei string) that is the opposite regression -- file as P0. |
| 9 | Rapid level-scrub (BAF) stability | BAF-02 / BAF-01 | Dual stale-guard timing | Scrub levels 10 -> 30 in under 2 seconds. Panel dims via is-stale; final level's top-4 + your-rank row match the final selection (no mid-scrub flash). |
| 10 | Rapid day-scrub (coinflip) stability | COINFLIP-01/02/03 | 3-checkpoint stale-guard | Scrub days 50 -> 70 in under 2 seconds. Coinflip panel dims; final day's bounty + state + leaderboard render correctly (no artifacts from intermediate days). |
| 11 | Empty state -- non-multiple-of-10 BAF level | BAF-02 / BAF-03 | Edge case | Scrub to level 7. Leaderboard body shows "BAF active only at every 10th level." or similar; round-status pill dim "NOT ELIGIBLE". |
| 12 | Empty state -- no coinflip activity for day | COINFLIP-02 | Edge case | Pick a day with 0 coinflip activity. Leaderboard shows "No coinflip activity for day N". No broken UI. |

<!-- Dev server setup -->
Website: python3 -m http.server 8080 in /home/zak/Dev/PurgeGame/website/
API: Fastify at localhost:3000 in /home/zak/Dev/PurgeGame/database/
Browser URL: http://localhost:8080/play/

<!-- Test-player discovery -->
curl -s http://localhost:3000/replay/players | python3 -c "import json,sys; d=json.load(sys.stdin); print('\\n'.join(d['players'][:10]))"

<!-- BAF-active level discovery (Scenarios 1-4) -->
curl -s http://localhost:3000/leaderboards/baf?level=20 | python3 -m json.tool | head -30
curl -s http://localhost:3000/leaderboards/baf?level=10 | python3 -m json.tool | head -30

<!-- Coinflip active day discovery (Scenarios 5, 6, 7) -->
curl -s http://localhost:3000/leaderboards/coinflip?day=64 | python3 -m json.tool | head -30

<!-- INTEG-05 probe for Scenario 4 (after Wave 2 ships) -->
PLAYER=0x...; LEVEL=20
curl -s "http://localhost:3000/player/$PLAYER/baf?level=$LEVEL" | python3 -m json.tool

<!-- Deferral precedent chain -->
Phase 50 deferred its UAT to Phase 51 naturally; Phase 51 deferred its UAT to Phase 52 naturally; Phase 52 deferred its UAT (52-UAT.md: DEFERRED, browser UAT unavailable); Phase 53 deferred (automated verification sufficient; 53-01-SUMMARY.md). Phase 54 may similarly defer to Phase 55 (Decimator) natural integration -- once Decimator ships, a real-user session exercising all panels at once will surface any cross-panel visual regression.
</interfaces>
</context>

<read_first>
- .planning/phases/54-coinflip-baf/54-VALIDATION.md (Manual-Only Verifications table at lines 94-106 -- the exact scenarios; every row is a candidate for pass/fail recording)
- .planning/phases/54-coinflip-baf/54-CONTEXT.md D-06 (prominence tier colors), D-07 (coinflip simple table), D-08 (bounty above leaderboard), D-09 (roundStatus pill)
- .planning/phases/54-coinflip-baf/54-RESEARCH.md Section 13 Pitfalls (12 pitfalls; Pitfall 7 bounty-null + Pitfall 8 score-unit are UAT-relevant)
- .planning/phases/51-profile-quests/51-UAT.md (Phase 51 deferral template; the structural shape if Phase 54 also defers)
- .planning/phases/52-tickets-packs-jackpot/52-UAT.md (Phase 52 deferral shape; most recent precedent)
- .planning/phases/53-purchase-flow/53-01-SUMMARY.md (Phase 53 UAT status -- per STATE.md this phase deferred UAT)
- CLAUDE.md (no em dashes, no emojis in any new file; use `--`)
</read_first>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 (CHECKPOINT): Run UAT scenarios against real data and record findings</name>
  <what-built>
    Wave 2 completed Phase 54: INTEG-05 endpoint shipped in database repo, baf-panel.js wired to the live response. All automated tests green (13 play/ test files including play-coinflip-panel + play-baf-panel).

    Twelve behaviors cannot be fully automated:
    1. Prominence tier colors (gold/silver/bronze/regular) -- visual color fidelity
    2. Panel-level data-prominence border transitions -- feel across level range
    3. Round-status pill visual states (open/closed/skipped/not_eligible colors)
    4. "Your rank N of M" row un-hide/hide timing (INTEG-05 wiring)
    5. Selected-player highlighting in both leaderboards
    6. Coinflip bounty armed-pulse animation (CSS keyframe feel)
    7. Coinflip leaderboard integer-BURNIE rendering (Pitfall 8 regression check)
    8. BAF leaderboard wei-BURNIE rendering (opposite Pitfall 8 regression)
    9. Rapid level-scrub stability (BAF dual stale-guard)
    10. Rapid day-scrub stability (coinflip 3-checkpoint stale-guard)
    11. Empty state at level % 10 != 0
    12. Empty state for zero-activity coinflip days

    This checkpoint asks the user (or a helper) to perform a UAT pass covering these scenarios and record the findings in `.planning/phases/54-coinflip-baf/54-UAT.md`.

    The checkpoint supports three terminal states:
    - `uat-pass` -- all scenarios verified; 54-UAT.md records the run; Phase 54 is fully validated.
    - `uat-partial -- [issues]` -- some scenarios failed; 54-UAT.md records pass-runs + failures with enough detail to file follow-up gap-closure plan.
    - `uat-deferred -- [reason]` -- user defers UAT; 54-UAT.md records the deferral with rationale. Precedent: Phase 50 -> 51 -> 52 -> 53 all deferred. Phase 54 can defer to Phase 55 (Decimator) natural integration (the final panel; once all six are live a real-user session will surface any cross-panel visual regression).
  </what-built>
  <how-to-verify>
    **Setup:**

    1. Start the database dev server (Fastify at localhost:3000). Database repo: `/home/zak/Dev/PurgeGame/database/`. Confirm INTEG-05 endpoint is live:
       ```
       curl -s http://localhost:3000/player/0xfabb0ac9d68b0b445fb7357272ff202c5651694a/baf?level=20 | python3 -m json.tool
       ```
       Expect the INTEG-05 response shape.

    2. Start a static file server at website root:
       ```
       cd /home/zak/Dev/PurgeGame/website
       python3 -m http.server 8080
       ```

    3. Open http://localhost:8080/play/ in Chrome (desktop). Mobile test not required for Phase 54 -- no mobile-specific features (D-02 no wallet; read-only display).

    4. Query available players + BAF-active levels + coinflip-active days:
       ```
       curl -s http://localhost:3000/replay/players | python3 -c "import json,sys; d=json.load(sys.stdin); print('\\n'.join(d['players'][:10]))"
       curl -s "http://localhost:3000/leaderboards/baf?level=20" | python3 -m json.tool | head -15
       curl -s "http://localhost:3000/leaderboards/coinflip?day=64" | python3 -m json.tool | head -20
       ```

    **Scenario 1 -- Prominence tier colors (BAF-02 / D-06):**

    1. Pick a test player with BAF stake. Scrub to level 20 (known-populated).
    2. Inspect the BAF panel top-4 leaderboard. Use DevTools color picker to verify:
       - Rank 1 row: color approximately #FFD700 (gold), font-weight 700, font-size 0.95rem
       - Rank 2 row: approximately #C0C0C0 (silver), font-weight 600
       - Rank 3 row: approximately #CD7F32 (bronze), font-weight 600
       - Rank 4 row: theme --text-primary (regular), font-weight 500
    3. Zoom out and confirm the visual hierarchy is obvious without DevTools (gold > silver > bronze > regular at a glance).

    **Scenario 2 -- Panel-level data-prominence border (BAF-03 feel):**

    1. Inspect the BAF panel wrapper element in DevTools; confirm data-prominence attribute updates as level scrubs.
    2. Scrub level 1: expect data-prominence="low", panel at 80% opacity.
    3. Scrub level 7: expect data-prominence="high" (levelsUntilBaf = 3, matches <= 3 threshold), accent 2px border.
    4. Scrub level 10: expect data-prominence="high" (isBafLevel=true).
    5. Scrub level 15: expect data-prominence="medium" (levelsUntilBaf = 5; 4-7 range), text-secondary border.
    6. Scrub level 21: expect data-prominence="low" (levelsUntilBaf = 9; >7 range).

    **Scenario 3 -- Round-status pill visual states (BAF-03 / D-09):**

    1. Scrub to a level in each of the four states (find via database queries):
       - Open: current BAF-active level with no distribution yet. Pill: green "OPEN".
       - Closed: past BAF level with eth_baf jackpot_distributions row. Pill: gray "CLOSED".
       - Skipped: past BAF level with baf_skipped row (coinflip lost). Pill: red "SKIPPED".
       - Not_eligible: level 7 or any non-multiple-of-10. Pill: dim "NOT ELIGIBLE".
    2. Use DevTools to confirm the data-status attribute matches the rendered text.
    3. If you cannot find a skipped level in live data, simulate by picking a level and overriding the INTEG-05 response in DevTools Network tab's override feature.

    **Scenario 4 -- Your-rank row for non-top-4 (BAF-01 / INTEG-05 wiring):**

    1. Probe INTEG-05 via curl to find a player with rank > 4 at level 20:
       ```
       for P in $(curl -s http://localhost:3000/replay/players | python3 -c "import json,sys; print('\n'.join(json.load(sys.stdin)['players'][:20]))"); do
         RANK=$(curl -s "http://localhost:3000/player/$P/baf?level=20" | python3 -c "import json,sys; d=json.load(sys.stdin); print(d.get('rank','null'))")
         echo "$P rank=$RANK"
       done | grep -v "rank=null" | grep -v "rank=[1-4]$" | head -3
       ```
    2. Pick one such player. Scrub to level 20 + select that player in the player-selector.
    3. EXPECTED: below the top-4 leaderboard, a your-rank row un-hides showing "You: rank 7 of 42 -- {N} BURNIE" (or actual values).
    4. Now select a player with rank=null (no BAF stake). EXPECTED: your-rank row stays hidden.
    5. Now scrub to a non-multiple-of-10 level (level 7). EXPECTED: your-rank row stays hidden (all players have rank=null there).

    **Scenario 5 -- Selected-player highlighting in both leaderboards:**

    1. Coinflip: Scrub to day 64 (known-populated with 5 players per RESEARCH). Select `0xfabb0ac9d68b0b445fb7357272ff202c5651694a` (known rank 1 that day). EXPECTED: coinflip leaderboard rank 1 row has aria-current="true", visually bold + underlined with bg-tertiary background.
    2. BAF: Scrub to level 20. Select `0xdf37f81daad2b0327a0a50003740e1c935c70913` (known rank 1 at level 20 per RESEARCH). EXPECTED: BAF leaderboard rank 1 row has aria-current="true", bold + underlined, with the gold rank-1 color still visible underneath.

    **Scenario 6 -- Coinflip bounty armed-pulse animation (COINFLIP-03):**

    1. Find a player + day with `currentBounty > 0` in the coinflip block:
       ```
       for D in $(seq 1 100); do
         R=$(curl -s "http://localhost:3000/player/$PLAYER?day=$D" | python3 -c "import json,sys; d=json.load(sys.stdin).get('coinflip',{}); print(d.get('currentBounty','0'))" 2>/dev/null || echo "0")
         [[ "$R" != "0" ]] && echo "day=$D bounty=$R" && break
       done
       ```
    2. Scrub to that day. EXPECTED: the "ARMED" indicator pill pulses (opacity 1 -> 0.5 -> 1 over 2 seconds, repeating). Background is rgba(239, 68, 68, 0.15) + error color text.
    3. Scrub to a day with currentBounty = 0. EXPECTED: pill static, reads "NOT ARMED", background rgba(100, 100, 100, 0.2) + text-dim.

    **Scenario 7 -- Coinflip leaderboard integer-BURNIE rendering (Pitfall 8 regression):**

    1. Scrub to day 64 (known-populated). Live sample: rank 1 score = "52875" integer.
    2. EXPECTED: the coinflip leaderboard rank 1 row shows score as "52875 BURNIE" (raw integer string).
    3. REGRESSION CHECK: if the score renders as "0.000000000000052875 BURNIE" that is Pitfall 8 (coinflip integer misinterpreted as wei; formatBurnie called by mistake). File as P0.

    **Scenario 8 -- BAF leaderboard wei-BURNIE rendering:**

    1. Scrub to level 20. Live sample: rank 1 score = "475215212469240581904067" wei-scale.
    2. EXPECTED: the BAF leaderboard rank 1 row shows score as "475,215 BURNIE" or similar formatted value (formatBurnie divides by 10^18 and adds comma separators).
    3. REGRESSION CHECK: if the score renders as "475215212469240581904067 BURNIE" (raw 24-digit number), that is the opposite regression (wei string passed through without formatBurnie). File as P0.

    **Scenario 9 -- Rapid level-scrub (BAF) stability:**

    1. Scrub levels 10 -> 30 in under 2 seconds.
    2. EXPECTED: BAF panel [data-bind="content"] dims to 0.6 opacity via .is-stale class during each transition.
    3. EXPECTED: final level's top-4 leaderboard matches the final selection (no intermediate-day content leaks).
    4. EXPECTED: your-rank row updates correctly for each level (may re-hide or un-hide depending on selected player's rank at the new level).
    5. DevTools Network tab shows multiple INTEG-05 + /leaderboards/baf requests; some may be in-flight when abandoned (stale-guard invalidates their render paths).

    **Scenario 10 -- Rapid day-scrub (coinflip) stability:**

    1. Scrub days 50 -> 70 in under 2 seconds.
    2. EXPECTED: coinflip panel dims; final day's bounty + state + leaderboard match the final selection.
    3. Spot-check that biggestFlipPlayer at the final day is NOT the stale player from an earlier day (3-checkpoint stale-guard worked).

    **Scenario 11 -- Empty state at level % 10 != 0 (BAF):**

    1. Scrub to level 7 (any non-multiple-of-10). 
    2. EXPECTED: BAF leaderboard body shows "BAF active only at every 10th level." (or similar D-gray-area text).
    3. EXPECTED: round-status pill shows "NOT ELIGIBLE" dim.
    4. EXPECTED: your-rank row stays hidden.

    **Scenario 12 -- Empty state for coinflip zero-activity day:**

    1. Pick a day with 0 entries in /leaderboards/coinflip.
    2. EXPECTED: coinflip leaderboard body shows "No coinflip activity for day N."
    3. EXPECTED: bounty + state sections still render (showing current/latest coinflip block values per Pitfall 10 design decision).

    **Record findings in 54-UAT.md:**

    Create `.planning/phases/54-coinflip-baf/54-UAT.md` (see Task 2 for template variants). Record each scenario with device/browser/tuple + pass/fail + notes.

    Resume signal options:
    - `uat-pass` -- all 12 scenarios recorded as pass; Phase 54 is visually validated end-to-end.
    - `uat-partial -- [brief issue list]` -- some scenarios failed; 54-UAT.md records them with detail.
    - `uat-deferred -- [reason]` -- user defers UAT (acceptable per Phase 50 + 51 + 52 + 53 precedent); 54-UAT.md records the deferral with rationale citing the 4-phase precedent chain.
  </how-to-verify>
  <resume-signal>Type one of: "uat-pass", "uat-partial -- [issues]", or "uat-deferred -- [reason]".</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write 54-UAT.md with the UAT record (pass-run, partial, or deferral)</name>
  <files>.planning/phases/54-coinflip-baf/54-UAT.md</files>
  <read_first>
    - .planning/phases/54-coinflip-baf/54-VALIDATION.md (Manual-Only Verifications table; each row becomes a record section)
    - .planning/phases/51-profile-quests/51-UAT.md (Phase 51 deferral template; structural template if Phase 54 also defers)
    - .planning/phases/52-tickets-packs-jackpot/52-UAT.md (Phase 52 deferral template; most recent precedent; reads "Browser UAT deferred per Phase 50+51 precedent chain")
    - Task 1 checkpoint resume signal (pass / partial / deferred)
  </read_first>
  <action>
Create `.planning/phases/54-coinflip-baf/54-UAT.md` using the Write tool. The exact content depends on the resume signal from Task 1.

## Variant A -- If resume signal was `uat-pass`

Write a full UAT record with one section per scenario. Template (fill with actual observations from the UAT run):

```markdown
# Phase 54 -- UAT Record

**Tester:** [name or "solo dev"]
**Date:** [YYYY-MM-DD]
**Stack:**
  - Website: http://localhost:8080/play/ (python3 -m http.server 8080 in /home/zak/Dev/PurgeGame/website/)
  - API: http://localhost:3000 (Fastify in /home/zak/Dev/PurgeGame/database/)
  - Browsers exercised: [e.g. "Chrome 120 desktop"]

## Scenario 1 -- Prominence tier colors (BAF-02 / D-06)

**Device:** [e.g. "Dell XPS 15 Linux, Chrome 120"]
**Level used:** level=20

- [ ] Rank 1 row color approximately #FFD700 (gold) -- [PASS/FAIL]
- [ ] Rank 1 row font-weight 700, font-size 0.95rem -- [PASS/FAIL]
- [ ] Rank 2 row color approximately #C0C0C0 (silver) -- [PASS/FAIL]
- [ ] Rank 3 row color approximately #CD7F32 (bronze) -- [PASS/FAIL]
- [ ] Rank 4 row theme default -- [PASS/FAIL]
- [ ] Visual hierarchy obvious without DevTools -- [PASS/FAIL]

**Scenario 1 overall:** [PASS / PARTIAL / FAIL]
**Issues:** [list; empty if all PASS]

## Scenario 2 -- Panel-level data-prominence border (BAF-03 feel)

**Device:** [same as above]
**Levels scrubbed:** 1, 7, 10, 15, 21

- [ ] Level 1 -- data-prominence="low", panel 80% opacity -- [PASS/FAIL]
- [ ] Level 7 -- data-prominence="high", accent 2px border -- [PASS/FAIL]
- [ ] Level 10 -- data-prominence="high", accent border -- [PASS/FAIL]
- [ ] Level 15 -- data-prominence="medium", text-secondary border -- [PASS/FAIL]
- [ ] Level 21 -- data-prominence="low", 80% opacity -- [PASS/FAIL]

**Scenario 2 overall:** [PASS / PARTIAL / FAIL]

## Scenario 3 -- Round-status pill visual states (BAF-03 / D-09)

**Device:** [same]
**Levels used:** [e.g. "open at 20, closed at 10, skipped at 30, not_eligible at 7"]

- [ ] "OPEN" pill green (rgba 34, 197, 94, 0.15 bg + success text) -- [PASS/FAIL]
- [ ] "CLOSED" pill gray (rgba 100, 100, 100, 0.2 bg + text-dim) -- [PASS/FAIL]
- [ ] "SKIPPED" pill red (rgba 239, 68, 68, 0.15 bg + error text) -- [PASS/FAIL]
- [ ] "NOT ELIGIBLE" pill dim (rgba 100, 100, 100, 0.1 bg + text-dim) -- [PASS/FAIL]
- [ ] data-status attribute matches rendered text -- [PASS/FAIL]

**Scenario 3 overall:** [PASS / PARTIAL / FAIL]

## Scenario 4 -- Your-rank row for non-top-4 (BAF-01 / INTEG-05)

**Device:** [same]
**Players tested:** [rank>4 addr, rank=null addr]

- [ ] rank > 4 player: row un-hides showing "You: rank N of M -- {score} BURNIE" -- [PASS/FAIL]
- [ ] rank = null player: row stays hidden -- [PASS/FAIL]
- [ ] Non-multiple-of-10 level: row stays hidden for all players -- [PASS/FAIL]
- [ ] Score rendered via formatBurnie (readable 6-digit format, not 24-digit wei) -- [PASS/FAIL]

**Scenario 4 overall:** [PASS / PARTIAL / FAIL]

## Scenario 5 -- Selected-player highlighting (COINFLIP-02 + BAF-02)

**Device:** [same]

- [ ] Coinflip: selected player (0xfabb...6a at day 64 rank 1) row has aria-current + bold + underlined + bg-tertiary -- [PASS/FAIL]
- [ ] BAF: selected player (0xdf37...13 at level 20 rank 1) row has aria-current + bold + underlined (gold tier still visible) -- [PASS/FAIL]

**Scenario 5 overall:** [PASS / PARTIAL / FAIL]

## Scenario 6 -- Coinflip bounty armed-pulse animation (COINFLIP-03)

**Device:** [same]
**Day with bounty > 0:** [day N]

- [ ] ARMED indicator pulses (opacity 1 -> 0.5 -> 1 over 2s, repeating) -- [PASS/FAIL]
- [ ] Background rgba(239, 68, 68, 0.15) + error color text -- [PASS/FAIL]
- [ ] Day with bounty=0: pill static, "NOT ARMED", dim bg -- [PASS/FAIL]

**Scenario 6 overall:** [PASS / PARTIAL / FAIL]

## Scenario 7 -- Coinflip leaderboard integer-BURNIE rendering (Pitfall 8)

**Device:** [same]
**Day used:** day=64 (rank 1 score=52875)

- [ ] Score renders as "52875 BURNIE" (raw integer) -- [PASS/FAIL]
- [ ] No "0.000000000000052875 BURNIE" artifact anywhere in leaderboard -- [PASS/FAIL]

**Scenario 7 overall:** [PASS / PARTIAL / FAIL]
**P0 if FAIL:** Pitfall 8 regression. Fix by removing formatBurnie call from coinflip leaderboard score rendering; use String(entry.score) directly.

## Scenario 8 -- BAF leaderboard wei-BURNIE rendering (opposite Pitfall 8)

**Device:** [same]
**Level used:** level=20 (rank 1 score=475215212469240581904067)

- [ ] Score renders as "475,215 BURNIE" or similar formatted value -- [PASS/FAIL]
- [ ] No 24-digit raw wei string visible in leaderboard -- [PASS/FAIL]

**Scenario 8 overall:** [PASS / PARTIAL / FAIL]
**P0 if FAIL:** Opposite Pitfall 8 regression. Fix by wrapping score in formatBurnie() call.

## Scenario 9 -- Rapid level-scrub (BAF) stability

**Device:** [same]
**Scrub range:** levels 10 -> 30 in ~1.5s

- [ ] Panel dims to 0.6 opacity during scrub -- [PASS/FAIL]
- [ ] Final level's top-4 matches final selection -- [PASS/FAIL]
- [ ] Your-rank row updates correctly -- [PASS/FAIL]
- [ ] No intermediate-level artifacts -- [PASS/FAIL]

**Scenario 9 overall:** [PASS / PARTIAL / FAIL]

## Scenario 10 -- Rapid day-scrub (coinflip) stability

**Device:** [same]
**Scrub range:** days 50 -> 70 in ~1.5s

- [ ] Coinflip panel dims during scrub -- [PASS/FAIL]
- [ ] Final day's bounty + state + leaderboard match -- [PASS/FAIL]
- [ ] biggestFlipPlayer reflects final-day value, not stale from mid-scrub -- [PASS/FAIL]

**Scenario 10 overall:** [PASS / PARTIAL / FAIL]

## Scenario 11 -- Empty state at non-multiple-of-10 BAF level

**Device:** [same]
**Level used:** level=7

- [ ] Leaderboard shows "BAF active only at every 10th level." or similar -- [PASS/FAIL]
- [ ] Round-status pill: "NOT ELIGIBLE" dim -- [PASS/FAIL]
- [ ] Your-rank row hidden -- [PASS/FAIL]

**Scenario 11 overall:** [PASS / PARTIAL / FAIL]

## Scenario 12 -- Empty state for coinflip zero-activity day

**Device:** [same]
**Day used:** [day with 0 entries]

- [ ] Leaderboard shows "No coinflip activity for day N." -- [PASS/FAIL]
- [ ] Bounty + state sections still render -- [PASS/FAIL]

**Scenario 12 overall:** [PASS / PARTIAL / FAIL]

## Overall Result

Phase 54 UAT: [PASS / PARTIAL / FAIL]

Ready for `/gsd-verify-phase 54`: [yes / no -- requires fix plan before verify]

## Issues Summary

[If any, list with severity (P0/P1/P2), expected vs observed, recommended fix approach]
```

## Variant B -- If resume signal was `uat-partial -- [issues]`

Fill in Variant A's template with actual pass/fail observations. Add an explicit `## Issues Requiring Fix` section listing each failure:
- What was expected (cite requirement ID + D-XX)
- What was observed (concrete detail: DevTools color picker value, console error text, visual glitch description)
- Severity (P0 blocking / P1 should-fix / P2 nice-to-have)
- Recommended fix approach (pointer to the offending code + likely change)

## Variant C -- If resume signal was `uat-deferred -- [reason]`

Write a short deferral note mirroring Phase 51 + 52 UAT shape:

```markdown
# Phase 54 -- UAT Record

**Status:** DEFERRED
**Date:** [YYYY-MM-DD]
**Reason:** [from resume signal -- e.g., "Browser UAT deferred to Phase 55 natural integration. Phase 55 adds the final player-facing panel (Decimator); a real-user session at that point exercises all six panels simultaneously (profile + tickets + packs + jackpot + coinflip + BAF + decimator). Any cross-panel visual regression will surface there. Automated suite is 13/13 play/ test files green + SHELL-01 recursive guardrail green, which validates the code contract; UAT covers the visual/device contract separately. Following Phase 50 + 51 + 52 + 53 precedent chain -- each prior phase deferred its UAT and no deferred visual issue surfaced in subsequent phases, confirming the pattern works."]

## Deferred Scenarios

All twelve scenarios from 54-VALIDATION.md Manual-Only Verifications table are deferred:

1. Prominence tier colors (BAF-02 / D-06)
2. Panel-level data-prominence border transitions (BAF-03)
3. Round-status pill visual states (BAF-03 / D-09)
4. Your-rank row for non-top-4 (BAF-01 / INTEG-05)
5. Selected-player highlighting (COINFLIP-02 + BAF-02)
6. Coinflip bounty armed-pulse animation (COINFLIP-03)
7. Coinflip leaderboard integer-BURNIE rendering (Pitfall 8)
8. BAF leaderboard wei-BURNIE rendering (opposite Pitfall 8)
9. Rapid level-scrub (BAF) stability
10. Rapid day-scrub (coinflip) stability
11. Empty state at non-multiple-of-10 BAF level
12. Empty state for coinflip zero-activity day

## Deferral Precedent Chain

Phase 50 deferred -- see `.planning/phases/50-route-foundation-day-aware-store/50-03-SUMMARY.md` frontmatter `uat_status: deferred`.
Phase 51 deferred -- see `.planning/phases/51-profile-quests/51-UAT.md`; cites Phase 50 precedent.
Phase 52 deferred -- see `.planning/phases/52-tickets-packs-jackpot/52-UAT.md`; cites Phase 50+51 chain.
Phase 53 deferred -- see `.planning/phases/53-purchase-flow/53-01-SUMMARY.md` + STATE.md.
Phase 54 defers to Phase 55 (Decimator) natural integration -- the final v2.4 player panel. Once Phase 55 ships, all six player panels are live; a real-user session exercising all of them at once is the most efficient UAT opportunity. At that moment:

1. Any Phase 54 visual defect (prominence color wrong on real theme, armed-pulse timing off, rapid-scrub flash) becomes immediately obvious alongside the other panels.
2. If any Phase 54 behavior fails, a gap-closure plan (`/gsd-plan-phase 54 --gaps`) can address it without re-opening full Phase 54 planning.

## Automated Verification State

Automated test suite as of 2026-04-24: 13/13 play/ test files green.

| Test file | Tests | Status |
|-----------|------:|--------|
| play/app/__tests__/play-route-structure.test.js | N | pass |
| play/app/__tests__/play-main-bootstrap.test.js | N | pass |
| play/app/__tests__/play-shell-01.test.js | N | pass (11 FORBIDDEN entries including Phase 54 additions) |
| play/app/__tests__/play-panel-stubs.test.js | N | pass |
| play/app/__tests__/play-profile-panel.test.js | N | pass (Phase 51) |
| play/app/__tests__/play-tickets-panel.test.js | N | pass (Phase 52) |
| play/app/__tests__/play-packs-panel.test.js | N | pass (Phase 52) |
| play/app/__tests__/play-jackpot-wrapper.test.js | N | pass (Phase 52) |
| play/app/__tests__/play-jackpot-shell01-regression.test.js | N | pass (Phase 52 D-09) |
| play/app/__tests__/play-purchase-panel.test.js | N | pass (Phase 53) |
| play/app/__tests__/play-coinflip-panel.test.js | ~28 | pass (Phase 54 NEW) |
| play/app/__tests__/play-baf-panel.test.js | ~30 | pass (Phase 54 NEW) |

Plus Phase 54 INTEG-05-SPEC.md authored + INTEG-05 database-side vitest tests green.

This covers the code contract (DOM structure, import posture, fetch wiring, stale-guard token comparison, data-bind markers, prominence attributes, score-unit discipline). UAT would cover the visual/device contract (prominence tier color accuracy, armed-pulse animation feel, rapid-scrub visual smoothness, round-status pill color distinction).

## Phase 54 Close Readiness

Phase 54 is ready for `/gsd-verify-phase 54` with this 54-UAT.md as the concrete artifact documenting the deferral decision. The phase can be marked complete; the UAT gap is documented, rationalized, and carries a concrete resurfacing trigger (Phase 55 natural integration).
```

Whichever variant matches the resume signal, do NOT use em dashes or emojis. Use `--` for double-hyphen separators.

## Self-check after writing

- File exists at `.planning/phases/54-coinflip-baf/54-UAT.md`.
- Line count at least 15 (deferral minimum; pass-run will be 150+).
- Terminal state documented (pass / partial / deferred) with enough detail that `/gsd-verify-phase 54` can reference it.
- No em dashes or emojis.
- References to specific Phase 54 decisions (D-06 / D-07 / D-08 / D-09) and requirements (COINFLIP-01..03, BAF-01..03, INTEG-05) make the document self-contained.
  </action>
  <verify>
    <automated>test -f .planning/phases/54-coinflip-baf/54-UAT.md && wc -l .planning/phases/54-coinflip-baf/54-UAT.md | awk '{exit ($1 < 15) ? 1 : 0}' && grep -Eq "uat-pass|PASS|FAIL|DEFERRED|deferred" .planning/phases/54-coinflip-baf/54-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/54-coinflip-baf/54-UAT.md` exits 0
    - `wc -l .planning/phases/54-coinflip-baf/54-UAT.md` reports >= 15 lines (deferral minimum)
    - `grep -c "Phase 54" .planning/phases/54-coinflip-baf/54-UAT.md` returns >= 1
    - One of these patterns matches (terminal-state declaration):
      - `grep -qE "Status:\\s+DEFERRED" .planning/phases/54-coinflip-baf/54-UAT.md`
      - OR `grep -qE "Overall Result.*PASS" .planning/phases/54-coinflip-baf/54-UAT.md`
      - OR `grep -qE "Overall Result.*PARTIAL" .planning/phases/54-coinflip-baf/54-UAT.md`
    - `grep -cE "COINFLIP-0|BAF-0|D-06|D-07|D-08|D-09|Pitfall 8" .planning/phases/54-coinflip-baf/54-UAT.md` returns >= 2 (at least two Phase 54 scenarios or decisions referenced)
    - `! grep -q "—" .planning/phases/54-coinflip-baf/54-UAT.md` exits 0 (no em dashes)
    - No emojis in the file (inspection; CLAUDE.md rule)
    - File is valid markdown (no malformed syntax)
  </acceptance_criteria>
  <done>
    `.planning/phases/54-coinflip-baf/54-UAT.md` captures the terminal UAT state for Phase 54 (pass / partial / deferred). `/gsd-verify-phase 54` has a concrete artifact to reference. If deferred, the rationale cites the 4-phase precedent chain (Phase 50 -> 51 -> 52 -> 53) and points to Phase 55 natural-integration as the surfacing opportunity. No em dashes or emojis.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human reviewer <-> running stack | UAT exercises the real system end-to-end; no new code paths |
| 54-UAT.md <-> planning process | Record document; no executable consequences |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-30 | (none) | UAT is read-only observation + report writing | accept | No code change; no new trust boundary introduced |
| T-54-31 | Repudiation | 54-UAT.md as proof of UAT run or deferral | accept | Git-committed planning artifact; future phases can reference |
| T-54-32 | Information disclosure | UAT record includes player addresses used in test scenarios | accept | Ethereum addresses are public by protocol; no PII |
</threat_model>

<verification>
- `test -f .planning/phases/54-coinflip-baf/54-UAT.md` exits 0
- Checkpoint Task 1 reached a terminal signal (uat-pass / uat-partial / uat-deferred)
- All Phase 54 automated tests stay green (no regression -- Wave 3 writes only the planning doc)
- ROADMAP.md Phase 54 Success Criteria are validated:
  (a) automatically via the test suite (13 play/ test files + database-side vitest for INTEG-05) AND/OR
  (b) manually via UAT (pass) OR
  (c) automatically + deferred for visual (UAT recorded deferral with 4-phase precedent chain)
</verification>

<success_criteria>
1. `.planning/phases/54-coinflip-baf/54-UAT.md` exists with the terminal UAT state for Phase 54.
2. If UAT ran: 12 scenarios each have pass/fail records with device/browser/tuple context.
3. If UAT deferred: rationale documented, precedent cited (Phase 50 -> 51 -> 52 -> 53 deferral chain), follow-up plan noted (Phase 55 natural integration).
4. No automated test regressions (Wave 3 is documentation only).
5. Phase 54 is ready for `/gsd-verify-phase 54`.
</success_criteria>

<output>
After completion, create `.planning/phases/54-coinflip-baf/54-04-SUMMARY.md` per the standard template, including:
- UAT terminal state (pass / partial / deferred)
- If pass: scenarios covered + device/browser list
- If partial: issues listed + follow-up gap plan recommendation
- If deferred: precedent (Phase 50 -> 51 -> 52 -> 53 chain) + Phase 55 natural-integration hook
- Phase 54 overall status: ready for /gsd-verify-phase 54
- Requirement closure summary: COINFLIP-01/02/03 + BAF-01/02/03 all flipped to [x] Validated in REQUIREMENTS.md; INTEG-05 flipped to [x] Validated; INTEG-04 stays [~] Deferred (Wave 0 change held)
</output>

## Plan Summary

**Objective:** Wave 3 -- optional manual UAT covering 12 scenarios that cannot be automated; terminal state recorded in 54-UAT.md (pass/partial/deferred per Phase 50 -> 51 -> 52 -> 53 precedent chain).

**Tasks:**
1. (CHECKPOINT) Run UAT scenarios: prominence tier colors + panel-level prominence borders + round-status pill 4 states + your-rank row un-hide/hide + selected-player highlighting + armed-pulse animation + Pitfall 8 regression checks (integer vs wei) + rapid-scrub stability (level + day) + empty states; resume with uat-pass / uat-partial / uat-deferred
2. Write 54-UAT.md matching resume signal (full record template for pass, issues list for partial, deferral note for deferred)

**Verify chain:**
- Task 1 is interactive; user drives the browser + curl checks
- Task 2 runs whichever variant matches the signal
- No code changes; Wave 3 is planning-docs only
- All prior tests stay green

**Context cost estimate:** ~8% for Task 2 file write; Task 1 interactive time is not orchestrator context consumption.

**Duration:** Task 1 highly variable (3-5 min if deferred; 30-60 min for a full UAT run across 12 scenarios). Task 2 runs in 1 minute.
