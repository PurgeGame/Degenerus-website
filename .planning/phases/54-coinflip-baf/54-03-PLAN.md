---
phase: 54
plan: 03
slug: wave2-integ05-hydration-hard-gated
type: execute
wave: 2
depends_on:
  - 54-01-PLAN.md
  - 54-02-PLAN.md
files_modified:
  - play/components/baf-panel.js
autonomous: false
requirements:
  - BAF-01
  - BAF-03
  - INTEG-05
tags:
  - phase-54
  - wave-2
  - hydration
  - hard-gated
  - integ-05
  - shell-01

must_haves:
  truths:
    - "Wave 2 does NOT merge until the INTEG-05 endpoint GET /player/:address/baf?level=N ships in /home/zak/Dev/PurgeGame/database/ per the 54-CONTEXT.md hard-gate + INTEG-05-SPEC.md contract (3 atomic commits on database/main: feat + docs + test, mirroring INTEG-01 and INTEG-02 shape)"
    - "play/components/baf-panel.js #refetchPlayer() method is flipped from 404-tolerant silent-fallback to live parsing: ok-response parses the JSON and calls #renderYourRank(data) + #renderRoundStatus(data.roundStatus); non-ok responses still fall back to the inline roundStatus derivation"
    - "The your-rank row (data-bind='your-rank') UN-HIDES when INTEG-05 returns a non-null rank, showing 'You: rank N of M -- {score} BURNIE'; it stays hidden when rank is null (player has no BAF stake at this level) to avoid cluttering the UI with a '--' row"
    - "The round-status pill (data-bind='round-status') shows authoritative state from INTEG-05 response: 'open' / 'closed' / 'skipped' / 'not_eligible' with data-status attribute applying the corresponding CSS pill color per Wave 1 CSS (already shipped)"
    - "Wave 1 render helpers (#renderYourRank, #renderRoundStatus, #renderRoundStatusFallback) are PRESERVED; only the 404 branch of #refetchPlayer() is changed (and the ok branch is already shipped from Wave 1 defensive coding). No structural rewrites."
    - "Score-unit discipline (Pitfall 8) continues to hold: INTEG-05 `score` field is WEI-scale BURNIE and MUST be rendered via formatBurnie; the Wave 1 #renderYourRank already encodes this correctly via the defensive ok-path code"
    - "Both Wave 0 test assertions that could not pass in Wave 1 (if any specific to live INTEG-05 response parsing) now pass; all Wave 1 tests stay green; SHELL-01 recursive guardrail stays green"
    - "Phase 50/51/52/53/54-01/54-02 tests all continue to pass (no regression)"
    - "The INTEG-05 side-quest is tracked in the Wave 2 SUMMARY.md: 3 commit hashes from database/main (mirroring Phase 51 INTEG-02 precedent d135605/dab5adf/64fe8db and Phase 52 INTEG-01 precedent a46fdcb/e130547/9988887)"
  artifacts:
    - path: "play/components/baf-panel.js"
      provides: "Wave 2 surgical edit: #refetchPlayer() method tightened to parse live INTEG-05 response + uncomment/remove the Wave 1 silent-404 note; all other methods (connectedCallback, disconnectedCallback, #refetchLeaderboard, #renderContext, #renderLeaderboard, #renderYourRank, #renderRoundStatus, #renderRoundStatusFallback, #showContent, bafContext helper, TEMPLATE) unchanged"
      min_lines: 240
      contains: "#bafPlayerFetchId"
  key_links:
    - from: "play/components/baf-panel.js #refetchPlayer()"
      to: "GET /player/:address/baf?level=N (INTEG-05 live)"
      via: "fetch with JSON parsing"
      pattern: "/player/.*?/baf\\?level="
    - from: "play/components/baf-panel.js #renderYourRank(data)"
      to: "INTEG-05 response fields (level, player, score, rank, totalParticipants, roundStatus)"
      via: "data.rank, data.totalParticipants, data.score, data.roundStatus references"
      pattern: "data\\.(rank|totalParticipants|score|roundStatus)"
    - from: ".planning/phases/54-coinflip-baf/INTEG-05-SPEC.md"
      to: "/home/zak/Dev/PurgeGame/database/src/api/routes/player.ts + schemas/player.ts"
      via: "database-side handler + Zod schema implementation"
      pattern: "database/src/api/(routes|schemas)/player\\.ts"
---

<objective>
Wave 2 completes Phase 54 by wiring the BAF per-player endpoint (INTEG-05) into the baf-panel your-rank row and round-status pill. This wave is HARD-GATED: the first task is a checkpoint that blocks execution until the database repo ships the INTEG-05 endpoint per `.planning/phases/54-coinflip-baf/INTEG-05-SPEC.md`.

Purpose: Close out BAF-01 (user sees selected player's BAF score and rank) + BAF-03 full (user sees whether the round is open or closed -- authoritative via INTEG-05 instead of the Wave 1 fallback derivation). After this plan, a non-top-4 player sees "You: rank 7 of 42 -- {score} BURNIE" below the leaderboard, and the round-status pill reflects backend truth including "skipped" (coinflip lost) and "closed" (jackpot already distributed) states that the Wave 1 fallback cannot detect.

**DO NOT MERGE UNTIL INTEG-05 ENDPOINT SHIPS.** The checkpoint task enforces this. The database team's delivery is documented in INTEG-05-SPEC.md (authored in Wave 0 Plan 54-01). Delivery is a solo-dev self-coordination step (user owns both website and database repos). Same pattern as Phase 51 INTEG-02 and Phase 52 INTEG-01 side-quests (3 atomic commits on `database/main`: feat + docs + test).

Output:
- `play/components/baf-panel.js` gets a small surgical edit to #refetchPlayer() method: the silent-404 path becomes the "fallback to derivation" path only when the response is genuinely non-OK; the ok-path parses + renders the full INTEG-05 response.
- Database-side 3-atomic-commit side-quest shipped to `/home/zak/Dev/PurgeGame/database/main`.
- No new files. No other play/ file touched. No CSS changes (Wave 1 shipped all needed styling).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-coinflip-baf/54-CONTEXT.md
@.planning/phases/54-coinflip-baf/54-RESEARCH.md
@.planning/phases/54-coinflip-baf/54-PATTERNS.md
@.planning/phases/54-coinflip-baf/54-VALIDATION.md
@.planning/phases/54-coinflip-baf/INTEG-05-SPEC.md
@.planning/phases/54-coinflip-baf/54-01-PLAN.md
@.planning/phases/54-coinflip-baf/54-02-PLAN.md

<!-- Wave 1 deliverable (the file this plan surgically edits) -->
@play/components/baf-panel.js

<!-- Wave 0 tests -->
@play/app/__tests__/play-baf-panel.test.js

<!-- Side-quest precedents (Phase 51 + Phase 52 hard-gate templates) -->
@.planning/phases/51-profile-quests/51-03-wave2-backend-wiring-hard-gated-PLAN.md
@.planning/phases/52-tickets-packs-jackpot/52-03-PLAN.md
@.planning/phases/52-tickets-packs-jackpot/INTEG-01-SPEC.md
@.planning/phases/51-profile-quests/INTEG-02-SPEC.md

<interfaces>
<!-- INTEG-05 live response shape (the backend must return this per INTEG-05-SPEC.md) -->
{
  level: 20,
  player: "0x71be63f3384f5fb98995898a86b02fb2426c5788",
  score: "344863111573291904385281",
  rank: 7,
  totalParticipants: 42,
  roundStatus: "open"
}

-- Edge cases per INTEG-05-SPEC.md:
-- Player has stake at level: non-null rank, score > 0
-- Player has NO stake at level: score="0", rank=null, totalParticipants>=0
-- Level has no entries: score="0", rank=null, totalParticipants=0
-- Level % 10 !== 0: score="0", rank=null, totalParticipants=0, roundStatus="not_eligible"

-- 400 error shape (Zod validation failure):
{ statusCode: 400, error: "Bad Request", message: "..." }

<!-- Wave 1 #refetchPlayer() current state (from Plan 54-02 Task 2) -->

async #refetchPlayer() {
  const level = get('replay.level');
  const addr = get('replay.player');
  const token = ++this.#bafPlayerFetchId;

  if (level == null || !addr) return;

  try {
    const res = await fetch(`${API_BASE}/player/${addr}/baf?level=${encodeURIComponent(level)}`);
    if (token !== this.#bafPlayerFetchId) return;

    if (!res.ok) {
      // Wave 1: INTEG-05 not yet shipped. Silent 404 tolerance.
      // Wave 2 flips this to live rendering.
      this.#renderRoundStatusFallback(level);
      return;
    }

    const data = await res.json();
    if (token !== this.#bafPlayerFetchId) return;

    this.#renderYourRank(data);
    this.#renderRoundStatus(data?.roundStatus);
  } catch (err) {
    if (token === this.#bafPlayerFetchId) {
      this.#renderRoundStatusFallback(level);
    }
  }
}

<!-- Wave 2 target state (surgical edit removes the Wave-1 stub comment + adjusts
     the fallback comment to reflect the new meaning) -->

async #refetchPlayer() {
  const level = get('replay.level');
  const addr = get('replay.player');
  const token = ++this.#bafPlayerFetchId;

  if (level == null || !addr) return;

  try {
    const res = await fetch(`${API_BASE}/player/${addr}/baf?level=${encodeURIComponent(level)}`);
    if (token !== this.#bafPlayerFetchId) return;

    if (!res.ok) {
      // INTEG-05 error path (400/500): fall back to inline derivation for the
      // round-status pill so the panel degrades gracefully. The your-rank row
      // stays hidden (no rank data to render).
      this.#renderRoundStatusFallback(level);
      return;
    }

    const data = await res.json();
    if (token !== this.#bafPlayerFetchId) return;

    this.#renderYourRank(data);
    this.#renderRoundStatus(data?.roundStatus);
  } catch (err) {
    if (token === this.#bafPlayerFetchId) {
      this.#renderRoundStatusFallback(level);
    }
  }
}

<!-- The only real code change is in the comment: "Wave 1 ... Wave 2 flips" ->
     "INTEG-05 error path" because the behavior is already correct. The ok-path
     was already live in Wave 1; the fallback-on-non-ok was also live.
     The "flip" is a documentation + verification change, not a logic change. -->

<!-- OPTIONAL defensive tightening (may be added during Wave 2 based on real
     INTEG-05 response observations): -->

-- If the backend ships a slightly different schema (e.g., `roundState` instead of
   `roundStatus`), Wave 2 MUST update #renderYourRank + #renderRoundStatus to
   match the shipped shape BEFORE committing. Read the openapi.json after the
   backend commits land to confirm.

-- If Wave 2 observes INTEG-05 returning score="0" + rank=null for a player
   outside top-4 where no totalParticipants exist at the level, #renderYourRank
   already handles this (hides the row when data.rank == null).

<!-- Side-quest pattern (3 atomic commits on database/main mirroring INTEG-02) -->

Commit 1: feat(api): add GET /player/:address/baf endpoint (INTEG-05)
  - database/src/api/routes/player.ts -- add handler per INTEG-05-SPEC.md "Proposed Backend Implementation"
  - database/src/api/schemas/player.ts -- add bafQuerySchema + bafPlayerResponseSchema
  - Imports: bafSkipped, jackpotDistributions tables + eq, and, inArray from drizzle

Commit 2: docs(openapi): document /player/:address/baf (INTEG-05)
  - database/src/api/openapi.json -- add endpoint definition with response schema
  - Follow existing pattern from /player/:address/tickets/by-trait (Phase 52 INTEG-01)

Commit 3: test(api): cover INTEG-05 happy path, rank null, empty level, not_eligible, skipped, closed
  - database/src/api/__tests__/player-baf.test.ts (new vitest file)
  - Tests: player in top-4 returns rank 1-4; player outside returns rank 5+; player with no stake returns rank=null; level % 10 !== 0 returns not_eligible; baf_skipped row returns skipped; eth_baf jackpot_distributions row returns closed

Precedent:
- INTEG-02 (Phase 51): d135605 feat, dab5adf docs, 64fe8db test
- INTEG-01 (Phase 52): a46fdcb feat, e130547 docs, 9988887 test
</interfaces>
</context>

<read_first>
- .planning/phases/54-coinflip-baf/54-CONTEXT.md (D-04 INTEG-05 requirement + D-06 top-4 prominence + "Your rank" row below leaderboard; D-09 roundStatus source from INTEG-05)
- .planning/phases/54-coinflip-baf/54-RESEARCH.md Section 8 (INTEG-05 Endpoint Spec Draft; lines 331-555; the authoritative contract)
- .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md (the exact spec authored in Wave 0; the backend implementation reads this)
- .planning/phases/54-coinflip-baf/54-02-PLAN.md Task 2 action section (how Wave 1 wrote #refetchPlayer with the 404-tolerant stub; Wave 2 surgically removes the "Wave 1 ... Wave 2 flips" comment)
- play/components/baf-panel.js (the Wave 1 file; locate #refetchPlayer() method)
- play/app/__tests__/play-baf-panel.test.js (all assertions must stay green after this wave)
- .planning/phases/51-profile-quests/51-03-wave2-backend-wiring-hard-gated-PLAN.md Task 1 (CHECKPOINT template + resume-signal pattern; clone shape for INTEG-05 gate)
- .planning/phases/52-tickets-packs-jackpot/52-03-PLAN.md Task 1 (secondary checkpoint template; INTEG-01 verification recipe)
- /home/zak/Dev/PurgeGame/database/src/api/routes/player.ts (check for existing INTEG-01 handler at /tickets/by-trait; INTEG-05 handler places immediately after it)
- /home/zak/Dev/PurgeGame/database/src/api/schemas/player.ts (where bafQuerySchema + bafPlayerResponseSchema append)
- /home/zak/Dev/PurgeGame/database/src/db/schema/views.ts (mv_baf_top4 materialized view definition; INTEG-05 uses the underlying baf_flip_totals table, not the view)
- /home/zak/Dev/PurgeGame/database/src/handlers/baf-jackpot.ts (handleBafSkipped handler at lines 53-68; references the baf_skipped table used in roundStatus derivation)
- CLAUDE.md (no em dashes, no emojis)
</read_first>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 (CHECKPOINT): Verify INTEG-05 endpoint ships before Wave 2 merge</name>
  <what-built>
    Wave 0 (Plan 54-01) wrote INTEG-05-SPEC.md into the Phase 54 directory with the full contract: path + query schema, response shape, roundStatus derivation logic, proposed handler + schema TypeScript, acceptance criteria, 3-commit timeline.

    Wave 1 (Plan 54-02) shipped play/components/baf-panel.js with a 404-tolerant INTEG-05 fetch path: #refetchPlayer() calls `/player/${addr}/baf?level=${level}` but tolerates any non-OK response by calling the fallback roundStatus derivation and leaving the your-rank row hidden. The ok-path already does the right thing defensively (calls #renderYourRank + #renderRoundStatus with the live response data).

    Wave 2 flips the semantics: the endpoint is expected to ship, so any non-OK response is now a real error (not an absent endpoint). The comment in #refetchPlayer() changes from "Wave 1 silent tolerance" to "INTEG-05 error path fallback". No real logic change on the frontend -- the ok-path was already correct.

    This checkpoint enforces the hard gate from CONTEXT.md D-04 and the Timeline section of INTEG-05-SPEC.md: "BAF-01 hydration lands only after the database team confirms the endpoint is shipped."

    Before proceeding, the user (or the agent after switching repos) must confirm the INTEG-05 endpoint is live.

    The checkpoint supports two terminal states:
    - `endpoint-shipped` -- the database repo has shipped the endpoint (3 atomic commits; vitest pass); Wave 2 Task 2 proceeds.
    - `deferred -- [reason]` -- the endpoint is not yet ready; Wave 2 remains blocked. The user switches repos, implements per the spec, and returns.
  </what-built>
  <how-to-verify>
    **Setup: confirm database-side readiness**

    1. Confirm the 3 atomic commits are on database/main. From any shell:
       ```
       cd /home/zak/Dev/PurgeGame/database
       git log --oneline --all | head -20
       ```
       Expect 3 recent commits mirroring INTEG-02 (Phase 51) and INTEG-01 (Phase 52) shape:
       - feat(api): add GET /player/:address/baf endpoint (INTEG-05)
       - docs(openapi): document /player/:address/baf (INTEG-05)
       - test(api): cover INTEG-05 happy path, rank null, empty level, not_eligible, skipped, closed

    2. Verify the new vitest file passes:
       ```
       cd /home/zak/Dev/PurgeGame/database
       npx vitest run src/api/__tests__/player-baf.test.ts 2>&1 | tail -20
       ```
       Expect all tests green.

    3. Start (or verify) the Fastify dev server at http://localhost:3000.

    4. Query available players to pick a test address:
       ```
       curl -s http://localhost:3000/replay/players | python3 -c "import json,sys; d=json.load(sys.stdin); print('\\n'.join(d['players'][:5]))"
       ```

    5. Probe the INTEG-05 endpoint with a player + level that has indexed BAF data (level 20 is known-live per 54-RESEARCH.md Section 7):
       ```
       PLAYER=0xfabb0ac9d68b0b445fb7357272ff202c5651694a  # known top-4 at level 20
       LEVEL=20
       curl -s "http://localhost:3000/player/$PLAYER/baf?level=$LEVEL" | python3 -m json.tool
       ```

    6. Verify the response includes the six top-level keys per INTEG-05-SPEC.md:
       - [ ] `level` (echo of query `level`)
       - [ ] `player` (echo of path `:address`, lowercased)
       - [ ] `score` (wei-encoded BURNIE decimal string)
       - [ ] `rank` (integer 1..N, OR null if player has no stake)
       - [ ] `totalParticipants` (integer, may be 0)
       - [ ] `roundStatus` (one of "open" | "closed" | "skipped" | "not_eligible")

    7. Spot-check edge cases:

       **Edge: player at top (rank 1 of level 20)**
       ```
       PLAYER_RANK1=0xdf37f81daad2b0327a0a50003740e1c935c70913  # known rank 1 at level 20 per RESEARCH §7
       curl -s "http://localhost:3000/player/$PLAYER_RANK1/baf?level=20" | python3 -m json.tool
       ```
       Expect: `rank: 1`, score matching /leaderboards/baf rank 1 score, totalParticipants >= 4, roundStatus: "open" or "closed" (depending on current state).

       **Edge: player has no BAF stake at level**
       ```
       SOME_PLAYER=0x71be63f3384f5fb98995898a86b02fb2426c5788
       HIGH_LEVEL=100  # likely no stake here
       curl -s "http://localhost:3000/player/$SOME_PLAYER/baf?level=$HIGH_LEVEL" | python3 -m json.tool
       ```
       Expect: `score: "0"`, `rank: null`, `totalParticipants` echoes level-wide count (may be 0 or >0 depending on others).

       **Edge: level is not a BAF level (level % 10 !== 0)**
       ```
       curl -s "http://localhost:3000/player/$PLAYER/baf?level=7" | python3 -m json.tool
       ```
       Expect: `score: "0"`, `rank: null`, `totalParticipants: 0`, `roundStatus: "not_eligible"`.

       **Edge: missing level query param (400)**
       ```
       curl -s "http://localhost:3000/player/$PLAYER/baf" -w "\n%{http_code}\n"
       ```
       Expect: HTTP 400 with Zod validation error JSON.

    8. Confirm the response matches the Wave 1 baf-panel #renderYourRank helper expectations:
       - `data.rank` is number or null
       - `data.totalParticipants` is number
       - `data.score` is decimal string (wei-scale BURNIE)
       - `data.roundStatus` is one of the 4 enum values

    9. If the shipped shape DIFFERS from INTEG-05-SPEC.md (e.g., `roundState` instead of `roundStatus`, or `scoreWei` instead of `score`):
       - Document the drift in the resume signal: `endpoint-shipped shape-drift -- [field renames]`
       - Wave 2 Task 2 will adjust the #renderYourRank + #renderRoundStatus calls to match.

    **Resume options:**

    - If all checks pass and the shipped shape matches the spec, resume with signal `endpoint-shipped` (optionally paste the 3 commit hashes from database/main).
    - If the shipped shape drifts from the spec, resume with `endpoint-shipped shape-drift -- [field renames]` so Task 2 adjusts the render calls.
    - If the endpoint is NOT ready, resume with `deferred -- [reason]` (e.g., `deferred -- database-side not implemented; switching to /database/ to ship`). Wave 2 stays blocked until the endpoint lands.

    **Estimated time:** 5-15 minutes per Phase 51 + Phase 52 precedent for the side-quest (3 commits), plus 2-3 minutes for the curl verification.
  </how-to-verify>
  <resume-signal>Type "endpoint-shipped" (optionally append " shape-drift -- [field renames]" if INTEG-05 ships with a different response shape, or paste the 3 commit hashes from database/main), or "deferred -- [reason]".</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Surgical edit to #refetchPlayer() comments + adjust if INTEG-05 ships with shape drift</name>
  <files>play/components/baf-panel.js</files>
  <read_first>
    - play/components/baf-panel.js (Wave 1 file; locate #refetchPlayer() method; confirm it's the only method being edited)
    - Task 1 checkpoint resume signal (pass with/without shape-drift, OR deferred -- if deferred, Task 2 is skipped entirely)
    - .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md (the spec; if the shipped shape differs, re-read the original to understand which fields map to the new names)
    - play/app/__tests__/play-baf-panel.test.js (all assertions must stay green)
  </read_first>
  <action>
Wave 2's real code change is minimal. The Wave 1 #refetchPlayer() was written defensively to handle BOTH a 404 (silent fallback) AND a successful response (parse + render). In Wave 2, the only thing that changes is:

1. The comment inside the `if (!res.ok)` branch is adjusted to reflect the new meaning (no longer "Wave 1 stub"; now "INTEG-05 error path fallback").
2. IF the Task 1 resume signal included `shape-drift`, the #renderYourRank or #renderRoundStatus calls are adjusted to match the shipped field names.
3. Everything else stays unchanged.

## Standard case (no shape drift) -- comment-only edit

Use the Edit tool. Locate the exact text in `play/components/baf-panel.js`:

```javascript
    if (!res.ok) {
      // Wave 1: INTEG-05 not yet shipped in /home/zak/Dev/PurgeGame/database/.
      // Silent 404 tolerance. Wave 2 flips this to live rendering.
      // Fall back to inline roundStatus derivation for the pill.
      this.#renderRoundStatusFallback(level);
      return;
    }
```

Replace with:

```javascript
    if (!res.ok) {
      // INTEG-05 error path (400/500): fall back to inline derivation for the
      // round-status pill so the panel degrades gracefully. The your-rank row
      // stays hidden because we have no rank data to render. Note: INTEG-05
      // returns 200 even when the player has no BAF stake (rank=null case);
      // only true errors reach this branch post-Wave-2.
      this.#renderRoundStatusFallback(level);
      return;
    }
```

That is the only text change in the standard case. The ok-path at lines below it already handles the live response correctly (parses JSON, passes to #renderYourRank + #renderRoundStatus).

## Shape-drift case (resume signal included `shape-drift -- [field renames]`)

If the database repo shipped INTEG-05 with renamed fields (e.g., `roundState` instead of `roundStatus`, or `scoreWei` instead of `score`), apply targeted adjustments to #renderYourRank and #renderRoundStatus as follows:

**Example: if `roundStatus` was renamed to `roundState`:**

Locate:
```javascript
    this.#renderYourRank(data);
    this.#renderRoundStatus(data?.roundStatus);
```

Replace with (adapting the field name):
```javascript
    this.#renderYourRank(data);
    this.#renderRoundStatus(data?.roundState);  // backend shipped as roundState
```

**Example: if `score` was renamed to `scoreWei`:**

Locate in #renderYourRank:
```javascript
    // Score-unit: INTEG-05 score is WEI-scale -> formatBurnie.
    const scoreEl = this.#bind('your-score');
    if (scoreEl) {
      scoreEl.textContent = (data.score && data.score !== '0')
        ? `${formatBurnie(data.score)} BURNIE`
        : '0 BURNIE';
    }
```

Replace with:
```javascript
    // Score-unit: INTEG-05 score field shipped as `scoreWei` (WEI-scale) -> formatBurnie.
    const scoreEl = this.#bind('your-score');
    if (scoreEl) {
      scoreEl.textContent = (data.scoreWei && data.scoreWei !== '0')
        ? `${formatBurnie(data.scoreWei)} BURNIE`
        : '0 BURNIE';
    }
```

Other fields that may drift: `rank` -> `playerRank`; `totalParticipants` -> `participantCount`. Apply the same pattern: find the `data.<field>` reference and swap to `data.<newField>`.

## Skip case (Task 1 resolved as `deferred`)

If Task 1 resume signal was `deferred -- [reason]`, Task 2 is SKIPPED entirely. The Wave 1 baf-panel.js remains unchanged; the 404-tolerant fallback continues to work. Phase 54 cannot close out BAF-01 + BAF-03 full validation until the endpoint ships in a future cycle. Record the deferral in the 54-03-SUMMARY.md and escalate to the user.

## Self-check after edit (when Task 1 was endpoint-shipped)

- `play/components/baf-panel.js` LOC within 2-3 lines of Wave 1 final (comment-block adjustment only; possibly ~5-line drift if shape-drift required multiple field swaps).
- The phrase "Wave 1: INTEG-05 not yet shipped" is GONE from the file.
- The phrase "Wave 2 flips this to live" is GONE from the file.
- The #refetchPlayer() method still calls `this.#renderYourRank(data)` and `this.#renderRoundStatus(data?.roundStatus)` (or the shape-drift-adjusted names).
- The ok-path unchanged from Wave 1 (it was already correct).
- `node --test play/app/__tests__/play-baf-panel.test.js` all green.
- `node --test play/app/__tests__/play-shell-01.test.js` green.
- All prior phase tests green.

## Live-browser verification (brief manual check after code edit)

Open http://localhost:8080/play/ after both website + database dev servers are running. Pick a player known to have BAF stake at level 20 (e.g., 0xfabb0ac9d68b0b445fb7357272ff202c5651694a per live data). Scrub to a day where that player is in top-4. The leaderboard should show the player highlighted via aria-current. Now pick a player who is NOT in top-4 at that level. The your-rank row should un-hide below the leaderboard showing "You: rank N of M -- {score} BURNIE".

If the your-rank row doesn't appear, inspect the DevTools Network tab for the `/player/${addr}/baf?level=20` request. A 200 response with `rank: null` means the player has no BAF stake at that level (correct; row stays hidden). A 200 response with `rank: 7` (or similar) should un-hide the row. A non-200 means INTEG-05 is misconfigured -- return to Task 1.
  </action>
  <verify>
    <automated>node --check play/components/baf-panel.js && node --test play/app/__tests__/play-baf-panel.test.js && node --test play/app/__tests__/play-coinflip-panel.test.js && node --test play/app/__tests__/play-shell-01.test.js && node --test play/app/__tests__/play-profile-panel.test.js && node --test play/app/__tests__/play-tickets-panel.test.js && node --test play/app/__tests__/play-packs-panel.test.js && node --test play/app/__tests__/play-jackpot-wrapper.test.js && node --test play/app/__tests__/play-jackpot-shell01-regression.test.js && node --test play/app/__tests__/play-panel-stubs.test.js && node --test play/app/__tests__/play-route-structure.test.js && node --test play/app/__tests__/play-main-bootstrap.test.js && node --test play/app/__tests__/play-purchase-panel.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check play/components/baf-panel.js` exits 0
    - `wc -l play/components/baf-panel.js` reports >= 240 (Wave 1 baseline preserved)
    - `! grep -q "Wave 1: INTEG-05 not yet shipped" play/components/baf-panel.js` exits 0 (stub comment removed)
    - `! grep -q "Wave 2 flips this to live" play/components/baf-panel.js` exits 0 (stub comment removed)
    - `grep -q "INTEG-05 error path" play/components/baf-panel.js` exits 0 (new comment in place)
    - `grep -c "this.#renderYourRank(data)" play/components/baf-panel.js` returns 1 (ok-path still calls render)
    - `grep -cE "this\\.#renderRoundStatus\\(data\\?\\.\\w+\\)" play/components/baf-panel.js` returns 1 (ok-path renders round status; field name may drift)
    - `grep -c "/player/.*?/baf\\?level=" play/components/baf-panel.js` returns 1 (fetch URL unchanged)
    - `grep -c "#bafPlayerFetchId" play/components/baf-panel.js` returns >= 3 (stale-guard preserved)
    - `grep -c "formatBurnie" play/components/baf-panel.js` returns >= 2 (score rendering preserved)
    - `node --test play/app/__tests__/play-baf-panel.test.js` exits 0 (all Wave 0 assertions still green)
    - `node --test play/app/__tests__/play-coinflip-panel.test.js` exits 0 (Phase 54 Plan 54-02 Task 1 still green)
    - `node --test play/app/__tests__/play-shell-01.test.js` exits 0 (recursive scan still green)
    - `node --test play/app/__tests__/play-profile-panel.test.js` exits 0 (Phase 51)
    - `node --test play/app/__tests__/play-tickets-panel.test.js` exits 0 (Phase 52)
    - `node --test play/app/__tests__/play-packs-panel.test.js` exits 0 (Phase 52)
    - `node --test play/app/__tests__/play-jackpot-wrapper.test.js` exits 0 (Phase 52)
    - `node --test play/app/__tests__/play-jackpot-shell01-regression.test.js` exits 0 (D-09 patch held)
    - `node --test play/app/__tests__/play-panel-stubs.test.js` exits 0 (Phase 50)
    - `node --test play/app/__tests__/play-route-structure.test.js` exits 0 (Phase 50)
    - `node --test play/app/__tests__/play-main-bootstrap.test.js` exits 0 (Phase 50)
    - `node --test play/app/__tests__/play-purchase-panel.test.js` exits 0 (Phase 53)
  </acceptance_criteria>
  <done>
    `play/components/baf-panel.js` #refetchPlayer() comment block is updated from Wave-1 stub language to Wave-2 "error path fallback" language. If the shipped INTEG-05 response schema drifted from INTEG-05-SPEC.md, the #renderYourRank / #renderRoundStatus calls are adjusted to reference the shipped field names. The ok-path behavior is unchanged (it was already defensive-correct in Wave 1). All 13 play/ test suites continue to pass. BAF-01 is now fully validated: a non-top-4 player sees their rank + total + score rendered below the leaderboard via the un-hidden your-rank row. BAF-03 round-status pill is now authoritative: "closed" and "skipped" states are visible (not just the Wave 1 fallback of "open" or "not_eligible"). SHELL-01 recursive guardrail continues to hold.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INTEG-05 response -> baf-panel #renderYourRank / #renderRoundStatus | Wave 2 binds to the shipped response shape. If the shipped shape differs from INTEG-05-SPEC.md, the shape-drift branch in Task 2 action re-maps field names. Render helpers remain textContent-based (no XSS surface). |
| database/main commits -> solo-dev verification | Checkpoint requires the user to confirm 3 atomic commits landed (feat + docs + test) mirroring Phase 51+52 precedent; no external team attests. Repudiation-accept per Phase 51+52 practice. |
| /home/zak/Dev/PurgeGame/database vitest -> endpoint correctness | Commit 3 of the side-quest adds vitest tests covering happy path + null rank + empty level + not_eligible + skipped + closed; tests pass before the endpoint is considered shipped. |
| baf-panel error path -> user-visible degradation | If INTEG-05 returns 500 post-ship, the panel falls back to inline roundStatus derivation (level % 10 heuristic) and the your-rank row stays hidden. The leaderboard + context row remain functional. Error is silent (no console throw that surfaces to the user) per 54-RESEARCH.md T-54-13. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-20 | Tampering | INTEG-05 response before it reaches #renderYourRank/#renderRoundStatus | mitigate | #bafPlayerFetchId stale-guard with 2 token checks (post-fetch + post-json) catches interleaved responses during rapid scrub. Pattern inherited from Wave 1. |
| T-54-21 | Spoofing | INTEG-05 roundStatus field spoofed with unknown enum value | mitigate | #renderRoundStatus LABELS object maps known enum values; unknown falls to `'--'` text + empty data-status attribute (no CSS match = base pill styling). Fails safe. |
| T-54-22 | Spoofing | Compromised backend returns HTML-shaped player field | mitigate | #renderYourRank uses textContent for all data writes. data.player is echoed via server path and already validated by addressParamSchema; the your-rank row renders only the rank and score, not the player address. |
| T-54-23 | Information disclosure | INTEG-05 error paths emit status codes via console | accept | Console warnings are dev-mode debug; DOM fallback text is generic ("BAF leaderboard unavailable.") and the fallback round-status is strictly conservative. No PII leaked. |
| T-54-24 | Denial of Service | Rapid player-scrub triggers N INTEG-05 fetches | accept | #bafPlayerFetchId counter invalidates stale responses but does not rate-limit outbound requests. Web scale: each request is small. No dedup helper needed (RESEARCH Section 10 determined no wire-dedup benefit). |
| T-54-25 | Elevation of Privilege | Shape-drift fix applied without re-running test suite | mitigate | Task 2 acceptance criteria run all 13 play/ test files; any regression in existing tests (e.g., #refetchPlayer identifier removed) fails CI before commit. |
| T-54-26 | Repudiation | Database-repo INTEG-05 commits as proof of delivery | accept | Task 1 checkpoint requires git log verification against the 3-atomic-commit shape; solo-dev self-coordination per Phase 51+52 precedent. |
| T-54-27 | Tampering | Wave 2 edit accidentally breaks Wave 1 404-tolerance branch | mitigate | The surgical edit only touches the comment text inside the `if (!res.ok)` branch; the `#renderRoundStatusFallback(level); return;` logic stays intact. Acceptance criteria grep for the presence of `INTEG-05 error path` comment and absence of the Wave-1 stub language. |
</threat_model>

<verification>
- Task 1 checkpoint pauses until INTEG-05 is confirmed live via curl + vitest (or is deferred with a clear reason).
- Task 2 performs 1-3 surgical comment edits (1 in the standard case; 2-3 if shape-drift was flagged).
- After Task 2, all Phase 54 test assertions turn green AND the Wave 1 ok-path + fallback logic both continue to work.
- SHELL-01 guardrail stays green; no new play/ imports.
- Phase 50 + 51 + 52 + 53 + 54-01 + 54-02 tests continue to pass.
- The dev browser at http://localhost:8080/play/ renders the BAF panel with: (a) top-4 leaderboard with D-06 prominence styling; (b) your-rank row un-hidden for players outside top-4 (e.g., "You: rank 7 of 42 -- 344,863 BURNIE"); (c) round-status pill showing authoritative state (open/closed/skipped/not_eligible).
- INTEG-05 side-quest is recorded in 54-03-SUMMARY.md with 3 database/main commit hashes.
</verification>

<success_criteria>
- [ ] Task 1 checkpoint resolved with `endpoint-shipped` (optionally with shape-drift markers) OR `deferred` with clear rationale
- [ ] INTEG-05 endpoint is reachable at `http://localhost:3000/player/{addr}/baf?level=N` and returns the INTEG-05-SPEC.md-compliant JSON shape (or a documented-drift shape)
- [ ] Database-side vitest passes for the new endpoint (6 test scenarios: happy path + rank null + empty level + not_eligible + skipped + closed)
- [ ] `play/components/baf-panel.js` #refetchPlayer() has its Wave-1 stub comment replaced with Wave-2 "error path fallback" comment
- [ ] IF shape-drift: #renderYourRank + #renderRoundStatus field references updated to match shipped schema
- [ ] All 13 play/ test files pass: play-coinflip-panel, play-baf-panel, play-tickets-panel, play-packs-panel, play-jackpot-wrapper, play-jackpot-shell01-regression, play-shell-01, play-panel-stubs, play-route-structure, play-main-bootstrap, play-profile-panel, play-purchase-panel, plus any Phase 54 additions
- [ ] BAF-01 validated: a non-top-4 player sees "You: rank N of M -- {score} BURNIE" row below the leaderboard when selected
- [ ] BAF-03 fully validated: round-status pill shows authoritative state (including "skipped" and "closed" which Wave 1 fallback cannot detect)
- [ ] SHELL-01 recursive guardrail stays green with 11 FORBIDDEN entries
- [ ] Phase 54 requirement matrix updated in 54-03-SUMMARY.md: COINFLIP-01/02/03 validated in Wave 1; BAF-01/02/03 validated in Wave 2; INTEG-04 formally deferred (Wave 0); INTEG-05 shipped (Wave 2)
</success_criteria>

<output>
After completion, create `.planning/phases/54-coinflip-baf/54-03-SUMMARY.md` per the standard template, including:
- INTEG-05 side-quest execution record: 3 commit hashes on database/main (format: feat + docs + test mirroring Phase 51 INTEG-02 and Phase 52 INTEG-01)
- Database-side test pass: vitest green for the new happy path + rank null + empty level + not_eligible + skipped + closed fixtures
- Website-side test pass: 13/13 play/ test files green
- Files modified: 1 (baf-panel.js)
- LOC delta: ~3-5 lines changed (comment text + possibly 1-3 field name references if shape-drift)
- Phase 54 exit state: ready for Wave 3 (manual UAT or deferral)
- REQUIREMENTS.md updates: INTEG-05 flipped from [ ] Pending to [x] Validated (2026-04-24); BAF-01 + BAF-02 + BAF-03 + COINFLIP-01 + COINFLIP-02 + COINFLIP-03 all flipped to [x] Validated; Traceability table rows updated; footer timestamp updated
</output>

## Plan Summary

**Objective:** Wave 2 -- wire the shipped INTEG-05 endpoint into the baf-panel for full BAF-01 + BAF-03 validation; hard-gated on database-side delivery.

**Tasks:**
1. (CHECKPOINT) Verify INTEG-05 endpoint live at `localhost:3000/player/{addr}/baf?level=` with happy path + edge cases (rank null + empty level + not_eligible + 400); confirm 3 atomic commits on database/main; resume with `endpoint-shipped` (+/- shape-drift markers) or `deferred`
2. Surgical comment edit to #refetchPlayer() in baf-panel.js (rename "Wave 1 stub" language to "INTEG-05 error path fallback"); if shape-drift was flagged, adjust #renderYourRank/#renderRoundStatus field references to match shipped schema

**Verify chain:**
- Task 1 blocks until backend ships (3 atomic commits on database/main expected)
- Task 2 is a 3-5 line comment edit (possibly 1-3 field name swaps); preserves all Wave 1 render helpers + stale-guards + defensive ok-path
- After Task 2, all 13 play/ test files pass
- REQUIREMENTS.md updates in 54-03-SUMMARY.md flip INTEG-05 to [x] Validated + all Phase 54 requirements to [x]

**Context cost estimate:** ~15% for Task 2 (small surgical edit + test re-runs); checkpoint is interactive so no consumption beyond what the user provides during the database side-quest.

**Duration:** Task 1 blocks on database side-quest (variable; 5-15 min per Phase 51 + 52 precedent). Task 2 runs in 1-2 minutes. Live-browser verification adds 2-3 minutes.
