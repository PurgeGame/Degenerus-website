---
phase: 54
plan: 02
slug: wave1-panels-hydration-and-css
type: execute
wave: 1
depends_on:
  - 54-01-PLAN.md
files_modified:
  - play/components/coinflip-panel.js
  - play/components/baf-panel.js
  - play/styles/play.css
autonomous: true
requirements:
  - COINFLIP-01
  - COINFLIP-02
  - COINFLIP-03
  - BAF-02
  - BAF-03
tags:
  - phase-54
  - wave-1
  - panel-hydration
  - game-ui
  - shell-01
  - score-unit-discipline

must_haves:
  truths:
    - "play/components/coinflip-panel.js evolves from 40-LOC Phase 50 stub to ~220-260 LOC hydrated panel with bounty section (COINFLIP-03), state section (COINFLIP-01), and leaderboard section (COINFLIP-02); all three sections render live from existing endpoints already shipped by Phase 51 INTEG-02"
    - "play/components/baf-panel.js evolves from 40-LOC Phase 50 stub to ~240-280 LOC hydrated panel with context row + round-status pill (BAF-03 partial), top-4 leaderboard with prominence data-rank attribute (BAF-02), and a hidden your-rank row (BAF-01; hydration wired but 404-tolerant until Wave 2 ships INTEG-05)"
    - "Both panels' fetch methods use the profile-panel.js stale-guard template verbatim -- #coinflipFetchId + #bafFetchId + #bafPlayerFetchId counters; token checks after every await point; is-stale class toggled for keep-old-data-dim (D-17 + D-18 carry-forward)"
    - "Wave 1 ships the baf-panel INTEG-05 fetch path as a 404-tolerant stub: #refetchPlayer() calls /player/:addr/baf?level=N, swallows non-OK responses silently (matches Phase 51 precedent). Wave 2 flips the response handling on once the endpoint ships."
    - "play/styles/play.css gains ~130 LOC of Phase 54 additions: BAF prominence tiers (rank 1 gold #FFD700, rank 2 silver #C0C0C0, rank 3 bronze #CD7F32, rank 4 regular per D-06), panel-level data-prominence borders (high/medium/low), coinflip bounty + leaderboard styling, round-status pill colors (open/closed/skipped/not_eligible), selected-player aria-current highlight, is-stale class for both panels, armed-pulse keyframe for bounty indicator"
    - "Score-unit discipline per Pitfall 8 is HONORED in render helpers: coinflip leaderboard scores render as integer BURNIE via direct `String(entry.score)` (no wei division); player-state coinflip amounts (deposited, claimable, bounty pool, biggest-flip, take-profit) render as wei-scale via formatBurnie; BAF leaderboard scores + INTEG-05 score render as wei-scale via formatBurnie"
    - "SHELL-01 recursive guardrail stays green: no new play/ file imports ethers, beta/app/wallet.js, beta/app/contracts.js, beta/app/utils.js, beta/app/coinflip.js, beta/app/baf.js, beta/components/coinflip-panel.js, beta/components/baf-panel.js, beta/components/connect-prompt.js, beta/components/purchase-panel.js, or beta/components/decimator-panel.js"
    - "Phase 50 stub-registration for <coinflip-panel> and <baf-panel> in play/index.html (lines 54-55) remains valid; Wave 1 does not touch play/index.html -- both panels are evolved in-place in their existing component files, preserving customElements.define() calls"
    - "Wave 0 test files (play-coinflip-panel.test.js ~28 assertions, play-baf-panel.test.js ~30 assertions) transition from RED (0 passes / ~58 fails) to GREEN or NEAR-GREEN: all markup/fetch/stale-guard/score-unit assertions pass; any BAF-01 assertions requiring live INTEG-05 data remain pending to Wave 2"
    - "Both panels use the existing skeleton-shimmer + data-bind='content' swap pattern from Phase 50; #showContent() helper flips skeleton hidden -> content visible on first successful fetch"
    - "Empty states render per D gray-area decisions: coinflip empty -> 'No coinflip activity for day N'; BAF empty -> 'No BAF activity at this level' (or round-status pill conveys the state when level % 10 != 0)"
    - "Selected-player highlighting uses [aria-current='true'] on matching leaderboard rows per 54-CONTEXT Gray Areas; CSS applies background + underline styling"
  artifacts:
    - path: "play/components/coinflip-panel.js"
      provides: "Hydrated <coinflip-panel> Custom Element: TEMPLATE string with bounty/state/leaderboard sections; #coinflipFetchId stale-guard; #refetch() parallel-fetch over /player/:addr?day= + /leaderboards/coinflip?day=; render helpers for bounty, state, leaderboard rows; connectedCallback subscribes to replay.day + replay.player; aria-current highlighting for selected player"
      min_lines: 220
      contains: "data-bind=\"deposited\""
    - path: "play/components/baf-panel.js"
      provides: "Hydrated <baf-panel> Custom Element: TEMPLATE string with context row (next-baf-level, levels-until, round-status) + leaderboard (4 rows with data-rank 1-4) + hidden your-rank row; #bafFetchId + #bafPlayerFetchId dual stale-guards; #refetchLeaderboard() fetches /leaderboards/baf?level=; #refetchPlayer() fetches /player/:addr/baf?level= (404-tolerant Wave 1 stub); inline bafContext logic (nextBafLevel = Math.ceil((level+1)/10)*10); data-prominence panel-level attribute; aria-current highlighting"
      min_lines: 240
      contains: "data-rank"
    - path: "play/styles/play.css"
      provides: "Phase 54 CSS append: ~130 LOC covering .play-coinflip-panel + .play-baf-panel scopes; prominence tier colors; round-status pill states; bounty armed indicator + @keyframes armed-pulse; coinflip + BAF leaderboard table grids; selected-player aria-current highlight; is-stale class for both panels"
      min_lines: 130
      contains: "data-rank"
  key_links:
    - from: "play/components/coinflip-panel.js"
      to: "GET /player/:address?day=N (coinflip block; already live via Phase 51 INTEG-02)"
      via: "fetch inside #refetch()"
      pattern: "/player/.*?day="
    - from: "play/components/coinflip-panel.js"
      to: "GET /leaderboards/coinflip?day=N (already live)"
      via: "fetch inside #refetch()"
      pattern: "/leaderboards/coinflip\\?day="
    - from: "play/components/baf-panel.js"
      to: "GET /leaderboards/baf?level=N (already live)"
      via: "fetch inside #refetchLeaderboard()"
      pattern: "/leaderboards/baf\\?level="
    - from: "play/components/baf-panel.js"
      to: "GET /player/:address/baf?level=N (INTEG-05; Wave 2 delivers; Wave 1 uses 404-tolerant stub)"
      via: "fetch inside #refetchPlayer()"
      pattern: "/player/.*?/baf\\?level="
    - from: "play/styles/play.css"
      to: "play/components/coinflip-panel.js + play/components/baf-panel.js class names + data-* attributes"
      via: "CSS selectors matching .play-coinflip-*, .play-baf-*, [data-rank], [data-status], [data-prominence]"
      pattern: "\\.play-(coinflip|baf)-"
---

<objective>
Wave 1 hydrates both Phase 50 stub panels into fully functional read-only leaderboards using existing endpoints, and lands the BAF-01 INTEG-05 fetch path as a 404-tolerant stub that Wave 2 flips live. The coinflip panel ships all three requirements (COINFLIP-01, COINFLIP-02, COINFLIP-03) because Phase 51's INTEG-02 already shipped the extended /player/:address?day=N response with the coinflip block. The BAF panel ships BAF-02 (top-4 leaderboard) + BAF-03 partial (level label + levels-until; round-status pill rendered but source comes from INTEG-05 in Wave 2) and pre-wires BAF-01 (your-rank row) with the INTEG-05 fetch that 404s silently until the endpoint ships.

Purpose: Close out 5 of Phase 54's 6 requirements (all three COINFLIP-* plus BAF-02 + BAF-03 core) using only endpoints already shipped. Wave 2 closes out BAF-01 (plus BAF-03 roundStatus source) once INTEG-05 lands. This mirrors Phase 51's Wave 1 + Wave 2 split (pre-backend scaffold + hard-gated fetch wiring).

Output:
- `play/components/coinflip-panel.js` evolves from 40-LOC stub to ~220-260 LOC hydrated panel; same file, evolved in place, customElements.define() line preserved.
- `play/components/baf-panel.js` evolves from 40-LOC stub to ~240-280 LOC hydrated panel; same file, evolved in place.
- `play/styles/play.css` gains a ~130-LOC Phase 54 section appended to the end of the file; pattern copies verbatim from 54-RESEARCH.md Section 9 lines 662-878.
- No new files created. No changes to play/index.html (Phase 50 registration holds). No changes to play/app/main.js (coinflip + baf are subscribe-driven; no main.js bootstrap needed).
- No optional `play/app/leaderboards-fetch.js` helper (RESEARCH Section 10 + PATTERNS.md planner decision default: SKIP; the two panels hit two different endpoints and wire-dedup provides no benefit).

Wave 0 tests transition from RED to mostly GREEN: all markup/fetch/stale-guard/score-unit/aria-current assertions pass. The INTEG-05-dependent BAF-01 assertions (e.g., "fetches /player/:addr/baf?level=") pass because the fetch call IS emitted -- the response just 404s silently until Wave 2. Any assertion that greps for live rank rendering against non-mock data remains Wave-2 territory and is not expected to pass here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-coinflip-baf/54-CONTEXT.md
@.planning/phases/54-coinflip-baf/54-RESEARCH.md
@.planning/phases/54-coinflip-baf/54-PATTERNS.md
@.planning/phases/54-coinflip-baf/54-VALIDATION.md
@.planning/phases/54-coinflip-baf/INTEG-05-SPEC.md
@.planning/phases/54-coinflip-baf/54-01-PLAN.md

<!-- Wave 0 deliverables -->
@play/app/__tests__/play-coinflip-panel.test.js
@play/app/__tests__/play-baf-panel.test.js
@play/app/__tests__/play-shell-01.test.js

<!-- Phase 50 stubs being evolved -->
@play/components/coinflip-panel.js
@play/components/baf-panel.js

<!-- Phase 51 gold-standard template for hydrated panel (COPY stale-guard shape from here) -->
@play/components/profile-panel.js

<!-- Phase 52 secondary template for list-row rendering -->
@play/components/tickets-panel.js

<!-- Wallet-free formatter surface -->
@beta/viewer/utils.js

<!-- Store API -->
@beta/app/store.js

<!-- Current play.css to understand append point -->
@play/styles/play.css

<!-- Existing Phase 50 HTML structure (unchanged this wave) -->
@play/index.html

<!-- Reference for beta prominence styling (PATTERN-MATCH ONLY; DO NOT IMPORT) -->
@beta/styles/baf.css
@beta/styles/coinflip.css

<interfaces>
<!-- Wallet-free imports (from 54-PATTERNS.md Shared Patterns section) -->
import { subscribe, get } from '../../beta/app/store.js';
import { API_BASE } from '../app/constants.js';
import { formatBurnie, truncateAddress } from '../../beta/viewer/utils.js';

<!-- Endpoint shapes (verified live per 54-RESEARCH.md Section 7) -->

-- GET /player/:address?day=N (Phase 51 INTEG-02 live)
-- Response includes top-level `coinflip: { depositedAmount, claimablePreview,
--   autoRebuyEnabled, autoRebuyStop, currentBounty, biggestFlipPlayer,
--   biggestFlipAmount }` object (all string fields except autoRebuyEnabled
--   which is boolean; biggestFlipPlayer may be null)
-- All string amounts are WEI-scale BURNIE.

-- GET /leaderboards/coinflip?day=N (already live)
-- Response: { entries: [{ player, score, rank, day }, ...] } up to 10 entries
-- entries[].score is INTEGER-scale BURNIE (NOT wei). Live sample from
-- day 64: "52875" = 52,875 BURNIE. Render via String(entry.score) directly.

-- GET /leaderboards/baf?level=N (already live)
-- Response: { entries: [{ player, score, rank, level }, ...] } exactly 4
-- entries at BAF-active levels (level % 10 == 0), empty otherwise.
-- entries[].score is WEI-scale BURNIE. Live sample level 20 rank 1:
-- "475215212469240581904067". Render via formatBurnie(entry.score).

-- GET /player/:address/baf?level=N (INTEG-05; NOT LIVE in Wave 1; Wave 1
-- fetch silently 404s)
-- Target response per INTEG-05-SPEC.md:
-- { level, player, score, rank, totalParticipants, roundStatus }
-- roundStatus: "open" | "closed" | "skipped" | "not_eligible"
-- score is WEI-scale BURNIE.

<!-- Stale-guard pattern (COPY from profile-panel.js lines 327-365) -->

async #refetch() {
  const addr = get('replay.player');
  const day = get('replay.day');
  const token = ++this.#coinflipFetchId;

  if (!addr || day == null) return;

  if (this.#loaded) {
    this.querySelector('[data-bind="content"]')?.classList.add('is-stale');
  }

  try {
    // ... fetches ...
    if (token !== this.#coinflipFetchId) return;
    // ... parse + render ...
  } catch (err) {
    if (token === this.#coinflipFetchId) {
      this.#renderError();
      this.#showContent();
    }
  }
}

<!-- Inline bafContext (from 54-RESEARCH.md Section 11 line 1122 + 54-PATTERNS.md) -->

function bafContext(level) {
  if (level == null) return { nextBafLevel: null, levelsUntilBaf: null, prominence: 'low' };
  const nextBafLevel = Math.ceil((level + 1) / 10) * 10;
  const levelsUntilBaf = nextBafLevel - level;
  const isBafLevel = level % 10 === 0;
  let prominence;
  if (isBafLevel) prominence = 'high';
  else if (levelsUntilBaf <= 3) prominence = 'high';
  else if (levelsUntilBaf <= 7) prominence = 'medium';
  else prominence = 'low';
  return { nextBafLevel, levelsUntilBaf, isBafLevel, prominence };
}

<!-- Score-unit discipline (Pitfall 8 -- LOAD-BEARING from 54-PATTERNS.md) -->
Endpoint                                        Score format           Render with
/leaderboards/coinflip?day=N                    INTEGER-scale BURNIE   String(entry.score)
/leaderboards/baf?level=N                       WEI-scale BURNIE       formatBurnie(entry.score)
/player/:addr/baf?level=N (INTEG-05 score)      WEI-scale BURNIE       formatBurnie(data.score)
/player/:addr?day=N coinflip block amounts     WEI-scale BURNIE       formatBurnie(value)

<!-- CSS additions (COPY from 54-RESEARCH.md Section 9 lines 662-878 VERBATIM) -->
Rules to append at end of play/styles/play.css:
- .play-baf-panel + data-prominence[high|medium|low] panel-level
- .play-baf-context + .play-baf-player-score + .play-baf-leaderboard grid
- .play-baf-entry[data-rank="1..4"] tier colors (#FFD700, #C0C0C0, #CD7F32, default)
- .play-baf-entry[aria-current="true"] highlight
- .play-baf-your-rank row (hidden in Wave 1 until INTEG-05 populates)
- .play-baf-round-status[data-status="open|closed|skipped|not_eligible"] pill colors
- .play-coinflip-bounty + .play-coinflip-bounty-armed + armed-pulse keyframe
- .play-coinflip-leaderboard grid + header + entry + aria-current
- .play-coinflip-state-row layout
- .is-stale 0.6 opacity + pointer-events: none for both panels

<!-- Selected-player check pattern -->
if (entry.player.toLowerCase() === selectedAddr?.toLowerCase()) {
  row.setAttribute('aria-current', 'true');
}
</interfaces>
</context>

<read_first>
- .planning/phases/54-coinflip-baf/54-CONTEXT.md (D-01..D-10; re-read D-02 no-import rule, D-05 /player/:address coinflip block source, D-06 BAF prominence tiers, D-07 coinflip table, D-08 bounty above leaderboard, D-09 roundStatus source)
- .planning/phases/54-coinflip-baf/54-RESEARCH.md Section 7 (Existing Endpoint Actual Shapes; verified live data samples), Section 9 (Prominence Styling Extraction from beta; CSS to copy verbatim lines 662-878), Section 10 (Custom Elements Structure; TEMPLATE skeletons at lines 899-1004), Section 11 (Day-Aware + Level-Aware Fetch Pattern Reuse; fetch method templates at lines 1050-1179), Section 13 Pitfall 8 (score-unit mismatch; warning signs and mitigation)
- .planning/phases/54-coinflip-baf/54-PATTERNS.md Section "Score-unit discipline" (lines 1462-1477; the unit table to encode in render helpers)
- .planning/phases/54-coinflip-baf/54-PATTERNS.md "Pattern Assignments" for coinflip-panel (lines 41-461) AND baf-panel (lines 462-815) -- these are the full LOC-by-LOC templates
- .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md (Task 1 of 54-01 authored this; response shape + roundStatus enum used by baf-panel #refetchPlayer)
- play/components/profile-panel.js FULL FILE (the gold-standard hydrated-panel template; copy imports, TEMPLATE structure, class shell, stale-guard pattern, #showContent helper, subscribe wiring, connectedCallback + disconnectedCallback)
- play/components/tickets-panel.js lines 77-137 (list-rendering pattern with document.createElement + textContent; coinflip + BAF leaderboards use this for row rendering -- NEVER innerHTML with user data)
- play/styles/play.css lines 300-410 (the .profile-panel CSS section -- the append-pattern for where Phase 54 adds its block)
- play/components/coinflip-panel.js (Phase 50 stub -- line 40 customElements.define() call MUST be preserved)
- play/components/baf-panel.js (Phase 50 stub -- same)
- beta/components/coinflip-panel.js lines 45-101 (beta's panel-header + bounty-tracker layout reference; PATTERN-MATCH ONLY, don't import; beta file is transitively wallet-tainted per 54-RESEARCH.md Section 6)
- beta/components/baf-panel.js full file (171 LOC; BAF panel layout reference including prominence via data-prominence attr; PATTERN-MATCH ONLY)
- beta/styles/baf.css (80 LOC prominence reference; 54-RESEARCH.md Section 9 lines 558-661 extracts the rules to copy)
- beta/styles/coinflip.css (208 LOC coinflip table reference; 54-RESEARCH.md Section 9 lines 810-877 extracts the ~70 LOC Phase 54 needs)
- beta/viewer/utils.js (WALLET-FREE surface; line 2 comment confirms; export list: formatEth, formatBurnie, truncateAddress, formatWei)
- play/app/__tests__/play-coinflip-panel.test.js (Wave 0 output; ~28 assertions this wave must turn green)
- play/app/__tests__/play-baf-panel.test.js (Wave 0 output; ~30 assertions)
- CLAUDE.md (no em dashes, no emojis; use `--`)
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hydrate play/components/coinflip-panel.js to full panel with COINFLIP-01/02/03</name>
  <files>play/components/coinflip-panel.js</files>
  <read_first>
    - play/components/profile-panel.js FULL FILE (417 LOC; the exact template -- imports, TEMPLATE const, class shell, private fields #unsubs/#loaded/#coinflipFetchId/#ready, connectedCallback, disconnectedCallback, #refetch with 3-checkpoint stale-guard, #showContent helper, #bind helper, render helpers per section)
    - play/components/tickets-panel.js lines 77-137 (document.createElement + textContent row-render pattern; coinflip leaderboard rows use this)
    - 54-RESEARCH.md Section 7 lines 285-330 (the verified /player/:address?day=N coinflip block shape)
    - 54-RESEARCH.md Section 7 lines 217-250 (the verified /leaderboards/coinflip?day=N shape + score-unit observation: scores are integer-scale, not wei)
    - 54-RESEARCH.md Section 10 lines 899-960 (coinflip TEMPLATE markup)
    - 54-RESEARCH.md Section 11 lines 1050-1099 (coinflip #refetch parallel-fetch stale-guard pattern)
    - 54-PATTERNS.md Pattern Assignments coinflip-panel section (lines 41-461; LOC-by-LOC analog map)
    - 54-PATTERNS.md Score-unit discipline section (lines 1462-1477; MUST be referenced in render helper comments)
    - play/app/__tests__/play-coinflip-panel.test.js (the ~28 assertions that must turn green after this task)
    - beta/viewer/utils.js lines 1-38 (formatBurnie + truncateAddress signatures)
  </read_first>
  <behavior>
Tests to turn GREEN after this task (from Wave 0 file play-coinflip-panel.test.js):

- Existence + registration (4 assertions) -- customElements.define('coinflip-panel'), class extends HTMLElement, connectedCallback + disconnectedCallback
- SHELL-01 negatives (4 assertions) -- no beta/app/coinflip.js, no beta/app/utils.js, no ethers, no beta/components/coinflip-panel.js imports
- Wallet-free positives (3 assertions) -- imports from beta/viewer/utils.js + beta/app/store.js (subscribe + get) + play/app/constants.js (API_BASE)
- Subscribe wiring (1 assertion) -- subscribes to replay.day AND replay.player (no replay.level)
- COINFLIP-01 state markup (1 assertion) -- data-bind for deposited, claimable, autorebuy, takeprofit
- COINFLIP-02 fetch + markup (2 assertions) -- fetch path /leaderboards/coinflip?day= + data-bind="leaderboard-entries"
- COINFLIP-03 markup (2 assertions) -- bounty data-binds (armed, bounty-pool, bounty-record, bounty-holder) + data-armed attribute
- /player/:addr?day= fetch (1 assertion)
- Stale-guard (2 assertions) -- #coinflipFetchId + is-stale
- Skeleton + content (2 assertions) -- data-bind="skeleton" + data-bind="content" + skeleton-shimmer
- Empty state (1 assertion) -- entries.length or equivalent length-check
- aria-current (1 assertion) -- aria-current for selected-player highlight
- Score-unit (2 assertions) -- formatBurnie + truncateAddress appear in source

Tests that stay green (no regression):
- play-shell-01.test.js (recursive scan stays clean)
- All Phase 50/51/52/53 tests

Implementation approach: REPLACE the entire 40-LOC Phase 50 stub with a ~220-260 LOC hydrated panel following the profile-panel.js template exactly. The customElements.define() line at the very end is preserved.

Key structural invariants:
- innerHTML = TEMPLATE is a static compile-time string (no user interpolation) -- SECURITY T-54-01
- All dynamic rendering uses document.createElement + textContent (never innerHTML with response data) -- prevents XSS on untrusted player names/scores per T-54-02
- Parallel Promise.all fetch -- one HTTP round trip per day-change covers COINFLIP-01 + COINFLIP-02 + COINFLIP-03 simultaneously
- Three stale-guard token checks -- after Promise.all resolves; after playerRes.json(); after lbRes.json() (per 54-RESEARCH.md line 1078-1079)
- Guard rails:
  - `if (!addr || day == null) return;` early-exit before fetching (first-day-load race; 54-RESEARCH Pitfall 9)
  - Graceful partial failure: if leaderboard 404s, still render the bounty + state sections (per 54-RESEARCH.md line 1074)
  - Null biggest-flip-player -> "--" placeholder (Pitfall 7)
  - Zero currentBounty -> NOT ARMED display (data-armed="false")
  </behavior>
  <action>
Use the Write tool to completely rewrite `play/components/coinflip-panel.js`. Target ~220-260 LOC. The structure and exact imports/identifiers MUST match the template below verbatim (identifiers like `#coinflipFetchId`, data-bind strings, fetch URL shapes are asserted by Wave 0 tests).

```javascript
// play/components/coinflip-panel.js -- <coinflip-panel> Custom Element (Phase 54 Wave 1)
//
// Three-section hydrated markup + parallel-fetch wiring with stale-response guard
// and keep-old-data-dim. All data flows from two endpoints shipped prior to Phase 54:
//   - GET /player/:address?day=N (coinflip block; shipped by Phase 51 INTEG-02)
//   - GET /leaderboards/coinflip?day=N (shipped earlier; verified live 2026-04-24)
//
// Sections (top to bottom, per CONTEXT.md D-08 bounty-above-state-above-leaderboard):
//   1. Bounty header (COINFLIP-03) -- current pool + armed indicator + biggest-flip
//      record holder + record amount. Source: /player/:addr?day=N coinflip block.
//   2. Player state (COINFLIP-01) -- deposited amount, claimable preview,
//      auto-rebuy enabled/stop (read-only display; no write UI per read-only scope).
//      Source: same coinflip block.
//   3. Leaderboard (COINFLIP-02) -- top-10 table with rank + truncated address + score.
//      Source: /leaderboards/coinflip?day=N.
//
// Data flow (Wave 1 ship; matches Phase 51 PROFILE-04 pattern):
//   subscribe('replay.day')    \
//                                --> #refetch() --> Promise.all([
//   subscribe('replay.player') /                      fetch /player/:addr?day=,
//                                                     fetch /leaderboards/coinflip?day=,
//                                                   ])
//                                                   --> #renderBounty(coinflip)
//                                                   --> #renderState(coinflip)
//                                                   --> #renderLeaderboard(entries)
//                                                   --> #showContent()
//   #coinflipFetchId stale-guard: token bumped on each #refetch() call; checked
//   after Promise.all resolves, after playerRes.json(), after lbRes.json() so
//   rapid scrubbing never lets a late response clobber a fresh one (D-18).
//   On second-and-later fetches, [data-bind="content"] gets .is-stale
//   (0.6 opacity dim) until the new data renders -- keep-old-data-dim (D-17).
//
// SECURITY (T-54-01): the innerHTML template is a static string with no
// user interpolation. All dynamic writes go through textContent or
// element.setAttribute (never .innerHTML with response data). Day query
// value is URL-encoded at the call site (T-54-21).
//
// SHELL-01: imports only from the wallet-free surface -- beta/app/store.js
// (verified wallet-free), beta/viewer/utils.js (explicit SHELL-01 comment at
// line 2), and play/app/constants.js (pure re-exports).
//
// SCORE-UNIT DISCIPLINE (Pitfall 8 -- 54-RESEARCH.md line 1517):
//   /leaderboards/coinflip score field -> INTEGER-scale BURNIE (NOT wei).
//     Render via String(entry.score). Do NOT call formatBurnie on it
//     (would misinterpret "52875" as 5.2875e-14 BURNIE).
//   coinflip block amounts (deposited, claimable, bounty pool, biggest-flip,
//     take-profit) -> WEI-scale BURNIE. Render via formatBurnie(value).

import { subscribe, get } from '../../beta/app/store.js';
import { API_BASE } from '../app/constants.js';
import { formatBurnie, truncateAddress } from '../../beta/viewer/utils.js';

const TEMPLATE = `
<section data-slot="coinflip" class="panel play-coinflip-panel">

  <div data-bind="skeleton">
    <div class="skeleton-header"><div class="skeleton-line skeleton-shimmer" style="width:40%"></div></div>
    <div class="skeleton-row"><div class="skeleton-line skeleton-shimmer" style="width:60%"></div></div>
    <div class="skeleton-row"><div class="skeleton-block skeleton-shimmer" style="height:120px"></div></div>
  </div>

  <div data-bind="content" hidden>
    <h2 class="panel-title">Coinflip</h2>

    <!-- COINFLIP-03 bounty header -->
    <div class="play-coinflip-bounty" data-bind="bounty">
      <div class="play-coinflip-section-header">Bounty</div>
      <div class="play-coinflip-bounty-armed" data-bind="armed" data-armed="false">NOT ARMED</div>
      <div class="play-coinflip-state-row">
        <span>Pool</span><span data-bind="bounty-pool">0</span>
      </div>
      <div class="play-coinflip-state-row">
        <span>Record flip</span><span data-bind="bounty-record">--</span>
      </div>
      <div class="play-coinflip-state-row">
        <span>Record holder</span><span data-bind="bounty-holder">--</span>
      </div>
    </div>

    <!-- COINFLIP-01 per-player state -->
    <div class="play-coinflip-state">
      <div class="play-coinflip-section-header">Your state</div>
      <div class="play-coinflip-state-row">
        <span>Deposited</span><span data-bind="deposited">0</span>
      </div>
      <div class="play-coinflip-state-row">
        <span>Claimable preview</span><span data-bind="claimable">0</span>
      </div>
      <div class="play-coinflip-state-row">
        <span>Auto-rebuy</span><span data-bind="autorebuy">--</span>
      </div>
      <div class="play-coinflip-state-row">
        <span>Take-profit</span><span data-bind="takeprofit">--</span>
      </div>
    </div>

    <!-- COINFLIP-02 daily leaderboard -->
    <div class="play-coinflip-leaderboard">
      <div class="play-coinflip-header">
        <span>Rank</span><span>Player</span><span>Score</span>
      </div>
      <div class="play-coinflip-entries" data-bind="leaderboard-entries">
      </div>
    </div>
  </div>
</section>
`;

class CoinflipPanel extends HTMLElement {
  #unsubs = [];
  #loaded = false;
  #coinflipFetchId = 0;

  connectedCallback() {
    this.innerHTML = TEMPLATE;

    // Wave 1: subscribe-driven refetch. Coinflip is day-scoped, not level-scoped.
    this.#unsubs.push(
      subscribe('replay.day',    () => this.#refetch()),
      subscribe('replay.player', () => this.#refetch()),
    );

    // Kick an initial fetch in case the store already has both values on first load.
    this.#refetch();
  }

  disconnectedCallback() {
    this.#unsubs.forEach((fn) => fn());
    this.#unsubs = [];
  }

  // --- Stale-guard fetch with 3-checkpoint token comparison -------------

  async #refetch() {
    const addr = get('replay.player');
    const day = get('replay.day');
    const token = ++this.#coinflipFetchId;

    // Pitfall 9: first-day-load race -- no-op until both store values exist.
    if (!addr || day == null) return;

    // D-17 keep-old-data-dim on second-and-later fetches.
    if (this.#loaded) {
      this.querySelector('[data-bind="content"]')?.classList.add('is-stale');
    }

    try {
      // Parallel fetches: COINFLIP-01+03 from player coinflip block,
      // COINFLIP-02 from leaderboards/coinflip.
      const [playerRes, lbRes] = await Promise.all([
        fetch(`${API_BASE}/player/${addr}?day=${encodeURIComponent(day)}`),
        fetch(`${API_BASE}/leaderboards/coinflip?day=${encodeURIComponent(day)}`),
      ]);
      if (token !== this.#coinflipFetchId) return;

      const playerData = playerRes.ok ? await playerRes.json() : null;
      if (token !== this.#coinflipFetchId) return;

      const lbData = lbRes.ok ? await lbRes.json() : null;
      if (token !== this.#coinflipFetchId) return;

      // Graceful partial failure: if leaderboard 404s, still render the bounty+state.
      const coinflip = playerData?.coinflip ?? null;
      const entries = Array.isArray(lbData?.entries) ? lbData.entries : [];

      this.#renderBounty(coinflip);
      this.#renderState(coinflip);
      this.#renderLeaderboard(entries, addr);
      this.#showContent();
      this.querySelector('[data-bind="content"]')?.classList.remove('is-stale');
    } catch (err) {
      if (token === this.#coinflipFetchId) {
        this.#renderError();
        this.#showContent();
        this.querySelector('[data-bind="content"]')?.classList.remove('is-stale');
      }
    }
  }

  // --- Render helpers ---------------------------------------------------

  #bind(key) {
    return this.querySelector(`[data-bind="${key}"]`);
  }

  #renderBounty(coinflip) {
    const pool = coinflip?.currentBounty ?? '0';
    const biggestAmount = coinflip?.biggestFlipAmount ?? '0';
    const biggestPlayer = coinflip?.biggestFlipPlayer ?? null;
    const isArmed = pool && pool !== '0';

    const armedEl = this.#bind('armed');
    if (armedEl) {
      armedEl.setAttribute('data-armed', isArmed ? 'true' : 'false');
      armedEl.textContent = isArmed ? 'ARMED' : 'NOT ARMED';
    }

    // Wei-scale BURNIE -> formatBurnie for user display (score-unit discipline).
    const poolEl = this.#bind('bounty-pool');
    if (poolEl) poolEl.textContent = isArmed ? `${formatBurnie(pool)} BURNIE` : '0';

    const recordEl = this.#bind('bounty-record');
    if (recordEl) {
      recordEl.textContent = (biggestAmount && biggestAmount !== '0')
        ? `${formatBurnie(biggestAmount)} BURNIE`
        : '--';
    }

    const holderEl = this.#bind('bounty-holder');
    if (holderEl) {
      holderEl.textContent = biggestPlayer ? truncateAddress(biggestPlayer) : '--';
    }
  }

  #renderState(coinflip) {
    const deposited = coinflip?.depositedAmount ?? '0';
    const claimable = coinflip?.claimablePreview ?? '0';
    const autoRebuy = coinflip?.autoRebuyEnabled === true;
    const takeProfit = coinflip?.autoRebuyStop ?? '0';

    // All amounts WEI-scale -> formatBurnie.
    const depEl = this.#bind('deposited');
    if (depEl) depEl.textContent = `${formatBurnie(deposited)} BURNIE`;

    const claimEl = this.#bind('claimable');
    if (claimEl) claimEl.textContent = `${formatBurnie(claimable)} BURNIE`;

    const autoEl = this.#bind('autorebuy');
    if (autoEl) autoEl.textContent = autoRebuy ? 'ON' : 'OFF';

    const tpEl = this.#bind('takeprofit');
    if (tpEl) {
      tpEl.textContent = (takeProfit && takeProfit !== '0')
        ? `${formatBurnie(takeProfit)} BURNIE`
        : '--';
    }
  }

  #renderLeaderboard(entries, selectedAddr) {
    const container = this.#bind('leaderboard-entries');
    if (!container) return;

    container.textContent = '';

    if (entries.length === 0) {
      const empty = document.createElement('div');
      empty.className = 'play-coinflip-empty';
      const day = get('replay.day');
      empty.textContent = `No coinflip activity for day ${day ?? '--'}.`;
      container.appendChild(empty);
      return;
    }

    const lowerSelected = selectedAddr ? selectedAddr.toLowerCase() : null;

    for (const entry of entries) {
      const row = document.createElement('div');
      row.className = 'play-coinflip-entry';
      row.setAttribute('data-rank', String(entry.rank));

      if (lowerSelected && entry.player && entry.player.toLowerCase() === lowerSelected) {
        row.setAttribute('aria-current', 'true');
      }

      const rankCell = document.createElement('span');
      rankCell.textContent = `#${entry.rank}`;

      const playerCell = document.createElement('span');
      playerCell.className = 'play-coinflip-player';
      playerCell.textContent = truncateAddress(entry.player);

      // Score-unit discipline: coinflip leaderboard scores are INTEGER-scale BURNIE
      // (live sample day 64 rank 1 = "52875" = 52,875 BURNIE). Do NOT call
      // formatBurnie on these (would misinterpret as wei -> 5.2875e-14).
      const scoreCell = document.createElement('span');
      scoreCell.textContent = `${String(entry.score ?? '0')} BURNIE`;

      row.appendChild(rankCell);
      row.appendChild(playerCell);
      row.appendChild(scoreCell);
      container.appendChild(row);
    }
  }

  #renderError() {
    const container = this.#bind('leaderboard-entries');
    if (container) {
      container.textContent = '';
      const err = document.createElement('div');
      err.className = 'play-coinflip-empty';
      err.textContent = 'Coinflip data unavailable.';
      container.appendChild(err);
    }
  }

  #showContent() {
    const skeleton = this.querySelector('[data-bind="skeleton"]');
    const content = this.querySelector('[data-bind="content"]');
    if (skeleton) skeleton.hidden = true;
    if (content) content.hidden = false;
    this.#loaded = true;
  }
}

customElements.define('coinflip-panel', CoinflipPanel);
```

## Self-check after write

- File replaces the 40-LOC Phase 50 stub with ~220-260 LOC.
- `customElements.define('coinflip-panel', CoinflipPanel);` is the final line.
- All test assertions from play-coinflip-panel.test.js turn green (existence, registration, class shell, 4-path SHELL-01 negatives, 3-path wallet-free positives, 2-subscribe, COINFLIP-01 markup, COINFLIP-02 fetch + markup, COINFLIP-03 markup + data-armed, player fetch, stale-guard + is-stale, skeleton + content, length check, aria-current, formatBurnie + truncateAddress).
- SHELL-01 guardrail continues to pass (no new forbidden imports).
  </action>
  <verify>
    <automated>node --check play/components/coinflip-panel.js && node --test play/app/__tests__/play-coinflip-panel.test.js && node --test play/app/__tests__/play-shell-01.test.js && node --test play/app/__tests__/play-profile-panel.test.js && node --test play/app/__tests__/play-tickets-panel.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check play/components/coinflip-panel.js` exits 0
    - `wc -l play/components/coinflip-panel.js` reports >= 220
    - `grep -c "customElements\.define('coinflip-panel'" play/components/coinflip-panel.js` returns 1
    - `grep -c "#coinflipFetchId" play/components/coinflip-panel.js` returns >= 3 (declaration + 3 token checks at minimum)
    - `grep -c "is-stale" play/components/coinflip-panel.js` returns >= 2
    - `grep -c "formatBurnie" play/components/coinflip-panel.js` returns >= 3 (deposited, claimable, bounty pool etc.)
    - `grep -c "truncateAddress" play/components/coinflip-panel.js` returns >= 2
    - `grep -c "String(entry.score" play/components/coinflip-panel.js` returns 1 (score-unit discipline for coinflip leaderboard)
    - `grep -c "data-bind=\"deposited\"" play/components/coinflip-panel.js` returns 1
    - `grep -c "data-bind=\"claimable\"" play/components/coinflip-panel.js` returns 1
    - `grep -c "data-bind=\"autorebuy\"" play/components/coinflip-panel.js` returns 1
    - `grep -c "data-bind=\"takeprofit\"" play/components/coinflip-panel.js` returns 1
    - `grep -c "data-bind=\"armed\"" play/components/coinflip-panel.js` returns 1
    - `grep -c "data-bind=\"bounty-pool\"" play/components/coinflip-panel.js` returns 1
    - `grep -c "data-bind=\"leaderboard-entries\"" play/components/coinflip-panel.js` returns 1
    - `grep -c "/leaderboards/coinflip?day=" play/components/coinflip-panel.js` returns 1
    - `grep -c "/player/.*?day=\\|/player/\${addr}?day=" play/components/coinflip-panel.js` returns >= 1
    - `grep -c "aria-current" play/components/coinflip-panel.js` returns >= 1
    - `! grep -q "from ['\"][^'\"]*\\/beta\\/app\\/coinflip\\.js['\"]" play/components/coinflip-panel.js` exits 0
    - `! grep -q "from ['\"][^'\"]*\\/beta\\/app\\/utils\\.js['\"]" play/components/coinflip-panel.js` exits 0
    - `! grep -q "from ['\"]ethers['\"]" play/components/coinflip-panel.js` exits 0
    - `node --test play/app/__tests__/play-coinflip-panel.test.js` exits 0 (all Wave 0 assertions green)
    - `node --test play/app/__tests__/play-shell-01.test.js` exits 0 (recursive scan still green)
    - `node --test play/app/__tests__/play-profile-panel.test.js` exits 0 (Phase 51 regression-free)
    - `node --test play/app/__tests__/play-tickets-panel.test.js` exits 0 (Phase 52 regression-free)
  </acceptance_criteria>
  <done>
    `play/components/coinflip-panel.js` is a 220-260 LOC hydrated panel replacing the 40-LOC Phase 50 stub. It ships COINFLIP-01 (state section from /player/:addr?day= coinflip block), COINFLIP-02 (leaderboard from /leaderboards/coinflip?day=), and COINFLIP-03 (bounty header from the same coinflip block). Parallel Promise.all fetch with 3-checkpoint stale-guard on #coinflipFetchId. All Wave 0 test assertions turn green. SHELL-01 recursive guardrail holds. Score-unit discipline is honored: coinflip leaderboard scores render as integer-BURNIE via String(entry.score); all amounts from the player coinflip block render as wei-BURNIE via formatBurnie.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hydrate play/components/baf-panel.js to full panel with BAF-02 + BAF-03 + BAF-01 404-tolerant stub</name>
  <files>play/components/baf-panel.js</files>
  <read_first>
    - play/components/profile-panel.js (template; carry the TEMPLATE const + class shell + stale-guard pattern)
    - play/components/coinflip-panel.js (Task 1 output; near-identical structure -- copy the shape, swap identifiers + endpoints + per-level semantics)
    - 54-RESEARCH.md Section 7 lines 252-284 (verified /leaderboards/baf?level= shape; wei-scale scores; 4 entries top-4 or empty at non-multiple-of-10 levels)
    - 54-RESEARCH.md Section 8 (INTEG-05 target response shape; Wave 1 fetch emits the call but tolerates 404 silently)
    - 54-RESEARCH.md Section 10 lines 962-1015 (baf TEMPLATE markup)
    - 54-RESEARCH.md Section 11 lines 1100-1179 (dual-fetch-id stale-guard pattern with #bafFetchId + #bafPlayerFetchId)
    - 54-RESEARCH.md Section 9 lines 730-760 (data-rank tier colors; the markup must set data-rank on each row so CSS applies gold/silver/bronze/regular)
    - 54-CONTEXT.md D-06 (BAF top-4 prominence styling + "You: rank N of M" row below leaderboard)
    - 54-CONTEXT.md D-09 (roundStatus source from INTEG-05)
    - 54-PATTERNS.md Pattern Assignments baf-panel section (lines 462-815; full LOC-by-LOC map with inline bafContext derivation)
    - 54-PATTERNS.md Score-unit discipline section (lines 1462-1477)
    - play/app/__tests__/play-baf-panel.test.js (~30 assertions that must turn green)
    - .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md (Task 1 of Plan 54-01; the target response shape for Wave 2)
    - beta/components/baf-panel.js (REFERENCE ONLY for layout -- does import from beta/app/baf.js which is wallet-tainted; pattern-match, don't import)
  </read_first>
  <behavior>
Tests to turn GREEN after this task (from Wave 0 file play-baf-panel.test.js):

- Existence + registration (4 assertions)
- SHELL-01 negatives (4 assertions) -- no beta/app/baf.js, no beta/app/utils.js, no beta/components/baf-panel.js, no ethers
- Wallet-free positives (3 assertions) -- beta/viewer/utils.js + beta/app/store.js + play/app/constants.js
- Subscribe wiring (1 assertion) -- replay.level + replay.player (no replay.day)
- BAF-02 fetch + markup (3 assertions) -- /leaderboards/baf?level= + data-bind="leaderboard-entries" + data-rank attribute
- BAF-03 markup (3 assertions) -- data-bind="round-status" + data-status + data-bind="next-baf-level" + data-bind="levels-until"
- BAF-01 fetch + markup (3 assertions) -- fetch /player/${addr}/baf?level= + data-bind="your-rank" + data-bind=[your-rank-value/total-participants/your-score]
- data-prominence (1 assertion)
- Dual stale-guards (3 assertions) -- #bafFetchId + #bafPlayerFetchId + is-stale
- Skeleton + content (2 assertions)
- Inline bafContext (1 assertion) -- Math.ceil((level+1)/10) literal
- Empty state (1 assertion) -- length check
- aria-current (1 assertion)
- Score-unit (2 assertions) -- formatBurnie + truncateAddress

Tests that stay green:
- play-shell-01.test.js (recursive scan stays clean; the 3 new FORBIDDEN entries added in Wave 0 are honored by this implementation)
- All Phase 50/51/52/53/54-01 tests

Implementation approach: REPLACE the entire 40-LOC Phase 50 stub with a ~240-280 LOC hydrated panel following the profile-panel.js template exactly AND the coinflip-panel.js structure from Task 1. Customize for BAF semantics: level-scoped (not day-scoped), dual fetch-id counters, inline bafContext logic, 404-tolerant INTEG-05 stub for Wave 1.

Key structural invariants:
- Panel-level `data-prominence="low|medium|high"` attribute set in connectedCallback after reading initial level from store; updated on level change
- Leaderboard rows MUST set `data-rank` attribute in the range "1" through "4" for the top-4 per D-06
- Your-rank row stays hidden on Wave 1 because INTEG-05 404s silently; Wave 2 un-hides it when the endpoint returns real data
- Round-status pill starts blank; populated by INTEG-05 response in Wave 2 (or by inline derivation fallback based on `level % 10`)
- Two fetch-id counters invalidate independently:
  - `#bafFetchId` bumps on level-change; governs the leaderboard fetch
  - `#bafPlayerFetchId` bumps on level OR player change; governs the INTEG-05 per-player fetch
  - Both use 2-checkpoint token comparison (post-fetch + post-json) per 54-RESEARCH.md Section 11
- Fallback roundStatus derivation when INTEG-05 404s (Wave 1): `level % 10 === 0 ? 'open' : 'not_eligible'` (correct in the simple case; Wave 2 gets authoritative state)
  </behavior>
  <action>
Use the Write tool to completely rewrite `play/components/baf-panel.js`. Target ~240-280 LOC.

```javascript
// play/components/baf-panel.js -- <baf-panel> Custom Element (Phase 54 Wave 1)
//
// Three-section hydrated markup + dual-fetch wiring with stale-response guards
// and keep-old-data-dim. Data flow:
//   - BAF-02 leaderboard top-4: GET /leaderboards/baf?level=N (live pre-Phase-54)
//   - BAF-01 your-rank + BAF-03 round-status: GET /player/:address/baf?level=N
//     (INTEG-05; Wave 1 emits the call and tolerates 404 silently; Wave 2 enables
//     rendering once the endpoint ships per INTEG-05-SPEC.md)
//
// Sections (top to bottom, per 54-RESEARCH.md Section 10):
//   1. Context row (BAF-03 partial) -- next-baf-level label + levels-until label
//      + round-status pill. Level arithmetic is inline (bafContext); round-status
//      reads from INTEG-05 response in Wave 2 with Wave 1 fallback derivation.
//   2. Top-4 leaderboard (BAF-02) -- 4 rows with rank + truncated address + score.
//      Each row has data-rank="1..4" so CSS applies the D-06 gold/silver/bronze/
//      regular tier styling.
//   3. Your-rank row (BAF-01; hidden until INTEG-05 returns data) -- "You: rank N
//      of M -- {score}". Source: INTEG-05 per-player endpoint.
//
// Data flow:
//   subscribe('replay.level')  --> #refetchLeaderboard() + #refetchPlayer()
//   subscribe('replay.player') --> #refetchPlayer()  (leaderboard unchanged)
//
//   Two stale-guard counters (Phase 54 Wave 1 +1 vs Phase 51 precedent):
//     #bafFetchId        -- bumped on leaderboard refetch; 2 token checks
//     #bafPlayerFetchId  -- bumped on INTEG-05 refetch; 2 token checks
//
// Prominence (D-06 -- approach-based not rank-based):
//   Panel-level data-prominence="low|medium|high" reflects how close the game
//   is to the next BAF level. bafContext() below derives prominence.
//   Rank-tier colors (rank 1 gold, rank 2 silver, rank 3 bronze, rank 4 regular)
//   are applied via data-rank attribute + CSS selectors in play.css.
//
// SECURITY (T-54-01): the innerHTML template is a static string with no
// user interpolation. All dynamic writes go through textContent or
// element.setAttribute. URL parameters are URL-encoded at the call site.
//
// SHELL-01: imports only from the wallet-free surface. The three new
// Wave 0 FORBIDDEN entries (beta/components/baf-panel.js, beta/app/baf.js,
// beta/app/coinflip.js) are honored -- none are imported here.
//
// SCORE-UNIT DISCIPLINE (Pitfall 8):
//   /leaderboards/baf score field -> WEI-scale BURNIE. formatBurnie(score).
//   INTEG-05 score field           -> WEI-scale BURNIE. formatBurnie(data.score).
//   Different from coinflip leaderboard (integer-scale) -- do NOT reuse the
//   coinflip-panel String() pattern here.

import { subscribe, get } from '../../beta/app/store.js';
import { API_BASE } from '../app/constants.js';
import { formatBurnie, truncateAddress } from '../../beta/viewer/utils.js';

// Inline bafContext derivation (54-PATTERNS.md recommendation: 15 LOC inline
// avoids a new helper file + avoids importing from wallet-tainted beta/app/baf.js).
function bafContext(level) {
  if (level == null) {
    return { nextBafLevel: null, levelsUntilBaf: null, isBafLevel: false, prominence: 'low' };
  }
  const nextBafLevel = Math.ceil((level + 1) / 10) * 10;
  const levelsUntilBaf = nextBafLevel - level;
  const isBafLevel = level % 10 === 0;
  let prominence;
  if (isBafLevel)              prominence = 'high';
  else if (levelsUntilBaf <= 3) prominence = 'high';
  else if (levelsUntilBaf <= 7) prominence = 'medium';
  else                          prominence = 'low';
  return { nextBafLevel, levelsUntilBaf, isBafLevel, prominence };
}

const TEMPLATE = `
<section data-slot="baf" class="panel play-baf-panel" data-prominence="low">

  <div data-bind="skeleton">
    <div class="skeleton-header"><div class="skeleton-line skeleton-shimmer" style="width:40%"></div></div>
    <div class="skeleton-row"><div class="skeleton-line skeleton-shimmer" style="width:60%"></div></div>
    <div class="skeleton-row"><div class="skeleton-block skeleton-shimmer" style="height:100px"></div></div>
  </div>

  <div data-bind="content" hidden>
    <h2 class="panel-title">BAF Leaderboard</h2>

    <!-- BAF-03 context row + round-status pill -->
    <div class="play-baf-context">
      <span data-bind="next-baf-level">Next BAF: Level --</span>
      <span data-bind="levels-until">-- levels away</span>
      <span class="play-baf-round-status" data-bind="round-status" data-status="">--</span>
    </div>

    <!-- BAF-02 top-4 leaderboard -->
    <div class="play-baf-leaderboard">
      <div class="play-baf-header">
        <span>Rank</span><span>Player</span><span>Score</span>
      </div>
      <div class="play-baf-entries" data-bind="leaderboard-entries">
      </div>
    </div>

    <!-- BAF-01 your-rank row (hidden until INTEG-05 returns data in Wave 2) -->
    <div class="play-baf-your-rank" data-bind="your-rank" hidden>
      <span>You: rank </span>
      <span data-bind="your-rank-value">--</span>
      <span> of </span>
      <span data-bind="total-participants">--</span>
      <span> -- </span>
      <span data-bind="your-score">--</span>
    </div>
  </div>
</section>
`;

class BafPanel extends HTMLElement {
  #unsubs = [];
  #loaded = false;
  #bafFetchId = 0;
  #bafPlayerFetchId = 0;

  connectedCallback() {
    this.innerHTML = TEMPLATE;

    // Wave 1: BAF is level-scoped for the leaderboard; INTEG-05 per-player
    // refetches on both level AND player change.
    this.#unsubs.push(
      subscribe('replay.level',  () => { this.#refetchLeaderboard(); this.#refetchPlayer(); }),
      subscribe('replay.player', () => this.#refetchPlayer()),
    );

    // Kick initial fetches in case the store already has values.
    this.#refetchLeaderboard();
    this.#refetchPlayer();
  }

  disconnectedCallback() {
    this.#unsubs.forEach((fn) => fn());
    this.#unsubs = [];
  }

  // --- BAF-02 + BAF-03 partial fetch (leaderboard + context row) --------

  async #refetchLeaderboard() {
    const level = get('replay.level');
    const token = ++this.#bafFetchId;

    if (level == null) return;

    if (this.#loaded) {
      this.querySelector('[data-bind="content"]')?.classList.add('is-stale');
    }

    try {
      const res = await fetch(`${API_BASE}/leaderboards/baf?level=${encodeURIComponent(level)}`);
      if (token !== this.#bafFetchId) return;
      const data = res.ok ? await res.json() : null;
      if (token !== this.#bafFetchId) return;

      const entries = Array.isArray(data?.entries) ? data.entries : [];

      this.#renderContext(level);
      this.#renderLeaderboard(entries, get('replay.player'));
      this.#showContent();
      this.querySelector('[data-bind="content"]')?.classList.remove('is-stale');
    } catch (err) {
      if (token === this.#bafFetchId) {
        this.#renderContext(level);
        this.#renderLeaderboardError();
        this.#showContent();
        this.querySelector('[data-bind="content"]')?.classList.remove('is-stale');
      }
    }
  }

  // --- BAF-01 + BAF-03 roundStatus fetch (INTEG-05; 404-tolerant in Wave 1) -

  async #refetchPlayer() {
    const level = get('replay.level');
    const addr = get('replay.player');
    const token = ++this.#bafPlayerFetchId;

    if (level == null || !addr) return;

    try {
      const res = await fetch(`${API_BASE}/player/${addr}/baf?level=${encodeURIComponent(level)}`);
      if (token !== this.#bafPlayerFetchId) return;

      if (!res.ok) {
        // Wave 1: INTEG-05 not yet shipped in /home/zak/Dev/PurgeGame/database/.
        // Silent 404 tolerance. Wave 2 flips this to live rendering.
        // Fall back to inline roundStatus derivation for the pill.
        this.#renderRoundStatusFallback(level);
        return;
      }

      const data = await res.json();
      if (token !== this.#bafPlayerFetchId) return;

      this.#renderYourRank(data);
      this.#renderRoundStatus(data?.roundStatus);
    } catch (err) {
      if (token === this.#bafPlayerFetchId) {
        this.#renderRoundStatusFallback(level);
      }
    }
  }

  // --- Render helpers ---------------------------------------------------

  #bind(key) {
    return this.querySelector(`[data-bind="${key}"]`);
  }

  #renderContext(level) {
    const ctx = bafContext(level);

    this.setAttribute('data-prominence', ctx.prominence);

    const nextEl = this.#bind('next-baf-level');
    if (nextEl) {
      nextEl.textContent = ctx.isBafLevel
        ? `BAF Active! Level ${level}`
        : `Next BAF: Level ${ctx.nextBafLevel ?? '--'}`;
    }

    const levelsEl = this.#bind('levels-until');
    if (levelsEl) {
      if (ctx.isBafLevel)         levelsEl.textContent = 'This level';
      else if (ctx.levelsUntilBaf != null) levelsEl.textContent = `${ctx.levelsUntilBaf} levels away`;
      else                        levelsEl.textContent = '--';
    }
  }

  #renderLeaderboard(entries, selectedAddr) {
    const container = this.#bind('leaderboard-entries');
    if (!container) return;

    container.textContent = '';

    if (entries.length === 0) {
      const empty = document.createElement('div');
      empty.className = 'play-baf-empty';
      const level = get('replay.level');
      empty.textContent = (level != null && level % 10 !== 0)
        ? 'BAF active only at every 10th level.'
        : 'No BAF activity at this level yet.';
      container.appendChild(empty);
      return;
    }

    const lowerSelected = selectedAddr ? selectedAddr.toLowerCase() : null;

    for (const entry of entries) {
      const row = document.createElement('div');
      row.className = 'play-baf-entry';
      row.setAttribute('data-rank', String(entry.rank));

      if (lowerSelected && entry.player && entry.player.toLowerCase() === lowerSelected) {
        row.setAttribute('aria-current', 'true');
      }

      const rankCell = document.createElement('span');
      rankCell.textContent = `#${entry.rank}`;

      const playerCell = document.createElement('span');
      playerCell.className = 'play-baf-player';
      playerCell.textContent = truncateAddress(entry.player);

      // Score-unit discipline: BAF scores are WEI-scale BURNIE
      // (live sample level 20 rank 1 = "475215212469240581904067" = 475,215 BURNIE).
      // MUST formatBurnie; raw string would show ~475 sextillion in UI.
      const scoreCell = document.createElement('span');
      scoreCell.textContent = `${formatBurnie(entry.score)} BURNIE`;

      row.appendChild(rankCell);
      row.appendChild(playerCell);
      row.appendChild(scoreCell);
      container.appendChild(row);
    }
  }

  #renderLeaderboardError() {
    const container = this.#bind('leaderboard-entries');
    if (container) {
      container.textContent = '';
      const err = document.createElement('div');
      err.className = 'play-baf-empty';
      err.textContent = 'BAF leaderboard unavailable.';
      container.appendChild(err);
    }
  }

  #renderYourRank(data) {
    // Wave 1: data is null from the 404 branch; Wave 2 will populate.
    // Even in Wave 1 we handle the live-data case defensively in case the
    // endpoint ships early.
    const row = this.#bind('your-rank');
    if (!row) return;

    if (!data || data.rank == null) {
      row.hidden = true;
      return;
    }

    row.hidden = false;

    const rankEl = this.#bind('your-rank-value');
    if (rankEl) rankEl.textContent = String(data.rank);

    const totalEl = this.#bind('total-participants');
    if (totalEl) totalEl.textContent = String(data.totalParticipants ?? '--');

    // Score-unit: INTEG-05 score is WEI-scale -> formatBurnie.
    const scoreEl = this.#bind('your-score');
    if (scoreEl) {
      scoreEl.textContent = (data.score && data.score !== '0')
        ? `${formatBurnie(data.score)} BURNIE`
        : '0 BURNIE';
    }
  }

  #renderRoundStatus(status) {
    const pill = this.#bind('round-status');
    if (!pill) return;

    const LABELS = {
      open: 'OPEN',
      closed: 'CLOSED',
      skipped: 'SKIPPED',
      not_eligible: 'NOT ELIGIBLE',
    };

    pill.setAttribute('data-status', status ?? '');
    pill.textContent = LABELS[status] ?? '--';
  }

  #renderRoundStatusFallback(level) {
    // Wave 1 fallback when INTEG-05 404s. Correct in the simple case
    // (level % 10 !== 0 is definitively "not_eligible"; otherwise assume "open").
    // Wave 2 replaces with authoritative INTEG-05 response including
    // "skipped" and "closed" states.
    const status = (level != null && level % 10 === 0) ? 'open' : 'not_eligible';
    this.#renderRoundStatus(status);
  }

  #showContent() {
    const skeleton = this.querySelector('[data-bind="skeleton"]');
    const content = this.querySelector('[data-bind="content"]');
    if (skeleton) skeleton.hidden = true;
    if (content) content.hidden = false;
    this.#loaded = true;
  }
}

customElements.define('baf-panel', BafPanel);
```

## Self-check after write

- File replaces the 40-LOC Phase 50 stub with ~240-280 LOC.
- `customElements.define('baf-panel', BafPanel);` is the final line.
- All test assertions from play-baf-panel.test.js turn green.
- SHELL-01 guardrail continues to pass (the new Wave 0 FORBIDDEN entries are honored -- no imports from beta/app/baf.js, beta/app/coinflip.js, or beta/components/baf-panel.js).
  </action>
  <verify>
    <automated>node --check play/components/baf-panel.js && node --test play/app/__tests__/play-baf-panel.test.js && node --test play/app/__tests__/play-coinflip-panel.test.js && node --test play/app/__tests__/play-shell-01.test.js && node --test play/app/__tests__/play-profile-panel.test.js && node --test play/app/__tests__/play-tickets-panel.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check play/components/baf-panel.js` exits 0
    - `wc -l play/components/baf-panel.js` reports >= 240
    - `grep -c "customElements\.define('baf-panel'" play/components/baf-panel.js` returns 1
    - `grep -c "#bafFetchId" play/components/baf-panel.js` returns >= 3 (declaration + checkpoints)
    - `grep -c "#bafPlayerFetchId" play/components/baf-panel.js` returns >= 3
    - `grep -c "is-stale" play/components/baf-panel.js` returns >= 2
    - `grep -c "formatBurnie" play/components/baf-panel.js` returns >= 2
    - `grep -c "truncateAddress" play/components/baf-panel.js` returns >= 1
    - `grep -cE "Math\\.ceil\\(\\s*\\(\\s*level\\s*\\+\\s*1" play/components/baf-panel.js` returns 1 (inline bafContext)
    - `grep -c "data-bind=\"round-status\"" play/components/baf-panel.js` returns 1
    - `grep -c "data-bind=\"next-baf-level\"" play/components/baf-panel.js` returns 1
    - `grep -c "data-bind=\"levels-until\"" play/components/baf-panel.js` returns 1
    - `grep -c "data-bind=\"your-rank\"" play/components/baf-panel.js` returns 1
    - `grep -c "data-bind=\"your-rank-value\"" play/components/baf-panel.js` returns 1
    - `grep -c "data-bind=\"total-participants\"" play/components/baf-panel.js` returns 1
    - `grep -c "data-bind=\"your-score\"" play/components/baf-panel.js` returns 1
    - `grep -c "data-bind=\"leaderboard-entries\"" play/components/baf-panel.js` returns 1
    - `grep -c "data-prominence" play/components/baf-panel.js` returns >= 2 (TEMPLATE attr + setAttribute call)
    - `grep -c "data-rank" play/components/baf-panel.js` returns >= 1 (row setAttribute)
    - `grep -c "data-status" play/components/baf-panel.js` returns >= 1
    - `grep -c "/leaderboards/baf?level=" play/components/baf-panel.js` returns 1
    - `grep -cE "/player/.*?/baf\\?level=" play/components/baf-panel.js` returns 1
    - `grep -c "aria-current" play/components/baf-panel.js` returns >= 1
    - `! grep -q "from ['\"][^'\"]*\\/beta\\/app\\/baf\\.js['\"]" play/components/baf-panel.js` exits 0
    - `! grep -q "from ['\"][^'\"]*\\/beta\\/app\\/utils\\.js['\"]" play/components/baf-panel.js` exits 0
    - `! grep -q "from ['\"][^'\"]*\\/beta\\/components\\/baf-panel\\.js['\"]" play/components/baf-panel.js` exits 0
    - `! grep -q "from ['\"]ethers['\"]" play/components/baf-panel.js` exits 0
    - `node --test play/app/__tests__/play-baf-panel.test.js` exits 0 (all Wave 0 assertions green)
    - `node --test play/app/__tests__/play-coinflip-panel.test.js` exits 0 (Task 1 still green)
    - `node --test play/app/__tests__/play-shell-01.test.js` exits 0 (recursive scan still green; the 3 new FORBIDDEN entries don't trip)
    - `node --test play/app/__tests__/play-profile-panel.test.js` exits 0
    - `node --test play/app/__tests__/play-tickets-panel.test.js` exits 0
  </acceptance_criteria>
  <done>
    `play/components/baf-panel.js` is a 240-280 LOC hydrated panel replacing the 40-LOC Phase 50 stub. It ships BAF-02 (top-4 leaderboard from /leaderboards/baf?level=), BAF-03 core (next-baf-level + levels-until from inline bafContext; round-status pill with fallback derivation for Wave 1), and pre-wires BAF-01 (your-rank row with INTEG-05 fetch that 404s silently until Wave 2 ships the endpoint). Dual stale-guard counters (#bafFetchId + #bafPlayerFetchId) invalidate independently. data-rank attribute on each row enables D-06 tier styling via CSS. data-prominence attribute on the panel element reflects approach-based prominence. All Wave 0 test assertions turn green. Score-unit discipline honored: BAF scores via formatBurnie (wei-scale). SHELL-01 recursive guardrail holds with the 3 new FORBIDDEN entries honored.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append ~130 LOC of Phase 54 CSS to play/styles/play.css</name>
  <files>play/styles/play.css</files>
  <read_first>
    - play/styles/play.css FULL FILE (to find the append point at the end of the existing file)
    - 54-RESEARCH.md Section 9 lines 662-878 (the exact CSS to append -- COPY VERBATIM)
    - 54-CONTEXT.md D-06 (BAF tier colors: rank 1 gold, rank 2 silver, rank 3 bronze, rank 4 regular)
    - 54-PATTERNS.md play.css section (lines 816-1140; confirms append-pattern)
    - beta/styles/baf.css (reference for panel-level data-prominence rules -- already extracted in RESEARCH Section 9)
    - beta/styles/coinflip.css lines 166-169 (armed-pulse @keyframes -- 54-RESEARCH.md Section 9 line 882 flags that this keyframe MUST be copied into play.css since play/ does NOT link beta/styles/coinflip.css)
  </read_first>
  <action>
Use the Edit tool to APPEND (not replace) ~130 LOC at the end of `play/styles/play.css`. The CSS is verbatim from 54-RESEARCH.md Section 9 lines 662-878.

Locate the end of the current file (likely a closing `}` for the last existing rule). Append this block after it (insert a blank line + the Phase 54 section header comment + the rules):

```css

/* ========================================================================
 * Phase 54 additions -- Coinflip & BAF Leaderboards
 * ======================================================================== */

/* --- BAF panel (Phase 54) --- */

/* Panel-level prominence -- approach-based not rank-based.
   bafContext() in baf-panel.js maps level->prominence (high/medium/low). */
.play-baf-panel {
  transition: border-color 0.3s ease;
}
.play-baf-panel[data-prominence="high"] {
  border-color: var(--accent-primary);
  border-width: 2px;
}
.play-baf-panel[data-prominence="medium"] {
  border-color: var(--text-secondary);
}
.play-baf-panel[data-prominence="low"] {
  opacity: 0.8;
}

/* Context row -- next-baf-level + levels-until + round-status pill */
.play-baf-context {
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-size: 0.85rem;
  color: var(--text-secondary);
  margin-bottom: 0.75rem;
  gap: 0.5rem;
}

/* Leaderboard grid */
.play-baf-leaderboard {
  border: 1px solid var(--border-color);
  border-radius: 6px;
  overflow: hidden;
}
.play-baf-header {
  display: grid;
  grid-template-columns: 50px 1fr 140px;
  padding: 0.5rem 0.75rem;
  background: var(--bg-secondary);
  font-size: 0.75rem;
  text-transform: uppercase;
  color: var(--text-dim);
  font-weight: 600;
}
.play-baf-entry {
  display: grid;
  grid-template-columns: 50px 1fr 140px;
  padding: 0.5rem 0.75rem;
  border-top: 1px solid var(--border-color);
  font-size: 0.85rem;
}

/* --- CONTEXT D-06 tier colors (NEW relative to beta) --- */
/* Rank-tier styling by data-rank attribute on each .play-baf-entry */
.play-baf-entry[data-rank="1"] {
  color: #FFD700;              /* gold */
  font-weight: 700;
  font-size: 0.95rem;
}
.play-baf-entry[data-rank="2"] {
  color: #C0C0C0;              /* silver */
  font-weight: 600;
}
.play-baf-entry[data-rank="3"] {
  color: #CD7F32;              /* bronze */
  font-weight: 600;
}
.play-baf-entry[data-rank="4"] {
  color: var(--text-primary);
  font-weight: 500;
}

/* Selected-player highlight in any row */
.play-baf-entry[aria-current="true"] {
  background: var(--bg-tertiary);
  text-decoration: underline;
}

/* Non-top-4 player row below leaderboard (INTEG-05 result; Wave 2 un-hides) */
.play-baf-your-rank {
  margin-top: 0.75rem;
  padding: 0.5rem 0.75rem;
  background: var(--bg-secondary);
  border-radius: 6px;
  border: 1px solid var(--accent-primary);
  font-size: 0.85rem;
}

/* Round status pill */
.play-baf-round-status {
  font-size: 0.72rem;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  padding: 0.15rem 0.5rem;
  border-radius: 4px;
  white-space: nowrap;
}
.play-baf-round-status[data-status="open"] {
  background: rgba(34, 197, 94, 0.15);
  color: var(--success, #22c55e);
}
.play-baf-round-status[data-status="closed"] {
  background: rgba(100, 100, 100, 0.2);
  color: var(--text-dim);
}
.play-baf-round-status[data-status="skipped"] {
  background: rgba(239, 68, 68, 0.15);
  color: var(--error, #ef4444);
}
.play-baf-round-status[data-status="not_eligible"] {
  background: rgba(100, 100, 100, 0.1);
  color: var(--text-dim);
}

/* Monospace player addresses */
.play-baf-player {
  font-family: monospace;
  font-size: 0.8rem;
}

/* Empty state */
.play-baf-empty {
  padding: 1rem;
  text-align: center;
  color: var(--text-dim);
  font-size: 0.85rem;
}

/* Keep-old-data-dim inherits Phase 51 pattern */
.play-baf-panel [data-bind="content"].is-stale {
  opacity: 0.6;
  pointer-events: none;
}

/* --- Coinflip panel (Phase 54) --- */

/* Bounty header with armed/not-armed pill */
.play-coinflip-bounty {
  padding: 0.75rem;
  border: 1px solid var(--border-color);
  border-radius: 6px;
  margin-bottom: 1rem;
  background: var(--bg-secondary);
}
.play-coinflip-section-header {
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--text-dim);
  margin-bottom: 0.5rem;
  font-weight: 600;
}
.play-coinflip-bounty-armed {
  display: inline-block;
  font-size: 0.72rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  padding: 0.2rem 0.6rem;
  border-radius: 4px;
  background: rgba(100, 100, 100, 0.2);
  color: var(--text-dim);
  margin-bottom: 0.5rem;
}
.play-coinflip-bounty-armed[data-armed="true"] {
  background: rgba(239, 68, 68, 0.15);
  color: var(--error, #ef4444);
  animation: armed-pulse 2s ease-in-out infinite;
}

/* armed-pulse keyframe -- copied from beta/styles/coinflip.css lines 166-169
   because play/ does NOT link beta/styles/coinflip.css. Keeps play/ self-contained. */
@keyframes armed-pulse {
  0%, 100% { opacity: 1; }
  50%      { opacity: 0.5; }
}

/* Player state + leaderboard row layout (shared grid) */
.play-coinflip-state,
.play-coinflip-leaderboard {
  margin-top: 0.5rem;
}
.play-coinflip-leaderboard {
  border: 1px solid var(--border-color);
  border-radius: 6px;
  overflow: hidden;
}
.play-coinflip-header,
.play-coinflip-entry {
  display: grid;
  grid-template-columns: 50px 1fr 140px;
  padding: 0.5rem 0.75rem;
  font-size: 0.85rem;
}
.play-coinflip-header {
  background: var(--bg-secondary);
  font-size: 0.75rem;
  text-transform: uppercase;
  color: var(--text-dim);
  font-weight: 600;
}
.play-coinflip-entry {
  border-top: 1px solid var(--border-color);
}
.play-coinflip-entry[aria-current="true"] {
  background: var(--bg-tertiary);
  font-weight: 600;
  text-decoration: underline;
}
.play-coinflip-player {
  font-family: monospace;
  font-size: 0.8rem;
}
.play-coinflip-state-row {
  display: flex;
  justify-content: space-between;
  padding: 0.25rem 0;
  font-size: 0.85rem;
}
.play-coinflip-empty {
  padding: 1rem;
  text-align: center;
  color: var(--text-dim);
  font-size: 0.85rem;
}

/* Keep-old-data-dim for coinflip panel */
.play-coinflip-panel [data-bind="content"].is-stale {
  opacity: 0.6;
  pointer-events: none;
}
```

## Self-check after append

- Line count grows by ~130 (end of file).
- No pre-existing rules modified -- this is pure append.
- CSS is valid (opens + closes braces balance).
- No em dashes; no emojis.
- data-rank tier colors present for rank 1-4.
- armed-pulse keyframe defined.
- data-prominence rules for high/medium/low.
- data-status rules for open/closed/skipped/not_eligible.
- is-stale dim class for both panels.
  </action>
  <verify>
    <automated>test -f play/styles/play.css && grep -q "Phase 54 additions" play/styles/play.css && grep -q "play-baf-panel\\[data-prominence=\"high\"\\]" play/styles/play.css && grep -q "data-rank=\"1\"" play/styles/play.css && grep -q "data-status=\"open\"" play/styles/play.css && grep -q "@keyframes armed-pulse" play/styles/play.css && grep -q "play-coinflip-panel \\[data-bind=\"content\"\\].is-stale" play/styles/play.css && grep -q "play-baf-panel \\[data-bind=\"content\"\\].is-stale" play/styles/play.css</automated>
  </verify>
  <acceptance_criteria>
    - `test -f play/styles/play.css` exits 0
    - `grep -q "Phase 54 additions" play/styles/play.css` exits 0 (section header marker)
    - `grep -q "play-baf-panel\[data-prominence=\"high\"\]" play/styles/play.css` exits 0
    - `grep -q "play-baf-panel\[data-prominence=\"medium\"\]" play/styles/play.css` exits 0
    - `grep -q "play-baf-panel\[data-prominence=\"low\"\]" play/styles/play.css` exits 0
    - `grep -q "data-rank=\"1\"" play/styles/play.css` exits 0
    - `grep -q "data-rank=\"2\"" play/styles/play.css` exits 0
    - `grep -q "data-rank=\"3\"" play/styles/play.css` exits 0
    - `grep -q "data-rank=\"4\"" play/styles/play.css` exits 0
    - `grep -q "#FFD700" play/styles/play.css` exits 0 (gold tier)
    - `grep -q "#C0C0C0" play/styles/play.css` exits 0 (silver tier)
    - `grep -q "#CD7F32" play/styles/play.css` exits 0 (bronze tier)
    - `grep -q "data-status=\"open\"" play/styles/play.css` exits 0
    - `grep -q "data-status=\"closed\"" play/styles/play.css` exits 0
    - `grep -q "data-status=\"skipped\"" play/styles/play.css` exits 0
    - `grep -q "data-status=\"not_eligible\"" play/styles/play.css` exits 0
    - `grep -q "@keyframes armed-pulse" play/styles/play.css` exits 0
    - `grep -q "play-coinflip-panel \[data-bind=\"content\"\].is-stale" play/styles/play.css` exits 0
    - `grep -q "play-baf-panel \[data-bind=\"content\"\].is-stale" play/styles/play.css` exits 0
    - `grep -q "play-baf-entry\[aria-current" play/styles/play.css` exits 0
    - `grep -q "play-coinflip-entry\[aria-current" play/styles/play.css` exits 0
    - `! grep -q "—" play/styles/play.css` exits 0 (no em dashes)
    - The file remains valid CSS (braces balance). Quick heuristic: the net count of `{` and `}` occurrences must be equal. Verify with: `test "$(grep -o '{' play/styles/play.css | wc -l)" = "$(grep -o '}' play/styles/play.css | wc -l)"`
  </acceptance_criteria>
  <done>
    `play/styles/play.css` has ~130 LOC appended covering the Phase 54 CSS surface: BAF panel-level data-prominence borders (high/medium/low), BAF leaderboard grid with rank-tier colors via data-rank attribute (gold #FFD700 / silver #C0C0C0 / bronze #CD7F32 / regular), BAF your-rank row styling, BAF round-status pill with 4 data-status states, coinflip bounty header + armed-pulse keyframe, coinflip leaderboard grid, selected-player highlight via aria-current, is-stale dim class for both panels, and all empty-state + monospace-player rules. The additions are pure append -- no existing rules modified. Brace count remains balanced. No em dashes. Rendered in the browser, rank 1 shows gold + bold + slightly larger; ranks 2-3 show silver/bronze; ranks outside top-4 show regular. The panel border pulses to accent color when the game is within 3 levels of the next BAF boundary.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /leaderboards/coinflip + /leaderboards/baf responses -> panel renderers | Both endpoints return untrusted string data (player addresses + scores). Renderers use textContent (never innerHTML interpolation) to prevent XSS from a compromised backend. |
| /player/:address?day= coinflip block -> coinflip-panel state section | Same rule: all amounts rendered via textContent with formatBurnie conversion. biggestFlipPlayer may be null; guard via null-check to prevent "null" literal in UI. |
| /player/:address/baf?level= (INTEG-05) response -> baf-panel your-rank row | Wave 1 fetch tolerates 404 silently; Wave 2 parses. The 404 path uses the inline roundStatus fallback derivation (level % 10 heuristic) which is strictly conservative -- cannot claim "closed" or "skipped" without authoritative data. |
| state.replay.{day, level, player} -> panel fetch parameters | Both panels validate presence (early-return when null) and encodeURIComponent on URL embedding. Malformed store values no-op the fetch. |
| CSS data-rank attribute values -> tier styling | data-rank is written by the renderer as `String(entry.rank)`; entry.rank is validated as integer by the backend schema. Unknown rank values (>4 or 0) get no CSS selector match, defaulting to base row styling (graceful degradation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-10 | Tampering | Rapid day/level scrubbing races late responses into current render | mitigate | Both panels implement multi-checkpoint stale-guard. Coinflip: 3 token checks (post-Promise.all, post-playerRes.json, post-lbRes.json). BAF: 2 checks on each of the two fetches. Pattern verified by Phase 51 profile-panel at scale. |
| T-54-11 | Tampering | BAF per-player fetch (INTEG-05) ships late responses | mitigate | #bafPlayerFetchId counter invalidates independently from #bafFetchId so a slow per-player response cannot pollute a freshly-refetched leaderboard. |
| T-54-12 | Spoofing | Compromised backend returns HTML-shaped player addresses to leaderboard | mitigate | Renderers use document.createElement + textContent exclusively. Never innerHTML with response strings. truncateAddress output is pure hex digits + dots. |
| T-54-13 | Information disclosure | Error paths log response status to console | accept | Console warnings are dev-mode debug; no PII leaked. DOM fallback text ("BAF leaderboard unavailable.") is generic. |
| T-54-14 | Denial of Service | Browser executes repeated fetch calls during rapid scrub | accept | Each panel's refetch is lightweight (2 endpoints for coinflip; 1 leaderboard + 1 INTEG-05 for BAF). No in-flight promise dedup needed per 54-RESEARCH Section 10 (different endpoints, no overlap). Network stack handles parallel in-flights fine. |
| T-54-15 | Elevation of Privilege | Wave 1 panel accidentally imports beta/app/baf.js via auto-complete | mitigate | play-shell-01.test.js FORBIDDEN array has 3 new Wave-0-added entries (beta/components/baf-panel.js, beta/app/coinflip.js, beta/app/baf.js) so any accidental import in Wave 1 fails CI immediately. Recursive scan catches it before commit. |
| T-54-16 | Repudiation | D-06 prominence styling uses hex literals (not CSS custom properties) | accept | Per 54-RESEARCH Q5 recommendation: hex literals keep Phase 54 self-contained; future design-system phase can refactor to custom properties. Decision logged in SUMMARY. |
| T-54-17 | Tampering | Score-unit mismatch bug (Pitfall 8) renders coinflip score as 10^-14 BURNIE | mitigate | Three separate defenses: (a) comment block in coinflip-panel.js source referencing Pitfall 8 and the scale observation; (b) String(entry.score) literal in render helper (not formatBurnie); (c) Wave 0 test file includes a score-unit assertion block on formatBurnie + truncateAddress presence. |
| T-54-18 | Tampering | BAF score displayed as raw 24-digit wei number instead of formatted BURNIE | mitigate | formatBurnie(entry.score) in baf-panel #renderLeaderboard; same comment + test patterns as coinflip but pointing the other direction. |
| T-54-19 | Spoofing | INTEG-05 roundStatus spoofs enum with unknown value ("maybe_open") | mitigate | Render helper maps through LABELS object; unknown status falls to '--' display with empty data-status attribute (no CSS match = base styling). Pill fails safe. |
</threat_model>

<verification>
- Task 1 produces a 220-260 LOC play/components/coinflip-panel.js with all COINFLIP-01/02/03 markup + fetch wiring + stale-guard + score-unit discipline.
- Task 2 produces a 240-280 LOC play/components/baf-panel.js with BAF-02 + BAF-03 core + BAF-01 pre-wiring + dual stale-guards + inline bafContext.
- Task 3 appends ~130 LOC to play/styles/play.css covering prominence + rank tier colors + round-status pill + armed-pulse keyframe + is-stale class for both panels.
- Wave 0 tests transition from RED to GREEN (~58 assertions total pass); SHELL-01 guardrail stays green.
- Phase 50/51/52/53 tests continue to pass (no regression).
- The play/ route renders both panels correctly when loaded in a browser against the live API (verified in Wave 3 UAT if not deferred).
- INTEG-05 fetch in baf-panel is 404-tolerant: the network tab shows a 404 but no console error; the your-rank row stays hidden; the round-status pill falls back to the level-arithmetic derivation.
</verification>

<success_criteria>
- [ ] `play/components/coinflip-panel.js` is 220+ LOC with COINFLIP-01/02/03 fully wired (data-bind markup + /player/:addr?day= + /leaderboards/coinflip?day= fetches + #coinflipFetchId stale-guard + formatBurnie on wei-scale amounts + String(entry.score) on integer-scale leaderboard + truncateAddress + aria-current highlight + empty state)
- [ ] `play/components/baf-panel.js` is 240+ LOC with BAF-02 + BAF-03 core + BAF-01 pre-wired (data-bind markup including your-rank + data-rank per row + data-status pill + data-prominence panel attribute + /leaderboards/baf?level= + /player/:addr/baf?level= fetches + #bafFetchId + #bafPlayerFetchId + inline bafContext Math.ceil derivation + formatBurnie + truncateAddress + aria-current + empty state + Wave 1 404-tolerant INTEG-05 stub)
- [ ] `play/styles/play.css` grows by ~130 LOC with all Phase 54 CSS rules including rank-tier colors (gold/silver/bronze/regular), panel-level data-prominence borders, round-status pill (open/closed/skipped/not_eligible), armed-pulse keyframe, is-stale dim class for both panels
- [ ] All Wave 0 test assertions in play-coinflip-panel.test.js (~28) and play-baf-panel.test.js (~30) turn GREEN
- [ ] play-shell-01.test.js continues to PASS (recursive scan of play/ tree catches zero forbidden imports; the 3 new Wave 0 FORBIDDEN entries honored)
- [ ] Phase 50/51/52/53 tests continue to PASS (no regression)
- [ ] No new files created (no play/app/leaderboards-fetch.js per RESEARCH Section 10 planner-decision default)
- [ ] No play/index.html changes (Phase 50 customElements registrations still valid)
- [ ] No play/app/main.js changes (both panels are subscribe-driven; no bootstrap work needed)
- [ ] COINFLIP-01, COINFLIP-02, COINFLIP-03 are fully validated (all three ship in Wave 1 because the underlying endpoints were already shipped)
- [ ] BAF-02 is fully validated (top-4 leaderboard with prominence styling)
- [ ] BAF-03 is PARTIAL validated (level label + levels-until + inline fallback round-status pill ship in Wave 1; authoritative roundStatus from INTEG-05 lands in Wave 2)
- [ ] BAF-01 is PRE-WIRED (fetch path emitted + hidden your-rank row markup present + render helper ready; live rendering enabled in Wave 2 when INTEG-05 ships)
</success_criteria>

<output>
After completion, create `.planning/phases/54-coinflip-baf/54-02-SUMMARY.md` per the standard template, including:
- Wave 1 deliverables: coinflip-panel.js (LOC delta: 40 -> ~240), baf-panel.js (40 -> ~260), play.css (+~130 LOC)
- Requirement coverage:
  - COINFLIP-01/02/03 FULLY SHIPPED (using existing endpoints)
  - BAF-02 FULLY SHIPPED
  - BAF-03 PARTIAL (core UI + fallback; authoritative source in Wave 2)
  - BAF-01 PRE-WIRED (render path ready; 404-tolerant until Wave 2)
- Test state: Wave 0 ~58 RED assertions transition to GREEN; no regressions in 230+ pre-existing tests
- Score-unit discipline (Pitfall 8): documented in each render helper with inline comment; String(entry.score) for coinflip leaderboard; formatBurnie for everything else
- No optional play/app/leaderboards-fetch.js helper shipped (planner-decision SKIP per RESEARCH Section 10)
- Next action: `/gsd-execute-phase 54` Wave 2 (Plan 54-03-PLAN.md) -- hard-gated on INTEG-05 delivery to database/main
</output>

## Plan Summary

**Objective:** Wave 1 evolves both Phase 50 stubs into functional panels using existing endpoints; pre-wires the INTEG-05-dependent BAF-01 path as a 404-tolerant stub for Wave 2 to flip live.

**Tasks:**
1. Hydrate coinflip-panel.js (40 -> ~240 LOC) with bounty + state + leaderboard sections reading from existing /player/:addr?day= coinflip block + /leaderboards/coinflip?day=
2. Hydrate baf-panel.js (40 -> ~260 LOC) with context + leaderboard (top-4, data-rank styling) + hidden your-rank row; dual stale-guards; inline bafContext; 404-tolerant INTEG-05 fetch
3. Append ~130 LOC to play.css: rank-tier colors, panel-level prominence borders, round-status pill, armed-pulse keyframe, is-stale for both panels

**Verify chain:**
- node --check on both component files
- node --test on both Wave 0 test files (RED to GREEN transition)
- node --test on play-shell-01.test.js (guardrail stays green with 3 new FORBIDDEN entries honored)
- node --test on all prior play/ test files (no regression)
- grep audits on specific data-bind strings, fetch URLs, stale-guard identifiers, hex colors, keyframe

**Context cost estimate:** ~45% for this plan. Task 1 (coinflip): ~15% -- Custom Element rewrite with parallel fetch + 3 render helpers. Task 2 (baf): ~18% -- similar scope plus dual fetch-id counters + inline bafContext + 404 tolerance. Task 3 (CSS): ~7% -- verbatim append of CSS lines. Plus ~5% for test re-runs between tasks.

**Duration:** Task 1 runs in 3-4 minutes (file Write + test run). Task 2 runs in 4-5 minutes (larger + more careful). Task 3 runs in 1-2 minutes (append + verify). Full plan expected to complete in 10-12 minutes of execution time plus test-feedback loops.
