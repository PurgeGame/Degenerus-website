---
phase: 54
plan: 01
slug: wave0-spec-tests-and-shell01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md
  - play/app/__tests__/play-coinflip-panel.test.js
  - play/app/__tests__/play-baf-panel.test.js
  - play/app/__tests__/play-shell-01.test.js
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - COINFLIP-01
  - COINFLIP-02
  - COINFLIP-03
  - BAF-01
  - BAF-02
  - BAF-03
  - INTEG-05
  - INTEG-04
tags:
  - phase-54
  - wave-0
  - tests
  - nyquist
  - shell-01
  - integ-05
  - integ-04-deferral

must_haves:
  truths:
    - "Every Phase 54 requirement (COINFLIP-01..03, BAF-01..03) has at least one failing automated test before Wave 1 code lands"
    - "INTEG-05-SPEC.md exists inside the Phase 54 phase directory with full endpoint contract (path/query/response/roundStatus derivation/acceptance criteria) authored verbatim from 54-RESEARCH.md Section 8 lines 331-555"
    - "play-shell-01.test.js FORBIDDEN array gains 3 new entries (beta/components/baf-panel.js, beta/app/coinflip.js, beta/app/baf.js) preventing accidental import of transitively wallet-tainted beta files into play/ tree in future waves"
    - "REQUIREMENTS.md marks INTEG-04 as deferred per Phase 54 D-10 with pointer to ROADMAP success criterion 5 allowing formal deferral; INTEG-05 status stays [ ] pending Wave 2 delivery"
    - "All Wave 0 tests (play-coinflip-panel.test.js, play-baf-panel.test.js) currently FAIL because the panel production files exist only as Phase 50 stubs (40 LOC each) that do NOT contain the Wave 1/2 markup + fetch wiring the tests grep for"
    - "play-shell-01.test.js PASSES after the FORBIDDEN array edit because no play/ file currently imports the three new forbidden paths (recursive scan stays green)"
    - "All existing play/ tests stay green (no regression in Phase 50/51/52/53 test suites)"
    - "Score-unit discipline (Pitfall 8) is encoded as a distinct assertion block in each test file so Wave 1 cannot accidentally render coinflip integer scores as wei-formatted BURNIE or vice versa"
  artifacts:
    - path: ".planning/phases/54-coinflip-baf/INTEG-05-SPEC.md"
      provides: "Database-repo contract spec for GET /player/:address/baf?level=N (path, query, response schema, roundStatus derivation, proposed backend implementation, acceptance criteria, timeline, open questions)"
      min_lines: 180
      contains: "roundStatus"
    - path: "play/app/__tests__/play-coinflip-panel.test.js"
      provides: "COINFLIP-01..03 contract-grep tests covering bounty + state + leaderboard data-bind markup, fetch URLs (/player/:addr?day= + /leaderboards/coinflip?day=), #coinflipFetchId stale-guard, is-stale class, aria-current highlighting, SHELL-01 negative assertions, score-unit discipline, empty-state handling"
      min_lines: 130
      contains: "data-bind=\"deposited\""
    - path: "play/app/__tests__/play-baf-panel.test.js"
      provides: "BAF-01..03 contract-grep tests covering leaderboard + round-status + your-rank data-bind markup, fetch URLs (/leaderboards/baf?level= + /player/:addr/baf?level=), dual stale-guards (#bafFetchId + #bafPlayerFetchId), prominence via data-rank, round-status via data-status, SHELL-01 negative assertions, score-unit discipline, inline bafContext derivation"
      min_lines: 140
      contains: "data-rank"
    - path: "play/app/__tests__/play-shell-01.test.js"
      provides: "Extended FORBIDDEN array with 3 new entries: beta/components/baf-panel.js (tag-name collision + transitively tainted), beta/app/coinflip.js (ethers at line 4), beta/app/baf.js (transitively tainted via utils.js)"
      min_lines: 110
      contains: "beta/app/coinflip.js"
    - path: ".planning/REQUIREMENTS.md"
      provides: "INTEG-04 status changed from [ ] to [~] with deferral note pointing to Phase 54 D-10 + ROADMAP success criterion 5; Traceability table row 163 updated; footer updated"
      min_lines: 180
  key_links:
    - from: "play/app/__tests__/play-coinflip-panel.test.js"
      to: "play/components/coinflip-panel.js (Wave 1 + Wave 2 deliverable)"
      via: "readFileSync + regex"
      pattern: "readFileSync\\(.*play/components/coinflip-panel\\.js"
    - from: "play/app/__tests__/play-baf-panel.test.js"
      to: "play/components/baf-panel.js (Wave 1 + Wave 2 deliverable)"
      via: "readFileSync + regex"
      pattern: "readFileSync\\(.*play/components/baf-panel\\.js"
    - from: "play/app/__tests__/play-shell-01.test.js"
      to: "beta/components/baf-panel.js, beta/app/coinflip.js, beta/app/baf.js (forbidden paths)"
      via: "recursive scan of play/ tree for these import strings"
      pattern: "beta/components/baf-panel\\.js|beta/app/coinflip\\.js|beta/app/baf\\.js"
    - from: ".planning/phases/54-coinflip-baf/INTEG-05-SPEC.md"
      to: "/home/zak/Dev/PurgeGame/database/src/api/routes/player.ts"
      via: "endpoint contract referenced during Wave 2 side-quest (3 atomic commits on database/main mirroring INTEG-01 + INTEG-02 shape)"
      pattern: "database/src/api/routes/player\\.ts"
---

<objective>
Wave 0 establishes the Phase 54 hard gate on backend delivery and the Nyquist test harness before any production code lands.

Purpose: Phase 54's BAF-01 + BAF-03 hydration is HARD-GATED on the database repo shipping a new `GET /player/:address/baf?level=N` endpoint (INTEG-05). Wave 0's job is to (a) write the INTEG-05 spec that locks the backend contract, (b) author two failing test files covering COINFLIP-01..03 + BAF-01..03 that Waves 1 and 2 turn progressively green, (c) extend play-shell-01.test.js FORBIDDEN array to cover the three newly-identified transitively-wallet-tainted beta files (beta/components/baf-panel.js, beta/app/coinflip.js, beta/app/baf.js), and (d) mark INTEG-04 as deferred per D-10 with a pointer to ROADMAP success criterion 5 which explicitly permits this deferral. No production code is written this wave.

Output:
- `.planning/phases/54-coinflip-baf/INTEG-05-SPEC.md` with the full contract proposal (copied verbatim from 54-RESEARCH.md Section 8 lines 331-555).
- `play/app/__tests__/play-coinflip-panel.test.js` -- ~28 contract-grep tests covering COINFLIP-01..03 + SHELL-01 negatives + stale-guard + aria-current + empty state + score-unit discipline + fetch URL shape.
- `play/app/__tests__/play-baf-panel.test.js` -- ~30 contract-grep tests covering BAF-01..03 + SHELL-01 negatives + prominence (data-rank) + round-status (data-status) + dual stale-guards + inline bafContext derivation + score-unit discipline.
- `play/app/__tests__/play-shell-01.test.js` -- FORBIDDEN array gains 3 new entries; all other logic unchanged.
- `.planning/REQUIREMENTS.md` -- INTEG-04 row flipped from [ ] to [~] with deferral annotation; Traceability table row updated; footer timestamp updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/54-coinflip-baf/54-CONTEXT.md
@.planning/phases/54-coinflip-baf/54-RESEARCH.md
@.planning/phases/54-coinflip-baf/54-PATTERNS.md
@.planning/phases/54-coinflip-baf/54-VALIDATION.md

<!-- Template to mirror for the spec file -->
@.planning/phases/51-profile-quests/INTEG-02-SPEC.md
@.planning/phases/52-tickets-packs-jackpot/INTEG-01-SPEC.md

<!-- Template to mirror for the test files -->
@play/app/__tests__/play-profile-panel.test.js
@play/app/__tests__/play-tickets-panel.test.js

<!-- Existing Phase 50 stubs whose contract-grep must initially fail -->
@play/components/coinflip-panel.js
@play/components/baf-panel.js

<!-- Existing SHELL-01 guardrail being extended -->
@play/app/__tests__/play-shell-01.test.js

<interfaces>
<!-- Test-framework facts (Node 20+ built-in) -->
Framework: node:test + node:assert/strict (no JSDOM, no build step)
File reading: readFileSync + existsSync from node:fs
Path: fileURLToPath + dirname + join from node:url + node:path
Invocation: `node --test play/app/__tests__/play-coinflip-panel.test.js`

<!-- INTEG-05 response fields (test file greps for these literals; see 54-RESEARCH.md §8) -->
Response top-level keys: level, player, score, rank, totalParticipants, roundStatus
roundStatus enum: "open" | "closed" | "skipped" | "not_eligible"

<!-- /leaderboards/coinflip?day=N response (tests grep for the URL; see 54-RESEARCH.md §7 lines 217-250) -->
Response shape: { entries: [{ player, score, rank, day }] } (up to 10 entries)
Score unit: INTEGER-scale BURNIE (NOT wei) -- e.g. "52875" = 52,875 BURNIE

<!-- /leaderboards/baf?level=N response (see 54-RESEARCH.md §7 lines 252-284) -->
Response shape: { entries: [{ player, score, rank, level }] } (exactly top-4)
Score unit: WEI-scale BURNIE -- e.g. "475215212469240581904067" = 475,215 BURNIE

<!-- /player/:address?day=N coinflip block (already live via Phase 51 INTEG-02; see 54-RESEARCH.md §7 lines 285-330) -->
coinflip: {
  depositedAmount, claimablePreview, autoRebuyEnabled, autoRebuyStop,
  currentBounty, biggestFlipPlayer, biggestFlipAmount
}
All string fields are WEI-scale BURNIE (format via formatBurnie)

<!-- Data-bind markers the test files assert on (Waves 1+2 deliver these as data-bind attrs) -->
coinflip-panel bindings:
  bounty section: data-bind="bounty", data-bind="armed", data-bind="bounty-pool",
    data-bind="bounty-record", data-bind="bounty-holder"
  state section: data-bind="deposited", data-bind="claimable",
    data-bind="autorebuy", data-bind="takeprofit"
  leaderboard: data-bind="leaderboard-entries"
  armed indicator: data-armed attribute

baf-panel bindings:
  context row: data-bind="next-baf-level", data-bind="levels-until"
  round status: data-bind="round-status" with data-status attribute
  leaderboard: data-bind="leaderboard-entries", per-row data-rank attribute
  your-rank row: data-bind="your-rank", data-bind="your-rank-value",
    data-bind="total-participants", data-bind="your-score"
  panel-level: data-prominence attribute

<!-- Score-unit discipline (Pitfall 8 from 54-RESEARCH.md line 1517 + 54-PATTERNS.md Score-unit discipline section) -->
Coinflip leaderboard score -> render as integer BURNIE (NO wei division; use String(entry.score) directly)
Coinflip state amounts (deposited/claimable/bounty/biggest-flip/takeprofit) -> wei-scale, use formatBurnie
BAF leaderboard score -> wei-scale, use formatBurnie
BAF per-player score (INTEG-05) -> wei-scale, use formatBurnie

<!-- Phase 51 INTEG-02-SPEC.md structural template (section-ordering to mirror) -->
Sections in order: header block, Why This Endpoint Is Needed, Endpoint
  (path + query params), Response JSON Schema, Field Notes, Derivation Logic
  (for roundStatus), Contract-Call Map, Proposed Backend Implementation,
  New Schema Definitions, Error Modes, Acceptance Criteria, Timeline,
  Open Questions, Confidence.

<!-- SHELL-01 extension -- new FORBIDDEN entries (see 54-RESEARCH.md §6 lines 188-213) -->
{ label: 'beta/components/baf-panel.js (transitively wallet-tainted)',
  pattern: /from\s+['"][^'"]*\/beta\/components\/baf-panel\.js['"]/ },
{ label: 'beta/app/coinflip.js (ethers at line 4)',
  pattern: /from\s+['"][^'"]*\/beta\/app\/coinflip\.js['"]/ },
{ label: 'beta/app/baf.js (transitively tainted via utils.js)',
  pattern: /from\s+['"][^'"]*\/beta\/app\/baf\.js['"]/ },
</interfaces>
</context>

<read_first>
<!-- Every executor must read these before touching a task -->
- .planning/phases/54-coinflip-baf/54-CONTEXT.md (D-01..D-10 locked decisions; parse D-04 for INTEG-05 requirement, D-10 for INTEG-04 deferral)
- .planning/phases/54-coinflip-baf/54-RESEARCH.md Section 6 (SHELL-01 Transitive Audit; confirms both beta components are wallet-tainted), Section 7 (Existing Endpoint Actual Shapes; verified live shapes for /leaderboards/coinflip + /leaderboards/baf + /player/:address coinflip block), Section 8 (INTEG-05 Endpoint Spec Draft; the spec to copy verbatim), Section 11 (Day-Aware + Level-Aware Fetch Pattern Reuse; reveals the stale-guard identifiers the tests assert on), Section 12 (Validation Architecture; proposed test outlines), Section 13 Pitfall 8 (score unit mismatch; LOAD-BEARING for test discipline)
- .planning/phases/54-coinflip-baf/54-PATTERNS.md Section "Score-unit discipline" (lines 1462-1477; wire format table; copy the 4-row table into test assertion comments for both test files)
- .planning/phases/54-coinflip-baf/54-VALIDATION.md Test Files to Author (lines 53-87; assertion inventory per file)
- .planning/phases/51-profile-quests/INTEG-02-SPEC.md (structural template; section-header ordering to mirror in INTEG-05-SPEC.md)
- .planning/phases/52-tickets-packs-jackpot/INTEG-01-SPEC.md (secondary structural template; see how Phase 52 appended implementation notes)
- play/app/__tests__/play-profile-panel.test.js (contract-grep test pattern; readFileSync + regex; ~24 assertions shape)
- play/app/__tests__/play-tickets-panel.test.js (secondary pattern; ~40 assertions for list-rendering panel)
- play/app/__tests__/play-shell-01.test.js (existing recursive guardrail; the FORBIDDEN array at lines 22-31 is the exact extension point)
- play/components/coinflip-panel.js (current 40-LOC Phase 50 stub; confirms Wave 0 test assertions will fail against it)
- play/components/baf-panel.js (current 40-LOC Phase 50 stub; same)
- .planning/REQUIREMENTS.md (locate INTEG-04 row at line 85 for status edit + Traceability table row 163 for Phase 54 status update)
- .planning/ROADMAP.md Phase 54 success criterion 5 (line 159; explicit permission to defer INTEG-04 if documented)
- CLAUDE.md (no em dashes, no emojis; always use "--" for double-hyphens)
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Author INTEG-05-SPEC.md (database-repo contract)</name>
  <files>.planning/phases/54-coinflip-baf/INTEG-05-SPEC.md</files>
  <read_first>
    - .planning/phases/54-coinflip-baf/54-RESEARCH.md Section 8 (INTEG-05 Endpoint Spec Draft; lines 331-555 are the authoritative content -- copy verbatim, do not paraphrase)
    - .planning/phases/51-profile-quests/INTEG-02-SPEC.md (structural template; section-header ordering)
    - .planning/phases/52-tickets-packs-jackpot/INTEG-01-SPEC.md (secondary template; see Timeline section pattern for 3-atomic-commit side-quest shape)
    - /home/zak/Dev/PurgeGame/database/src/api/routes/player.ts (existing handler file the new endpoint extends -- identify the Phase 52 INTEG-01 `/tickets/by-trait` entry as the sibling pattern for `/baf`)
    - /home/zak/Dev/PurgeGame/database/src/api/schemas/player.ts (current schema file the new bafQuerySchema + bafPlayerResponseSchema will append to)
    - /home/zak/Dev/PurgeGame/database/src/handlers/baf-jackpot.ts (lines 53-68 for handleBafSkipped handler referenced in roundStatus derivation)
  </read_first>
  <action>
Create `.planning/phases/54-coinflip-baf/INTEG-05-SPEC.md` using the Write tool. The content below is authoritative -- copy it verbatim (section headers, tables, code blocks, SQL, prose). Do not paraphrase or re-order sections.

BEGIN CONTENT:

```
# INTEG-05 Spec: GET /player/:address/baf?level=N (per-player BAF score endpoint)

**Phase:** 54 -- Coinflip & BAF Leaderboards
**Requirement:** INTEG-05 (hard gate; blocks Phase 54 Wave 2 BAF-01 + BAF-03 hydration)
**Owner:** website repo (proposal); database repo (implementation)
**Status:** DRAFT -- solo dev self-coordination; implement when switching to /home/zak/Dev/PurgeGame/database/
**Posted:** 2026-04-24 -- solo dev (no external team); this file is the canonical spec to reference when adding the endpoint in the database repo.

## Why This Endpoint Is Needed

BAF-01 requires showing "selected player's BAF score and rank at current level/window". The existing `/leaderboards/baf?level=N` returns only the top-4 per level by design (the materialized view `mv_baf_top4` has a `WHERE rank <= 4` filter). Any player outside the top-4 needs a separate lookup: their BAF score AND their rank relative to all participants in the level.

BAF-03 requires showing "whether the round is open or closed". The round status is a combination of: (1) did BAF-eligibility hit (level % 10 == 0)? (2) did the coinflip on the advance day win? (3) has the jackpot distribution already run? Deriving this purely from `game.phase` in the frontend is unreliable -- the backend has the authoritative state via `baf_skipped` events + jackpot-distribution history.

A new endpoint class is cleanest: it does not pollute `/player/:address` (already one of the largest dashboard endpoints), and it keeps the BAF-specific indexing logic colocated with BAF data.

## Endpoint

`GET /player/:address/baf?level=N`

### Path Parameters

- `:address` -- 42-char `0x...` Ethereum address, lowercased (same validation pattern as existing routes via `addressParamSchema`)

### Query Parameters

- `level` (required, integer, >= 1) -- BAF level. Not optional because the view is per-level; there is no meaningful "no level" state. If omitted, return 400.

**Recommendation:** Require `level`. Phase 54 frontend ALWAYS supplies it (from `state.replay.level`). Reject missing with 400.

**Alternative:** Accept `day` instead of `level` (backend resolves day -> level via `daily_rng`). Pro: matches Phase 51's `?day=N` pattern. Con: BAF is level-scoped not day-scoped (a level spans ~5 days). For INTEG-05 v1, ship `?level=N` only; add `?day=M` later if needed.

## Response JSON Schema

```json
{
  "level": 20,
  "player": "0x71be63f3384f5fb98995898a86b02fb2426c5788",
  "score": "344863111573291904385281",
  "rank": 7,
  "totalParticipants": 42,
  "roundStatus": "open"
}
```

Field notes:

- `level` -- echo of `?level=N` query parameter
- `player` -- echo of `:address` path parameter (lowercased)
- `score` -- wei-encoded BURNIE as decimal string (same scale as `/leaderboards/baf` entries); `"0"` if the player has no stake at this level
- `rank` -- 1-indexed rank within the level; `null` if player has no BAF stake at this level
- `totalParticipants` -- count of DISTINCT players with totalStake > 0 at this level; `0` if the level has no BAF entries at all
- `roundStatus` -- one of `"open"` | `"closed"` | `"skipped"` | `"not_eligible"` (see derivation below)

### Edge cases

| Condition | score | rank | totalParticipants | roundStatus |
|-----------|-------|------|-------------------|-------------|
| Player has BAF stake at this level | "123..." | 1..N | N | "open" / "closed" / "skipped" |
| Player has ZERO BAF stake at this level | "0" | null | N (others exist) | whatever the level's status is |
| Level has no BAF entries at all (no one staked) | "0" | null | 0 | "open" or "not_eligible" |
| Level is NOT a multiple of 10 (no BAF eligibility) | "0" | null | 0 | "not_eligible" |
| BAF was skipped (coinflip lost on advance day) | "0" or actual | null or rank | N | "skipped" |

## roundStatus Derivation

Four states:

1. **"not_eligible"** -- Level is not a multiple of 10. BAF jackpots only fire at level boundaries where `level % 10 == 0`. Frontend shows a softer label such as "BAF active at next level {nextBafLevel}".
2. **"open"** -- Level % 10 == 0 AND no `baf_skipped` row AND no `jackpot_distributions` row with `awardType IN ('eth_baf', 'tickets_baf')` for this level. Players can still stake.
3. **"closed"** -- Level % 10 == 0 AND the BAF jackpot has run (there EXISTS a `jackpot_distributions` row with `awardType IN ('eth_baf', 'tickets_baf')` for this level).
4. **"skipped"** -- Level % 10 == 0 AND `baf_skipped` row exists for this level (coinflip lost on advance day). Contract `DegenerusJackpots.markBafSkipped` emits; handler `handleBafSkipped` at `database/src/handlers/baf-jackpot.ts:53-68` indexes.

Backend pseudo-code:

```sql
IF level % 10 != 0 THEN roundStatus := 'not_eligible'
ELSIF EXISTS (SELECT 1 FROM baf_skipped WHERE level = ?) THEN roundStatus := 'skipped'
ELSIF EXISTS (SELECT 1 FROM jackpot_distributions WHERE level = ? AND awardType IN ('eth_baf', 'tickets_baf')) THEN roundStatus := 'closed'
ELSE roundStatus := 'open'
```

## Contract-Call Map

| Response field | Primary source | Notes |
|----------------|----------------|-------|
| `level` | Query echo | From `?level=N` |
| `player` | Path echo | Validated via `addressParamSchema`; lowercased |
| `score` | `baf_flip_totals.totalStake` | Filtered by `(player, level)`; returns '0' if no row |
| `rank` | `ROW_NUMBER() OVER (PARTITION BY level ORDER BY CAST("totalStake" AS NUMERIC) DESC)` | Same expression used by `mv_baf_top4`; returns `null` when the player has no row |
| `totalParticipants` | `SELECT COUNT(*) FROM baf_flip_totals WHERE level = ?` | Count of distinct (player, level) rows at the level |
| `roundStatus` | See derivation above | Three-table query: `baf_skipped`, `jackpot_distributions`, level arithmetic |

## Proposed Backend Implementation

Handler file: extend `database/src/api/routes/player.ts` (where Phase 52 INTEG-01's `/player/:address/tickets/by-trait` already landed; place the new handler just before or after it).

```typescript
// INTEG-05 (Phase 54): GET /player/:address/baf?level=N
// Returns the player's BAF score and rank at a given level, plus the round status.
fastify.get('/:address/baf', {
  schema: {
    params: addressParamSchema,
    querystring: bafQuerySchema,
    response: {
      200: bafPlayerResponseSchema,
      400: errorResponseSchema,
    },
  },
}, async (request, reply) => {
  const { address } = request.params;
  const { level } = request.query;

  // --- Score + rank + totalParticipants ---
  // One CTE derives both: ranked row for the player + count window.
  const result = await fastify.db.execute(sql`
    WITH ranked AS (
      SELECT
        player,
        "totalStake",
        ROW_NUMBER() OVER (ORDER BY CAST("totalStake" AS NUMERIC) DESC) AS rank,
        COUNT(*) OVER () AS total_participants
      FROM baf_flip_totals
      WHERE level = ${level}
    )
    SELECT player, "totalStake", rank, total_participants
    FROM ranked
    WHERE player = ${address}
    UNION ALL
    SELECT NULL, '0', NULL, COALESCE((SELECT MAX(total_participants) FROM ranked), 0)
    WHERE NOT EXISTS (SELECT 1 FROM ranked WHERE player = ${address})
    LIMIT 1
  `);

  const row = (result as any).rows?.[0] ?? (result as any)[0];
  const score = row?.totalStake ?? '0';
  const rank = row?.rank != null ? Number(row.rank) : null;
  const totalParticipants = Number(row?.total_participants ?? 0);

  // --- roundStatus derivation ---
  let roundStatus: 'open' | 'closed' | 'skipped' | 'not_eligible';
  if (level % 10 !== 0) {
    roundStatus = 'not_eligible';
  } else {
    const [skipped] = await fastify.db
      .select({ level: bafSkipped.level })
      .from(bafSkipped)
      .where(eq(bafSkipped.level, level));
    if (skipped) {
      roundStatus = 'skipped';
    } else {
      const [distributed] = await fastify.db
        .select({ id: jackpotDistributions.id })
        .from(jackpotDistributions)
        .where(and(
          eq(jackpotDistributions.level, level),
          inArray(jackpotDistributions.awardType, ['eth_baf', 'tickets_baf']),
        ))
        .limit(1);
      roundStatus = distributed ? 'closed' : 'open';
    }
  }

  return {
    level,
    player: address,
    score,
    rank,
    totalParticipants,
    roundStatus,
  };
});
```

## New Schema Definitions

Add to `database/src/api/schemas/player.ts`:

```typescript
// INTEG-05 (Phase 54): GET /player/:address/baf
export const bafQuerySchema = z.object({
  level: z.coerce.number().int().min(1),
});

export const bafPlayerResponseSchema = z.object({
  level: z.number().int(),
  player: z.string(),
  score: z.string(),
  rank: z.number().int().nullable(),
  totalParticipants: z.number().int(),
  roundStatus: z.enum(['open', 'closed', 'skipped', 'not_eligible']),
});
```

## Error Modes

| Status | Condition | Response |
|--------|-----------|----------|
| 200 | Player has BAF stake at level | Full payload with non-null rank |
| 200 | Player has NO BAF stake at level | Full payload, score="0", rank=null, totalParticipants echoes level-wide count |
| 200 | Level has no entries at all | Full payload, all zeros; roundStatus reflects level-eligibility |
| 400 | `level` missing or invalid | `{ statusCode: 400, error: "Bad Request", message: "..." }` (Zod validation handles this) |

## Acceptance Criteria

- Endpoint reachable at `GET /player/:address/baf?level=N`
- `score` matches `mv_baf_top4.score` for the same `(player, level)` tuple when the player is in the top-4
- `rank` is 1 for the top staker at that level, continuing through 1..N
- `rank` is `null` when the player has no row in `baf_flip_totals` at that level (including all players at level % 10 != 0)
- `totalParticipants` equals `COUNT(DISTINCT player)` from `baf_flip_totals WHERE level = :level`
- `roundStatus === 'not_eligible'` when `level % 10 != 0`
- `roundStatus === 'skipped'` when a `baf_skipped` row exists for the level
- `roundStatus === 'closed'` when a BAF `jackpot_distributions` row exists for the level
- `roundStatus === 'open'` otherwise

## Timeline

Same pattern as INTEG-01 (Phase 52) and INTEG-02 (Phase 51) -- 3 atomic commits on `database/main`:

1. **Commit 1 -- `feat(api): add GET /player/:address/baf endpoint (INTEG-05)`** -- handler + schema definitions
2. **Commit 2 -- `docs(openapi): document /player/:address/baf (INTEG-05)`** -- openapi.json update
3. **Commit 3 -- `test(api): cover INTEG-05 happy path, rank null, empty level, not_eligible, skipped, closed`** -- Vitest tests

Precedent shipped in 3-5 minutes per Phase 51 INTEG-02 (`d135605`, `dab5adf`, `64fe8db`) and Phase 52 INTEG-01 (`a46fdcb`, `e130547`, `9988887`). Phase 54 INTEG-05 is comparable scope.

## Open Questions (for INTEG-05 spec)

1. **Should `?level=N` or `?day=M` be the primary query?** v1 ships `?level=N` only; defer `?day=M` support.
2. **Should the rank be a dense-rank or row-number?** Tied scores get sequential ranks with row-number (e.g., two players at 100 BURNIE get ranks 1 and 2 by insertion order). Dense-rank would give both rank 1. Backend uses row-number because `mv_baf_top4` already does. Consistency > philosophical correctness; ties are rare in practice.
3. **Should the endpoint echo the player's deposited coinflip amount for bundled coinflip+BAF rendering?** No -- separate concern. If the panel needs both, fetch from `/player/:address?day=N` (coinflip block) AND `/player/:address/baf?level=N` (BAF block).

## Confidence

- **HIGH** on the need + path (new endpoint class; no conflict with existing routes)
- **HIGH** on data source (`baf_flip_totals` is the authoritative indexed table; `handleBafFlipRecorded` stores cumulative `totalStake` per `(player, level)` pair)
- **HIGH** on `roundStatus` derivation (3 tables covered: `baf_skipped`, `jackpot_distributions`, level arithmetic)
- **MEDIUM** on the ROW_NUMBER CTE behavior (verified PostgreSQL supports the UNION ALL pattern but exact escape for parameter binding in Drizzle may need adjustment; the database team will finalize at commit time)
```

END CONTENT.

## Self-check after writing

- File exists at `.planning/phases/54-coinflip-baf/INTEG-05-SPEC.md`.
- At least 180 lines.
- Contains the word `roundStatus` (frontmatter check + body).
- Contains the SQL CTE block starting with `WITH ranked AS`.
- Contains the four roundStatus enum values verbatim: `"open"`, `"closed"`, `"skipped"`, `"not_eligible"`.
- Contains the three-commit Timeline section mirroring INTEG-02-SPEC.md.
- No em dashes; no emojis.
- All TypeScript snippets render without backtick escaping issues.
  </action>
  <verify>
    <automated>test -f .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md && wc -l .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md | awk '{exit ($1 < 180) ? 1 : 0}' && grep -q "roundStatus" .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md && grep -q "WITH ranked AS" .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md && grep -q "not_eligible" .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md && grep -q "bafQuerySchema" .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md` exits 0
    - `wc -l .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md` reports >= 180 lines
    - `grep -c "roundStatus" .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md` returns >= 10
    - `grep -c "not_eligible" .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md` returns >= 3
    - `grep -q "GET /player/:address/baf?level=N" .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md` exits 0
    - `grep -q "WITH ranked AS" .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md` exits 0
    - `grep -q "bafQuerySchema" .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md` exits 0
    - `grep -q "baf_flip_totals" .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md` exits 0
    - `grep -q "handleBafSkipped" .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md` exits 0
    - `grep -q "## Timeline" .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md` exits 0
    - `grep -q "## Acceptance Criteria" .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md` exits 0
    - `grep -q "Confidence" .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md` exits 0
    - `! grep -q "—" .planning/phases/54-coinflip-baf/INTEG-05-SPEC.md` exits 0 (no em dashes)
  </acceptance_criteria>
  <done>
    `.planning/phases/54-coinflip-baf/INTEG-05-SPEC.md` exists with the full endpoint contract per 54-RESEARCH.md Section 8. All sections present: Why This Endpoint Is Needed, Endpoint (path + query), Response JSON Schema, roundStatus Derivation, Contract-Call Map, Proposed Backend Implementation (TypeScript handler + SQL CTE), New Schema Definitions (Zod), Error Modes, Acceptance Criteria, Timeline (3-commit shape), Open Questions, Confidence. The database repo has a self-contained spec to implement against without re-reading 54-RESEARCH.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author play-coinflip-panel.test.js + play-baf-panel.test.js + extend play-shell-01.test.js</name>
  <files>play/app/__tests__/play-coinflip-panel.test.js, play/app/__tests__/play-baf-panel.test.js, play/app/__tests__/play-shell-01.test.js</files>
  <read_first>
    - play/app/__tests__/play-profile-panel.test.js (PRIMARY template; the exact contract-grep shape; copy header + readFileSync boilerplate + assertion style)
    - play/app/__tests__/play-tickets-panel.test.js (SECONDARY template; shows how list-rendering assertions are structured -- coinflip + baf leaderboards use the same row-entry pattern)
    - play/app/__tests__/play-shell-01.test.js (the existing FORBIDDEN array at lines 22-31 -- Task 2 appends 3 new entries)
    - play/components/coinflip-panel.js (Phase 50 stub; confirm tests fail against the current 40-LOC content)
    - play/components/baf-panel.js (Phase 50 stub; same)
    - .planning/phases/54-coinflip-baf/54-RESEARCH.md Section 12 (lines 1208-1433; proposed test file outlines with ~25 assertions each -- USE THESE AS THE SKELETON)
    - .planning/phases/54-coinflip-baf/54-VALIDATION.md Test Files to Author (lines 53-87)
    - .planning/phases/54-coinflip-baf/54-PATTERNS.md Score-unit discipline section (lines 1462-1477; the 4-row table MUST appear as a comment block in each test file)
  </read_first>
  <action>
Three edits in this task: (1) create play-coinflip-panel.test.js; (2) create play-baf-panel.test.js; (3) extend play-shell-01.test.js FORBIDDEN array.

## Edit 1: Create `play/app/__tests__/play-coinflip-panel.test.js`

Use the Write tool. Target ~130 lines, ~28 assertions. Content:

```javascript
// play/app/__tests__/play-coinflip-panel.test.js
//
// Phase 54 Wave 0 -- contract-grep tests for <coinflip-panel>.
//
// Coverage:
//   COINFLIP-01 -- selected player's coinflip state (deposited, claimable, autoRebuy, takeprofit)
//   COINFLIP-02 -- daily coinflip leaderboard (top-10 from /leaderboards/coinflip?day=N)
//   COINFLIP-03 -- current bounty + biggest-flip player + amount (from /player/:addr?day=N coinflip block)
//   SHELL-01 negatives -- no imports of beta/app/coinflip.js, beta/app/utils.js, ethers, wallet, contracts
//   Fetch wiring -- Promise.all over /player/:addr?day= and /leaderboards/coinflip?day=
//   Stale-guard -- #coinflipFetchId counter + is-stale class toggle
//   Score-unit discipline (Pitfall 8) -- coinflip leaderboard scores are INTEGER-scale (no wei division);
//     state amounts are WEI-scale (formatBurnie required)
//   Empty state -- entries.length check or equivalent guard against rendering zero-entry leaderboards
//   Selected-player highlighting -- aria-current="true" on matching row

import { test } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync, existsSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';

const __dirname = dirname(fileURLToPath(import.meta.url));
// play/app/__tests__/ -> up 1 -> play/app -> up 1 -> play
const PLAY_ROOT = join(__dirname, '../..');
const PANEL = join(PLAY_ROOT, 'components', 'coinflip-panel.js');

// ---------------------------------------------------------------------------
// Existence + registration
// ---------------------------------------------------------------------------

test('coinflip-panel.js exists', () => {
  assert.ok(existsSync(PANEL), `expected ${PANEL} to exist`);
});

test('coinflip-panel.js registers the <coinflip-panel> custom element', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /customElements\.define\(\s*['"]coinflip-panel['"]/);
});

test('coinflip-panel.js defines a class extending HTMLElement', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /class\s+\w+\s+extends\s+HTMLElement/);
});

test('coinflip-panel.js has connectedCallback and disconnectedCallback', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /connectedCallback\s*\(\s*\)/);
  assert.match(src, /disconnectedCallback\s*\(\s*\)/);
});

// ---------------------------------------------------------------------------
// SHELL-01: no wallet-tainted imports
// ---------------------------------------------------------------------------

test('SHELL-01: coinflip-panel.js does NOT import beta/app/coinflip.js (ethers at line 4)', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.doesNotMatch(src, /from\s+['"][^'"]*\/beta\/app\/coinflip\.js['"]/);
});

test('SHELL-01: coinflip-panel.js does NOT import beta/app/utils.js (ethers at line 3)', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.doesNotMatch(src, /from\s+['"][^'"]*\/beta\/app\/utils\.js['"]/);
});

test('SHELL-01: coinflip-panel.js does NOT import ethers directly', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.doesNotMatch(src, /from\s+['"]ethers['"]/);
});

test('SHELL-01: coinflip-panel.js does NOT import beta/components/coinflip-panel.js', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.doesNotMatch(src, /from\s+['"][^'"]*\/beta\/components\/coinflip-panel\.js['"]/);
});

test('coinflip-panel.js imports from beta/viewer/utils.js (wallet-free surface)', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /from\s+['"][^'"]*\/beta\/viewer\/utils\.js['"]/);
});

test('coinflip-panel.js imports subscribe + get from the reused beta store', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /from\s+['"][^'"]*\/beta\/app\/store\.js['"]/);
  assert.match(src, /subscribe/);
  assert.match(src, /\bget\b/);
});

test('coinflip-panel.js imports API_BASE from play/app/constants.js', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /from\s+['"][^'"]*\/app\/constants\.js['"]/);
  assert.match(src, /API_BASE/);
});

// ---------------------------------------------------------------------------
// Store wiring -- subscribes to day + player (coinflip is day-based, not level-based)
// ---------------------------------------------------------------------------

test('coinflip-panel.js subscribes to replay.day AND replay.player (no replay.level)', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /subscribe\(\s*['"]replay\.day['"]/);
  assert.match(src, /subscribe\(\s*['"]replay\.player['"]/);
});

// ---------------------------------------------------------------------------
// COINFLIP-01: state section (deposited, claimable, autoRebuy, takeprofit)
// ---------------------------------------------------------------------------

test('COINFLIP-01: renders state data-binds (deposited, claimable, autorebuy, takeprofit)', () => {
  const src = readFileSync(PANEL, 'utf8');
  for (const key of ['deposited', 'claimable', 'autorebuy', 'takeprofit']) {
    assert.match(src, new RegExp(`data-bind=["']${key}["']`),
      `expected data-bind="${key}" for COINFLIP-01 state section`);
  }
});

// ---------------------------------------------------------------------------
// COINFLIP-02: leaderboard
// ---------------------------------------------------------------------------

test('COINFLIP-02: fetches /leaderboards/coinflip?day=', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /\/leaderboards\/coinflip\?day=/);
});

test('COINFLIP-02: renders leaderboard entries container', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /data-bind=["']leaderboard-entries["']/);
});

// ---------------------------------------------------------------------------
// COINFLIP-03: bounty section (pool, record flip, record holder, armed)
// ---------------------------------------------------------------------------

test('COINFLIP-03: renders bounty data-binds (armed, bounty-pool, bounty-record, bounty-holder)', () => {
  const src = readFileSync(PANEL, 'utf8');
  for (const key of ['armed', 'bounty-pool', 'bounty-record', 'bounty-holder']) {
    assert.match(src, new RegExp(`data-bind=["']${key}["']`),
      `expected data-bind="${key}" for COINFLIP-03 bounty section`);
  }
});

test('COINFLIP-03: armed indicator uses data-armed attribute', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /data-armed=/);
});

// ---------------------------------------------------------------------------
// Fetch wiring: player endpoint for COINFLIP-01/03 reads
// ---------------------------------------------------------------------------

test('coinflip-panel.js fetches /player/${addr}?day= (extended response coinflip block)', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /\/player\/\$\{[^}]+\}\?day=|\/player\/\$\{.+?\}\?day=/);
});

// ---------------------------------------------------------------------------
// Stale-guard + keep-old-data-dim (D-17 / D-18 carry-forward from Phase 51)
// ---------------------------------------------------------------------------

test('coinflip-panel.js uses #coinflipFetchId stale-guard counter', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /#coinflipFetchId/);
});

test('coinflip-panel.js toggles is-stale class for keep-old-data-dim', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /is-stale/);
});

// ---------------------------------------------------------------------------
// Skeleton + content pattern
// ---------------------------------------------------------------------------

test('coinflip-panel.js has data-bind="skeleton" and data-bind="content"', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /data-bind=["']skeleton["']/);
  assert.match(src, /data-bind=["']content["']/);
});

test('coinflip-panel.js includes skeleton-shimmer in the skeleton block', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /skeleton-shimmer/);
});

// ---------------------------------------------------------------------------
// Empty-state handling (COINFLIP-02 renders nothing gracefully)
// ---------------------------------------------------------------------------

test('coinflip-panel.js handles empty leaderboard gracefully (length check)', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /entries\s*\.\s*length|entries\.length|\.length\s*===\s*0|\.length\s*<\s*1/);
});

// ---------------------------------------------------------------------------
// Selected-player aria-current
// ---------------------------------------------------------------------------

test('coinflip-panel.js highlights selected player with aria-current', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /aria-current/);
});

// ---------------------------------------------------------------------------
// Score-unit discipline (Pitfall 8 -- LOAD-BEARING per 54-RESEARCH.md line 1517)
// ---------------------------------------------------------------------------
// Table from 54-PATTERNS.md Score-unit discipline section:
//
//   Endpoint                                            Score format                  Render with
//   /leaderboards/coinflip?day=N                        INTEGER-scale BURNIE          String(entry.score) OR no-divisor format
//   /leaderboards/baf?level=N                           WEI-scale BURNIE              formatBurnie(entry.score)
//   /player/:addr/baf?level=N (INTEG-05)                WEI-scale BURNIE              formatBurnie(data.score)
//   /player/:addr?day=N coinflip block                  WEI-scale BURNIE              formatBurnie(value)
//
// WARNING SIGN OF BUG: coinflip leaderboard shows "0.000000000000052875 BURNIE"
// (wrong -- treats integer as wei) OR player state shows "200000000000000000000"
// without formatBurnie (wrong -- raw wei leaked to UI).

test('score-unit: coinflip-panel.js uses formatBurnie for state amounts (wei-scale)', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /formatBurnie/);
});

test('score-unit: coinflip-panel.js uses truncateAddress for player addresses', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /truncateAddress/);
});
```

## Edit 2: Create `play/app/__tests__/play-baf-panel.test.js`

Use the Write tool. Target ~140 lines, ~30 assertions. Content:

```javascript
// play/app/__tests__/play-baf-panel.test.js
//
// Phase 54 Wave 0 -- contract-grep tests for <baf-panel>.
//
// Coverage:
//   BAF-01 -- selected player's BAF score and rank (hard-gated on INTEG-05 in Wave 2)
//   BAF-02 -- top-4 BAF leaderboard with prominence styling via data-rank attribute
//   BAF-03 -- round-status pill (open/closed/skipped/not_eligible) + next-baf-level label
//   SHELL-01 negatives -- no imports of beta/app/baf.js, beta/app/utils.js, beta/components/baf-panel.js, ethers
//   Fetch wiring -- /leaderboards/baf?level=N (Wave 1) + /player/:addr/baf?level=N (Wave 2 INTEG-05)
//   Dual stale-guards -- #bafFetchId (leaderboard) + #bafPlayerFetchId (per-player INTEG-05)
//   Score-unit discipline (Pitfall 8) -- BAF scores are WEI-scale (formatBurnie required)
//   Inline bafContext derivation -- nextBafLevel via Math.ceil((level+1)/10)*10 (per 54-PATTERNS.md)
//   Selected-player highlighting -- aria-current="true" on matching row
//   Empty state -- entries.length check or equivalent

import { test } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync, existsSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';

const __dirname = dirname(fileURLToPath(import.meta.url));
const PLAY_ROOT = join(__dirname, '../..');
const PANEL = join(PLAY_ROOT, 'components', 'baf-panel.js');

// ---------------------------------------------------------------------------
// Existence + registration
// ---------------------------------------------------------------------------

test('baf-panel.js exists', () => {
  assert.ok(existsSync(PANEL), `expected ${PANEL} to exist`);
});

test('baf-panel.js registers the <baf-panel> custom element', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /customElements\.define\(\s*['"]baf-panel['"]/);
});

test('baf-panel.js defines a class extending HTMLElement', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /class\s+\w+\s+extends\s+HTMLElement/);
});

test('baf-panel.js has connectedCallback and disconnectedCallback', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /connectedCallback\s*\(\s*\)/);
  assert.match(src, /disconnectedCallback\s*\(\s*\)/);
});

// ---------------------------------------------------------------------------
// SHELL-01: no wallet-tainted imports (three newly-added FORBIDDEN paths covered)
// ---------------------------------------------------------------------------

test('SHELL-01: baf-panel.js does NOT import beta/app/baf.js (transitively tainted via utils.js)', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.doesNotMatch(src, /from\s+['"][^'"]*\/beta\/app\/baf\.js['"]/);
});

test('SHELL-01: baf-panel.js does NOT import beta/app/utils.js (ethers at line 3)', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.doesNotMatch(src, /from\s+['"][^'"]*\/beta\/app\/utils\.js['"]/);
});

test('SHELL-01: baf-panel.js does NOT import beta/components/baf-panel.js (tag-name collision + transitively tainted)', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.doesNotMatch(src, /from\s+['"][^'"]*\/beta\/components\/baf-panel\.js['"]/);
});

test('SHELL-01: baf-panel.js does NOT import ethers directly', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.doesNotMatch(src, /from\s+['"]ethers['"]/);
});

test('baf-panel.js imports from beta/viewer/utils.js (wallet-free surface)', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /from\s+['"][^'"]*\/beta\/viewer\/utils\.js['"]/);
});

test('baf-panel.js imports subscribe + get from the reused beta store', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /from\s+['"][^'"]*\/beta\/app\/store\.js['"]/);
  assert.match(src, /subscribe/);
  assert.match(src, /\bget\b/);
});

test('baf-panel.js imports API_BASE from play/app/constants.js', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /from\s+['"][^'"]*\/app\/constants\.js['"]/);
  assert.match(src, /API_BASE/);
});

// ---------------------------------------------------------------------------
// Store wiring -- BAF is level-based, subscribes to both level + player
// ---------------------------------------------------------------------------

test('baf-panel.js subscribes to replay.level AND replay.player', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /subscribe\(\s*['"]replay\.level['"]/);
  assert.match(src, /subscribe\(\s*['"]replay\.player['"]/);
});

// ---------------------------------------------------------------------------
// BAF-02: leaderboard top-4 + prominence via data-rank
// ---------------------------------------------------------------------------

test('BAF-02: fetches /leaderboards/baf?level=', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /\/leaderboards\/baf\?level=/);
});

test('BAF-02: renders leaderboard entries container', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /data-bind=["']leaderboard-entries["']/);
});

test('BAF-02: uses data-rank attribute for prominence tiers (gold/silver/bronze/regular per D-06)', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /data-rank/);
});

// ---------------------------------------------------------------------------
// BAF-03: round-status + next-BAF-level + levels-until
// ---------------------------------------------------------------------------

test('BAF-03: renders round-status pill with data-bind and data-status attribute', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /data-bind=["']round-status["']/);
  assert.match(src, /data-status/);
});

test('BAF-03: renders next-baf-level data-bind', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /data-bind=["']next-baf-level["']/);
});

test('BAF-03: renders levels-until data-bind', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /data-bind=["']levels-until["']/);
});

// ---------------------------------------------------------------------------
// BAF-01: your-rank row hydrated from INTEG-05 (Wave 2 adds the fetch + render)
// ---------------------------------------------------------------------------

test('BAF-01: fetches /player/${addr}/baf?level= (INTEG-05)', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /\/player\/[^`'"]+\/baf\?level=/);
});

test('BAF-01: renders your-rank section with data-bind="your-rank"', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /data-bind=["']your-rank["']/);
});

test('BAF-01: renders your-rank-value, total-participants, your-score bindings', () => {
  const src = readFileSync(PANEL, 'utf8');
  for (const key of ['your-rank-value', 'total-participants', 'your-score']) {
    assert.match(src, new RegExp(`data-bind=["']${key}["']`),
      `expected data-bind="${key}" for BAF-01 your-rank section`);
  }
});

// ---------------------------------------------------------------------------
// Prominence (panel-level + row-level)
// ---------------------------------------------------------------------------

test('baf-panel.js uses data-prominence attribute on the panel element', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /data-prominence/);
});

// ---------------------------------------------------------------------------
// Dual stale-guards (leaderboard + per-player fetch invalidate at different cadences)
// ---------------------------------------------------------------------------

test('baf-panel.js uses #bafFetchId stale-guard counter (leaderboard fetch)', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /#bafFetchId/);
});

test('baf-panel.js uses #bafPlayerFetchId stale-guard counter (INTEG-05 fetch)', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /#bafPlayerFetchId/);
});

test('baf-panel.js toggles is-stale class for keep-old-data-dim', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /is-stale/);
});

// ---------------------------------------------------------------------------
// Skeleton + content pattern
// ---------------------------------------------------------------------------

test('baf-panel.js has data-bind="skeleton" and data-bind="content"', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /data-bind=["']skeleton["']/);
  assert.match(src, /data-bind=["']content["']/);
});

test('baf-panel.js includes skeleton-shimmer in the skeleton block', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /skeleton-shimmer/);
});

// ---------------------------------------------------------------------------
// Inline bafContext derivation -- nextBafLevel = Math.ceil((level+1)/10)*10
// per 54-PATTERNS.md recommendation to inline the 15-LOC logic.
// ---------------------------------------------------------------------------

test('baf-panel.js includes nextBafLevel derivation (Math.ceil((level+1)/10))', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /Math\.ceil\(\s*\(?\s*level\s*\+\s*1\s*\)?\s*\/\s*10\s*\)/);
});

// ---------------------------------------------------------------------------
// Empty-state handling
// ---------------------------------------------------------------------------

test('baf-panel.js handles empty leaderboard gracefully (length check)', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /entries\s*\.\s*length|entries\.length|\.length\s*===\s*0|\.length\s*<\s*1/);
});

// ---------------------------------------------------------------------------
// Selected-player aria-current
// ---------------------------------------------------------------------------

test('baf-panel.js highlights selected player with aria-current', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /aria-current/);
});

// ---------------------------------------------------------------------------
// Score-unit discipline (Pitfall 8 -- LOAD-BEARING per 54-RESEARCH.md line 1517)
// ---------------------------------------------------------------------------
// Table from 54-PATTERNS.md Score-unit discipline section:
//
//   /leaderboards/baf?level=N                           WEI-scale BURNIE              formatBurnie(entry.score)
//   /player/:addr/baf?level=N (INTEG-05 score field)    WEI-scale BURNIE              formatBurnie(data.score)
//
// WARNING SIGN OF BUG: BAF leaderboard shows "475,215,212,469,240,581,904,067 BURNIE"
// (wrong -- treats wei as integer; must divide via formatBurnie).

test('score-unit: baf-panel.js uses formatBurnie for BAF scores (wei-scale)', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /formatBurnie/);
});

test('score-unit: baf-panel.js uses truncateAddress for player addresses', () => {
  const src = readFileSync(PANEL, 'utf8');
  assert.match(src, /truncateAddress/);
});
```

## Edit 3: Extend `play/app/__tests__/play-shell-01.test.js` FORBIDDEN array

Use the Edit tool (surgical). Locate the FORBIDDEN array at lines 22-31 of the current file.

Current content at lines 22-31:

```javascript
const FORBIDDEN = [
  { label: "bare 'ethers' specifier", pattern: /from\s+['"]ethers['"]/ },
  { label: 'beta/app/wallet.js', pattern: /from\s+['"][^'"]*\/beta\/app\/wallet\.js['"]/ },
  { label: 'beta/app/contracts.js', pattern: /from\s+['"][^'"]*\/beta\/app\/contracts\.js['"]/ },
  { label: 'beta/app/utils.js (ethers-tainted at line 3)', pattern: /from\s+['"][^'"]*\/beta\/app\/utils\.js['"]/ },
  { label: 'beta/components/connect-prompt.js', pattern: /from\s+['"][^'"]*\/beta\/components\/connect-prompt\.js['"]/ },
  { label: 'beta/components/purchase-panel.js', pattern: /from\s+['"][^'"]*\/beta\/components\/purchase-panel\.js['"]/ },
  { label: 'beta/components/coinflip-panel.js', pattern: /from\s+['"][^'"]*\/beta\/components\/coinflip-panel\.js['"]/ },
  { label: 'beta/components/decimator-panel.js', pattern: /from\s+['"][^'"]*\/beta\/components\/decimator-panel\.js['"]/ },
];
```

Replace with (three new entries appended BEFORE the closing `];`):

```javascript
const FORBIDDEN = [
  { label: "bare 'ethers' specifier", pattern: /from\s+['"]ethers['"]/ },
  { label: 'beta/app/wallet.js', pattern: /from\s+['"][^'"]*\/beta\/app\/wallet\.js['"]/ },
  { label: 'beta/app/contracts.js', pattern: /from\s+['"][^'"]*\/beta\/app\/contracts\.js['"]/ },
  { label: 'beta/app/utils.js (ethers-tainted at line 3)', pattern: /from\s+['"][^'"]*\/beta\/app\/utils\.js['"]/ },
  { label: 'beta/components/connect-prompt.js', pattern: /from\s+['"][^'"]*\/beta\/components\/connect-prompt\.js['"]/ },
  { label: 'beta/components/purchase-panel.js', pattern: /from\s+['"][^'"]*\/beta\/components\/purchase-panel\.js['"]/ },
  { label: 'beta/components/coinflip-panel.js', pattern: /from\s+['"][^'"]*\/beta\/components\/coinflip-panel\.js['"]/ },
  { label: 'beta/components/decimator-panel.js', pattern: /from\s+['"][^'"]*\/beta\/components\/decimator-panel\.js['"]/ },
  { label: 'beta/components/baf-panel.js (Phase 54: transitively wallet-tainted + tag-name collision)', pattern: /from\s+['"][^'"]*\/beta\/components\/baf-panel\.js['"]/ },
  { label: 'beta/app/coinflip.js (Phase 54: ethers at line 4)', pattern: /from\s+['"][^'"]*\/beta\/app\/coinflip\.js['"]/ },
  { label: 'beta/app/baf.js (Phase 54: transitively tainted via utils.js)', pattern: /from\s+['"][^'"]*\/beta\/app\/baf\.js['"]/ },
];
```

This is a pure additive edit: 3 new array elements appended at the end of the FORBIDDEN literal. No other logic changes; the recursive scanner at lines 80-100 iterates over FORBIDDEN and catches violations without modification.

## Self-check after all three edits

- `play/app/__tests__/play-coinflip-panel.test.js` exists with ~28 assertions covering COINFLIP-01..03 + SHELL-01 + score-unit.
- `play/app/__tests__/play-baf-panel.test.js` exists with ~30 assertions covering BAF-01..03 + SHELL-01 + dual stale-guards + inline bafContext + score-unit + prominence.
- `play/app/__tests__/play-shell-01.test.js` FORBIDDEN array has 11 entries (was 8; added 3).
- Running `node --test play/app/__tests__/play-coinflip-panel.test.js` emits failing assertions (stub file has 40 LOC, lacks all the Wave 1/2 markup).
- Running `node --test play/app/__tests__/play-baf-panel.test.js` emits failing assertions (same reason).
- Running `node --test play/app/__tests__/play-shell-01.test.js` PASSES (no play/ file currently imports the 3 new forbidden paths).
- All other play/ tests continue to pass unchanged (play-profile-panel, play-tickets-panel, play-packs-panel, play-jackpot-wrapper, play-jackpot-shell01-regression, play-panel-stubs, play-route-structure, play-main-bootstrap, play-purchase-panel).
  </action>
  <verify>
    <automated>node --check play/app/__tests__/play-coinflip-panel.test.js && node --check play/app/__tests__/play-baf-panel.test.js && node --check play/app/__tests__/play-shell-01.test.js && node --test play/app/__tests__/play-shell-01.test.js && node --test play/app/__tests__/play-profile-panel.test.js && node --test play/app/__tests__/play-tickets-panel.test.js && node --test play/app/__tests__/play-packs-panel.test.js && node --test play/app/__tests__/play-jackpot-wrapper.test.js && node --test play/app/__tests__/play-panel-stubs.test.js && node --test play/app/__tests__/play-route-structure.test.js && node --test play/app/__tests__/play-main-bootstrap.test.js && node --test play/app/__tests__/play-purchase-panel.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `test -f play/app/__tests__/play-coinflip-panel.test.js` exits 0
    - `test -f play/app/__tests__/play-baf-panel.test.js` exits 0
    - `wc -l play/app/__tests__/play-coinflip-panel.test.js` reports >= 130 lines
    - `wc -l play/app/__tests__/play-baf-panel.test.js` reports >= 140 lines
    - `node --check play/app/__tests__/play-coinflip-panel.test.js` exits 0 (valid JS syntax)
    - `node --check play/app/__tests__/play-baf-panel.test.js` exits 0
    - `node --check play/app/__tests__/play-shell-01.test.js` exits 0
    - `grep -c "beta/app/coinflip.js" play/app/__tests__/play-shell-01.test.js` returns >= 1
    - `grep -c "beta/app/baf.js" play/app/__tests__/play-shell-01.test.js` returns >= 1
    - `grep -c "beta/components/baf-panel.js" play/app/__tests__/play-shell-01.test.js` returns >= 1
    - `grep -cE "^\s*\{ label: " play/app/__tests__/play-shell-01.test.js` returns 11 (was 8; +3)
    - `node --test play/app/__tests__/play-shell-01.test.js` exits 0 (guardrail stays green; no play/ file currently imports the new forbidden paths)
    - `node --test play/app/__tests__/play-profile-panel.test.js` exits 0 (Phase 51 regression-free)
    - `node --test play/app/__tests__/play-tickets-panel.test.js` exits 0 (Phase 52 regression-free)
    - `node --test play/app/__tests__/play-packs-panel.test.js` exits 0
    - `node --test play/app/__tests__/play-jackpot-wrapper.test.js` exits 0
    - `node --test play/app/__tests__/play-panel-stubs.test.js` exits 0
    - `node --test play/app/__tests__/play-route-structure.test.js` exits 0
    - `node --test play/app/__tests__/play-main-bootstrap.test.js` exits 0
    - `node --test play/app/__tests__/play-purchase-panel.test.js` exits 0 (Phase 53 regression-free)
    - `grep -c "COINFLIP-0" play/app/__tests__/play-coinflip-panel.test.js` returns >= 3 (all three sub-requirements referenced)
    - `grep -c "BAF-0" play/app/__tests__/play-baf-panel.test.js` returns >= 3
    - `grep -q "Score-unit discipline" play/app/__tests__/play-coinflip-panel.test.js` exits 0
    - `grep -q "Score-unit discipline" play/app/__tests__/play-baf-panel.test.js` exits 0
    - `grep -q "Pitfall 8" play/app/__tests__/play-coinflip-panel.test.js` exits 0
    - `grep -q "Pitfall 8" play/app/__tests__/play-baf-panel.test.js` exits 0
  </acceptance_criteria>
  <done>
    All three test file changes are committed: (1) play-coinflip-panel.test.js exists with assertions that currently FAIL against the 40-LOC Phase 50 stub (Wave 1 will turn most green; Wave 2 turns the INTEG-05-dependent ones green); (2) play-baf-panel.test.js exists with the same RED state; (3) play-shell-01.test.js FORBIDDEN array has 11 entries covering all previously-known wallet-tainted imports PLUS the three new Phase-54-identified paths (beta/components/baf-panel.js, beta/app/coinflip.js, beta/app/baf.js). The recursive scanner continues to PASS because no play/ file imports those forbidden paths yet. All pre-existing play/ tests stay green (no regression). Score-unit discipline comment blocks are embedded in both new test files so Wave 1 executors see the Pitfall-8 warning directly while authoring the render helpers.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mark INTEG-04 deferred in REQUIREMENTS.md per D-10</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current lines 85 INTEG-04 description, line 163 traceability row, line 186 footer)
    - .planning/phases/54-coinflip-baf/54-CONTEXT.md D-10 (deferral rationale: "COINFLIP works without it; ROADMAP success criterion 5 permits formal deferral")
    - .planning/ROADMAP.md line 159 (Phase 54 Success Criterion 5; the explicit permission path for deferral)
  </read_first>
  <action>
Perform three surgical edits to `.planning/REQUIREMENTS.md`:

## Edit 1: Line 85 -- INTEG-04 description flip from [ ] to [~]

Find exactly:
```
- [ ] **INTEG-04**: Ship or confirm coinflip recycle/history endpoint, OR document as deferred (optional — COINFLIP works without it)
```

Replace with:
```
- [~] **INTEG-04**: Ship or confirm coinflip recycle/history endpoint, OR document as deferred (deferred per Phase 54 D-10 + ROADMAP Success Criterion 5; COINFLIP functional without it via /player/:address coinflip block + /leaderboards/coinflip; revisit in future phase if player-rank-below-top-10 surfacing is needed)
```

(Also fix the pre-existing em dash in the original. The project CLAUDE.md forbids em dashes. Use `--` double-hyphens instead. If the original already contains `—` it must be replaced.)

## Edit 2: Line 163 -- Traceability table row update

Find exactly:
```
| INTEG-04 | Phase 54 (gate, optional) | Pending |
```

Replace with:
```
| INTEG-04 | Phase 54 (gate, optional) | Deferred (2026-04-24; per D-10 + ROADMAP SC5; coinflip functional without it) |
```

## Edit 3: Footer timestamp -- line 186 update

Find exactly (truncate first part for brevity; actual line starts with `*Last updated:`):
```
*Last updated: 2026-04-24 -- Phase 53 Option B landed: PURCHASE-03 validated; PURCHASE-01/02/04 deferred with scaffold + spec; SIM-01 added to INTEG block as new coordination requirement for the degenerus-sim HTTP API (gates Phase 53 live-wiring follow-up). Previous update 2026-04-23: PROFILE-02 high-difficulty clause struck (D-20, vestigial); PROFILE-05 added (D-11, D-12, Daily Activity counts); INTEG-02 reissued for Phase 51 extended /player/:address?day=N endpoint (D-15); Phase 54 per-player leaderboard endpoint renumbered from old INTEG-02 to INTEG-05*
```

Replace with:
```
*Last updated: 2026-04-24 -- Phase 54 Wave 0 kickoff: INTEG-04 marked deferred per D-10 (ROADMAP Success Criterion 5 permits; coinflip functional without recycle history endpoint via existing /player/:address coinflip block + /leaderboards/coinflip); INTEG-05-SPEC.md authored for per-player BAF score endpoint. Previous update 2026-04-24 (earlier): Phase 53 Option B landed -- PURCHASE-03 validated; PURCHASE-01/02/04 deferred with scaffold + spec; SIM-01 added to INTEG block as new coordination requirement for the degenerus-sim HTTP API (gates Phase 53 live-wiring follow-up). Earlier 2026-04-23: PROFILE-02 high-difficulty clause struck (D-20, vestigial); PROFILE-05 added (D-11, D-12, Daily Activity counts); INTEG-02 reissued for Phase 51 extended /player/:address?day=N endpoint (D-15); Phase 54 per-player leaderboard endpoint renumbered from old INTEG-02 to INTEG-05*
```

## Self-check after all three edits

- Line 85 INTEG-04 status changed from `[ ]` to `[~]` with deferral reason.
- Line 163 Traceability row shows "Deferred (2026-04-24; ...)".
- Footer timestamp references Phase 54 Wave 0 + INTEG-04 deferral.
- No em dashes (`—`) anywhere in the file.
- No emojis.
- File remains valid markdown.
- INTEG-05 row at line 164 is NOT touched (stays [ ] Pending; Wave 2 will flip to [~]/[x] depending on delivery).

The changes are surgical: ~4 lines modified in the body + 1 footer line rewritten. Total diff should be small.
  </action>
  <verify>
    <automated>test -f .planning/REQUIREMENTS.md && grep -q "\\[~\\] \\*\\*INTEG-04\\*\\*" .planning/REQUIREMENTS.md && grep -q "INTEG-04 | Phase 54 (gate, optional) | Deferred" .planning/REQUIREMENTS.md && grep -q "Phase 54 Wave 0 kickoff" .planning/REQUIREMENTS.md && grep -c "INTEG-04" .planning/REQUIREMENTS.md | awk '{exit ($1 < 3) ? 1 : 0}'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/REQUIREMENTS.md` exits 0
    - `grep -q "\\[~\\] \\*\\*INTEG-04\\*\\*" .planning/REQUIREMENTS.md` exits 0 (status flipped)
    - `grep -q "deferred per Phase 54 D-10" .planning/REQUIREMENTS.md` exits 0
    - `grep -q "INTEG-04 | Phase 54 (gate, optional) | Deferred (2026-04-24" .planning/REQUIREMENTS.md` exits 0
    - `grep -q "Phase 54 Wave 0 kickoff" .planning/REQUIREMENTS.md` exits 0
    - `grep -c "INTEG-04" .planning/REQUIREMENTS.md` returns >= 3 (description line + traceability row + any remaining references)
    - `! grep -q "—" .planning/REQUIREMENTS.md` exits 0 (no em dashes anywhere in the file)
    - `grep -q "\\[ \\] \\*\\*INTEG-05\\*\\*" .planning/REQUIREMENTS.md` exits 0 (INTEG-05 row NOT touched; still pending)
    - `grep -q "INTEG-05 | Phase 54 (gate) | Pending" .planning/REQUIREMENTS.md` exits 0 (INTEG-05 traceability row unchanged)
  </acceptance_criteria>
  <done>
    INTEG-04 is formally marked deferred in REQUIREMENTS.md with a rationale pointer to Phase 54 D-10 + ROADMAP Success Criterion 5. The traceability table row reflects the deferred status with the date. The footer timestamp documents the Phase 54 Wave 0 kickoff as the most recent change. INTEG-05 is untouched (still Pending, waiting for Wave 2 delivery). No em dashes were introduced; existing ones (if any) are replaced with `--`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave 0 test file -> future Wave 1/2 production code | Contract-grep assertions lock the shape Wave 1/2 must deliver. If Wave 1 code ships differently, the test file is the authoritative contract and must be updated deliberately. |
| INTEG-05-SPEC.md -> /home/zak/Dev/PurgeGame/database/ | The spec is the canonical contract for the database-side implementation in Wave 2. Any drift between spec and implementation must be reconciled at Wave 2 checkpoint time. |
| play-shell-01.test.js FORBIDDEN array -> play/ tree | Adding new forbidden entries tightens the fence; any future play/ file that accidentally imports one of the three newly-forbidden paths fails CI immediately (no silent wallet-taint regression). |
| REQUIREMENTS.md status changes -> project-wide tracking | Marking INTEG-04 deferred changes the coverage metrics in STATE.md (deferred count +1); verify-phase checks honor the deferral signal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Tampering | INTEG-05-SPEC.md verbatim copy from 54-RESEARCH.md Section 8 | mitigate | Task 1 acceptance criteria include grep assertions on specific phrases (roundStatus derivation, bafQuerySchema, Timeline section) so a paraphrased-or-drifted spec fails CI. |
| T-54-02 | Tampering | Wave 0 test assertions drift from Wave 1 deliverable | accept | Contract-grep tests are the authoritative shape. If Wave 1 decides a different data-bind key is better (e.g., "bounty-amount" instead of "bounty-pool"), Wave 1 MUST update the test file first, then implement -- deliberate edit, not silent drift. |
| T-54-03 | Spoofing | SHELL-01 FORBIDDEN pattern bypass via path-normalization quirk | mitigate | Regex uses `[^'"]*\/beta\/...` which matches both relative `../../beta/...` and absolute `/beta/...` paths. Node module resolution normalizes these consistently; no known bypass. |
| T-54-04 | Information disclosure | INTEG-05 response echoing :address path parameter | accept | Standard pattern already used by /player/:address and /player/:address/tickets/by-trait (Phase 51 + 52). No PII; Ethereum addresses are public by protocol. |
| T-54-05 | Denial of Service | Wave 0 test authoring regex accidentally matches forbidden path inside test file comments (false positive) | mitigate | play-shell-01.test.js explicitly excludes `play/app/__tests__/` via isInTestsDir() filter at lines 61-64; test files referencing forbidden strings in comments/docstrings are safe. |
| T-54-06 | Elevation of Privilege | REQUIREMENTS.md status edit reverts existing Phase 51/52/53 statuses | mitigate | Task 3 uses surgical Edit tool calls against specific line anchors; no full-file rewrite. Acceptance criteria re-assert INTEG-05 row unchanged (still Pending) which catches accidental cross-row edits. |
| T-54-07 | Repudiation | INTEG-05-SPEC.md as proof of backend contract when database team implements | accept | File committed to git; future PRs can reference the exact version-at-implementation. Solo-dev self-coordination per Phase 51+52 precedent. |
</threat_model>

<verification>
- Task 1 produces a 180+ line INTEG-05-SPEC.md with all required sections (Why, Endpoint, Response Schema, roundStatus Derivation, Contract-Call Map, Backend Implementation, New Schemas, Error Modes, Acceptance Criteria, Timeline, Open Questions, Confidence).
- Task 2 produces two new test files (~130 lines coinflip, ~140 lines baf) plus a 3-entry additive edit to play-shell-01.test.js FORBIDDEN array.
- Task 3 performs 3 surgical edits to REQUIREMENTS.md (INTEG-04 status flip + traceability row + footer timestamp).
- All pre-existing play/ test suites stay green across all three task edits (no regressions in Phase 50/51/52/53 coverage).
- play-shell-01.test.js continues to PASS after Edit 2.3 because no play/ file currently imports the three new forbidden paths (they are introduced to prevent future imports, not catch existing violations).
- New test files FAIL initially because the Phase 50 stubs lack Wave 1/2 markup; this is the intended RED state per Nyquist-validation discipline.
</verification>

<success_criteria>
- [ ] `.planning/phases/54-coinflip-baf/INTEG-05-SPEC.md` exists with 180+ lines and all required sections
- [ ] `play/app/__tests__/play-coinflip-panel.test.js` exists with ~28 contract-grep assertions covering COINFLIP-01..03 + SHELL-01 negatives + stale-guard + score-unit + aria-current
- [ ] `play/app/__tests__/play-baf-panel.test.js` exists with ~30 contract-grep assertions covering BAF-01..03 + SHELL-01 negatives + dual stale-guards + prominence (data-rank) + round-status (data-status) + inline bafContext + score-unit + aria-current
- [ ] `play/app/__tests__/play-shell-01.test.js` FORBIDDEN array has 11 entries (was 8, added 3: beta/components/baf-panel.js, beta/app/coinflip.js, beta/app/baf.js)
- [ ] `.planning/REQUIREMENTS.md` INTEG-04 status is `[~]` with deferral annotation + traceability table + footer all updated
- [ ] No em dashes or emojis anywhere in new/modified files
- [ ] play-shell-01.test.js continues to PASS (no play/ file currently imports the new forbidden paths)
- [ ] All pre-existing play/ test files continue to PASS (no regression)
- [ ] play-coinflip-panel.test.js and play-baf-panel.test.js FAIL (expected: stubs don't have Wave 1/2 markup)
</success_criteria>

<output>
After completion, create `.planning/phases/54-coinflip-baf/54-01-SUMMARY.md` per the standard template, including:
- Wave 0 deliverables shipped: INTEG-05-SPEC.md (N lines), play-coinflip-panel.test.js (N assertions), play-baf-panel.test.js (N assertions), play-shell-01.test.js FORBIDDEN +3
- Test state snapshot: pre-existing green count; new RED count; SHELL-01 stays green
- REQUIREMENTS.md status changes: INTEG-04 -> deferred; INTEG-05 stays Pending
- Database-side side-quest scheduled for Wave 2 start (3 atomic commits on database/main per INTEG-05-SPEC.md Timeline)
- Next action: `/gsd-execute-phase 54` Wave 1 (Plan 54-02-PLAN.md) -- pre-backend hydration of both panels
</output>

## Plan Summary

**Objective:** Wave 0 establishes the Phase 54 hard gate on backend delivery and the Nyquist test harness before any production code lands.

**Tasks:**
1. Author INTEG-05-SPEC.md per 54-RESEARCH.md Section 8 (verbatim copy with all endpoint contract details)
2. Create play-coinflip-panel.test.js + play-baf-panel.test.js (~58 contract-grep assertions total) + extend play-shell-01.test.js FORBIDDEN array with 3 new entries
3. Mark INTEG-04 deferred in REQUIREMENTS.md with D-10 + ROADMAP SC5 rationale

**Verify chain:**
- New test files author cleanly (node --check)
- play-shell-01.test.js stays green (additive FORBIDDEN entries don't match any current play/ import)
- New test files FAIL on the existing 40-LOC Phase 50 stubs (expected RED state)
- All pre-existing play/ test suites stay green
- REQUIREMENTS.md passes grep checks on status flip + traceability + footer

**Context cost estimate:** ~35% for this plan. INTEG-05-SPEC.md authoring is ~15% (long document but verbatim-copy from RESEARCH); two test files are ~15% combined; REQUIREMENTS.md surgical edits + SHELL-01 array extension are ~5%.

**Duration:** Task 1 runs in 1-2 minutes (Write). Task 2 runs in 2-3 minutes (2 creates + 1 surgical edit). Task 3 runs in 1 minute (3 small edits).
