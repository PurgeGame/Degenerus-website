---
phase: 55
plan: 03
slug: wave2-integ03-hydration-hard-gated
type: execute
wave: 2
depends_on:
  - 55-01-PLAN.md
  - 55-02-PLAN.md
files_modified:
  - play/components/decimator-panel.js
autonomous: false
requirements:
  - DECIMATOR-02
  - DECIMATOR-03
  - DECIMATOR-04
  - INTEG-03
tags:
  - phase-55
  - wave-2
  - hydration
  - hard-gated
  - integ-03
  - shell-01

must_haves:
  truths:
    - "Wave 2 does NOT merge until the INTEG-03 endpoint GET /player/:address/decimator?level=N&day=M ships in /home/zak/Dev/PurgeGame/database/ per the 55-CONTEXT.md D-09 hard-gate + INTEG-03-SPEC.md contract (3 atomic commits on database/main: feat + docs + test, mirroring INTEG-01 + INTEG-02 + INTEG-05 shape)"
    - "play/components/decimator-panel.js #refetchLevel() method is flipped from Wave 1 safe-degrade stub (404-silent fallback rendering bucket table without aria-current, stats row placeholders, payout hidden) to Wave 2 live semantics (200 response populates DECIMATOR-02 bucket + subbucket, DECIMATOR-03 full effective + weighted, DECIMATOR-04 payout pill state machine, window badge 3-state upgrade via #renderRoundStatus)"
    - "The bucket table player-row aria-current highlight UN-POPULATES when INTEG-03 returns bucket != null for the selected (player, level) pair; when bucket === null the row set remains rendered but with no aria-current (player didn't burn at this level)"
    - "The stats row Bucket + Subbucket cells render integer strings via String(value) when populated; the Effective + Weighted cells render via formatBurnie(value) per Pitfall 8 score-unit discipline; all four cells null-guard per Pitfall 7 (show '--' placeholder when value null)"
    - "The payout pill (data-bind='payout') shows authoritative 5-state machine from INTEG-03 response per Pitfall 10: closed+won (You won X.XX ETH / data-won='true'), closed+lost (Not your subbucket / data-won='false'), closed+no-burn (You didn't burn at level N / data-won='false'), open (Round in progress / data-won=''), not_eligible (No decimator round at level N / data-won='')"
    - "Wave 1 render helpers (#renderBucketTable, #renderStatsRow, #renderPayout, #renderRoundStatus) are PRESERVED; only the potential for shape-drift adjustments + verification of live response handling are the deliverables. No structural rewrites. The Wave 1 code ALREADY handles the 200-response path defensively -- it calls #renderBucketTable + #renderStatsRow + #renderPayout + #renderRoundStatus on successful parse."
    - "Score-unit discipline (Pitfall 8) continues to hold: INTEG-03 fields `effectiveAmount` and `weightedAmount` are WEI-scale BURNIE and MUST render via formatBurnie; `payoutAmount` is WEI-scale ETH and MUST render via formatEth; `bucket` + `subbucket` + `winningSubbucket` are integers and render via String()"
    - "Contract-truth bucket range constant (Pitfall 1 / Assumption A6) continues to hold: Wave 2 does NOT change the bucketRange(level) helper -- it renders 5-12 on normal levels and 2-12 on centennial (level % 100 === 0) levels regardless of what INTEG-03 returns. If INTEG-03 somehow returned bucket=1 or bucket>12, the bucket table would still render the correct contract-verified range and NOT highlight any row (INTEG-03 response would be contract-invalid)."
    - "Wave 0 test assertions that were amber (INTEG-03-dependent live-data pending) turn GREEN: aria-current on player's bucket row, payout pill data-won variants, stats row populated values. All Wave 1 tests stay green; SHELL-01 recursive guardrail stays green."
    - "Phase 50/51/52/53/54/55-01/55-02 tests all continue to pass (no regression across 13 play/ test files)"
    - "The INTEG-03 side-quest is tracked in the Wave 2 SUMMARY.md: 3 commit hashes from database/main mirroring Phase 51 INTEG-02 (d135605/dab5adf/64fe8db) + Phase 52 INTEG-01 (a46fdcb/e130547/9988887) + Phase 54 INTEG-05 (a0d4e69/6392541/08ef417) precedents"
    - "If INTEG-03 ships with response shape DRIFT from INTEG-03-SPEC.md (e.g., `winningSub` instead of `winningSubbucket`, or `roundState` instead of `roundStatus`), the checkpoint's resume signal flags this and Task 2 performs targeted field-name adjustments to render helpers WITHOUT changing the structural approach"
  artifacts:
    - path: "play/components/decimator-panel.js"
      provides: "Wave 2 verified (possibly surgical-edited) decimator-panel.js: #refetchLevel() method confirms live INTEG-03 response shape matches spec; render helpers produce DECIMATOR-02/03/04 authoritative state. Total file LOC within 5-10 of Wave 1 final (comment updates + potential field-name swaps for shape drift). No structural method rewrites. customElements.define() preserved."
      min_lines: 280
      contains: "#decimatorLevelFetchId"
  key_links:
    - from: "play/components/decimator-panel.js #refetchLevel()"
      to: "GET /player/:address/decimator?level=N&day=M (INTEG-03 live)"
      via: "fetch with JSON parsing + #renderBucketTable + #renderStatsRow + #renderPayout + #renderRoundStatus"
      pattern: "/player/.*?/decimator\\?level="
    - from: "play/components/decimator-panel.js render helpers"
      to: "INTEG-03 response fields (level, player, bucket, subbucket, effectiveAmount, weightedAmount, winningSubbucket, payoutAmount, roundStatus)"
      via: "data.{bucket, subbucket, effectiveAmount, weightedAmount, winningSubbucket, payoutAmount, roundStatus} references"
      pattern: "data\\.(bucket|subbucket|effectiveAmount|weightedAmount|winningSubbucket|payoutAmount|roundStatus)"
    - from: ".planning/phases/55-decimator/INTEG-03-SPEC.md"
      to: "/home/zak/Dev/PurgeGame/database/src/api/routes/player.ts + schemas/player.ts"
      via: "database-side handler + Zod schema implementation"
      pattern: "database/src/api/(routes|schemas)/player\\.ts"
---

<objective>
Wave 2 completes Phase 55 by wiring the decimator per-level endpoint (INTEG-03) into the decimator-panel bucket table + stats row + payout pill. This wave is HARD-GATED: the first task is a checkpoint that blocks execution until the database repo ships the INTEG-03 endpoint per `.planning/phases/55-decimator/INTEG-03-SPEC.md`.

Purpose: Close out DECIMATOR-02 (bucket/subbucket assignment per burned level), DECIMATOR-03 full (effective + weighted burn amounts per level), and DECIMATOR-04 (winning subbucket reveal + payout for resolved levels). After this plan, a selected player's bucket assignment is highlighted via aria-current in the bucket table, the stats row shows populated values, and the payout pill reflects all 5 states (closed+won / closed+lost / closed+no-burn / open / not_eligible).

**DO NOT MERGE UNTIL INTEG-03 ENDPOINT SHIPS.** The checkpoint task enforces this. The database team's delivery is documented in INTEG-03-SPEC.md (authored in Wave 0 Plan 55-01). Delivery is a solo-dev self-coordination step (user owns both website and database repos). Same pattern as Phase 51 INTEG-02 + Phase 52 INTEG-01 + Phase 54 INTEG-05 side-quests (3 atomic commits on `database/main`: feat + docs + test).

**Structural observation:** Wave 1 shipped `#refetchLevel()` with DEFENSIVE ok-path code already in place -- it calls `#renderBucketTable(level, data)` + `#renderStatsRow(data)` + `#renderPayout(data, level)` + `#renderRoundStatus(data?.roundStatus)` on 200 response. Wave 2's code change is MINIMAL in the standard case: the 404 branch's behavior is now "real error path" (vs Wave 1's "endpoint not yet shipped" meaning). If the shipped response shape drifts from INTEG-03-SPEC.md, targeted field-name swaps are required in the render helpers.

Output:
- `play/components/decimator-panel.js` gets a small surgical edit (possibly zero changes in the standard case; 1-5 line comment + field-name swaps if shape drift occurs).
- Database-side 3-atomic-commit side-quest shipped to `/home/zak/Dev/PurgeGame/database/main`.
- No new files. No other play/ file touched. No CSS changes (Wave 1 shipped all needed styling).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-decimator/55-CONTEXT.md
@.planning/phases/55-decimator/55-RESEARCH.md
@.planning/phases/55-decimator/55-PATTERNS.md
@.planning/phases/55-decimator/55-VALIDATION.md
@.planning/phases/55-decimator/INTEG-03-SPEC.md
@.planning/phases/55-decimator/55-01-PLAN.md
@.planning/phases/55-decimator/55-02-PLAN.md

<!-- Wave 1 deliverable (the file this plan verifies + possibly surgically edits) -->
@play/components/decimator-panel.js

<!-- Wave 0 tests -->
@play/app/__tests__/play-decimator-panel.test.js

<!-- Side-quest precedents (Phase 51 + 52 + 54 hard-gate templates) -->
@.planning/phases/51-profile-quests/51-03-wave2-backend-wiring-hard-gated-PLAN.md
@.planning/phases/52-tickets-packs-jackpot/52-03-PLAN.md
@.planning/phases/54-coinflip-baf/54-03-PLAN.md
@.planning/phases/52-tickets-packs-jackpot/INTEG-01-SPEC.md
@.planning/phases/51-profile-quests/INTEG-02-SPEC.md
@.planning/phases/54-coinflip-baf/INTEG-05-SPEC.md

<interfaces>
<!-- INTEG-03 live response shape (the backend must return this per INTEG-03-SPEC.md) -->
{
  "level": 20,
  "player": "0x71be63f3384f5fb98995898a86b02fb2426c5788",
  "bucket": 7,
  "subbucket": 3,
  "effectiveAmount": "5832100000000000000000",
  "weightedAmount": "833157142857142857142",
  "winningSubbucket": 3,
  "payoutAmount": "41235700000000000000",
  "roundStatus": "closed"
}

<!-- Edge cases per INTEG-03-SPEC.md: -->
-- Player burned at level, round open:     { bucket: 7, subbucket: 3, winningSubbucket: null, payoutAmount: "0", roundStatus: "open" }
-- Player burned at level, round closed+won: { bucket: 7, subbucket: 3, winningSubbucket: 3, payoutAmount: "4.12e19", roundStatus: "closed" }
-- Player burned at level, round closed+lost: { bucket: 7, subbucket: 4, winningSubbucket: 3, payoutAmount: "0", roundStatus: "closed" }
-- Player did NOT burn (any state):         { bucket: null, subbucket: null, effectiveAmount: "0", weightedAmount: "0", payoutAmount: "0", roundStatus: "open"|"closed" }
-- Level has NO decimator round:            { bucket: null, subbucket: null, all zeros, roundStatus: "not_eligible" }

<!-- 400 error shape (Zod validation failure): -->
{ "statusCode": 400, "error": "Bad Request", "message": "..." }

<!-- 404 error shape (day not found): -->
{ "statusCode": 404, "error": "Not Found", "message": "day_not_found", "day": M }

<!-- Wave 1 #refetchLevel() current state (from Plan 55-02 Task 1) -->

async #refetchLevel() {
  const addr  = get('replay.player');
  const level = get('replay.level');
  const day   = get('replay.day');
  const token = ++this.#decimatorLevelFetchId;

  if (!addr || level == null) return;

  try {
    const url = day != null
      ? `${API_BASE}/player/${addr}/decimator?level=${encodeURIComponent(level)}&day=${encodeURIComponent(day)}`
      : `${API_BASE}/player/${addr}/decimator?level=${encodeURIComponent(level)}`;
    const res = await fetch(url);
    if (token !== this.#decimatorLevelFetchId) return;

    if (!res.ok) {
      // INTEG-03 safe-degrade stub (Wave 1 pre-backend or Wave 2 error path):
      // render bucket table WITHOUT aria-current, clear stats row placeholders,
      // hide payout pill. Wave 2 on 200 response upgrades these to live data.
      this.#renderBucketTable(level, null);
      this.#renderStatsRow(null);
      this.#renderPayout(null, level);
      return;
    }

    const data = await res.json();
    if (token !== this.#decimatorLevelFetchId) return;

    this.#renderBucketTable(level, data);
    this.#renderStatsRow(data);
    this.#renderPayout(data, level);
    this.#renderRoundStatus(data?.roundStatus);
  } catch (err) {
    if (token === this.#decimatorLevelFetchId) {
      this.#renderBucketTable(level, null);
      this.#renderStatsRow(null);
      this.#renderPayout(null, level);
    }
  }
}

<!-- Wave 2 target state (standard case -- comment adjustment only, ok-path already correct from Wave 1) -->

async #refetchLevel() {
  const addr  = get('replay.player');
  const level = get('replay.level');
  const day   = get('replay.day');
  const token = ++this.#decimatorLevelFetchId;

  if (!addr || level == null) return;

  try {
    const url = day != null
      ? `${API_BASE}/player/${addr}/decimator?level=${encodeURIComponent(level)}&day=${encodeURIComponent(day)}`
      : `${API_BASE}/player/${addr}/decimator?level=${encodeURIComponent(level)}`;
    const res = await fetch(url);
    if (token !== this.#decimatorLevelFetchId) return;

    if (!res.ok) {
      // INTEG-03 error path (400/500): fall back to safe-degrade rendering so
      // the panel degrades gracefully. The bucket table still shows the correct
      // range (per contract-truth bucketRange(level)) just without aria-current.
      // Note: INTEG-03 returns 200 even when the player has no burn at this level
      // (bucket=null case); only true errors reach this branch post-Wave-2.
      this.#renderBucketTable(level, null);
      this.#renderStatsRow(null);
      this.#renderPayout(null, level);
      return;
    }

    const data = await res.json();
    if (token !== this.#decimatorLevelFetchId) return;

    this.#renderBucketTable(level, data);     // DECIMATOR-02 / D-03
    this.#renderStatsRow(data);                // DECIMATOR-02 / DECIMATOR-03
    this.#renderPayout(data, level);           // DECIMATOR-04 / Pitfall 10
    this.#renderRoundStatus(data?.roundStatus);// Upgrade D-05 badge from binary
  } catch (err) {
    if (token === this.#decimatorLevelFetchId) {
      this.#renderBucketTable(level, null);
      this.#renderStatsRow(null);
      this.#renderPayout(null, level);
    }
  }
}

<!-- The only code change in the standard case is in the comment inside the
     `if (!res.ok)` branch: "safe-degrade stub (Wave 1 pre-backend...)" becomes
     "error path (400/500): fall back to safe-degrade rendering..." because the
     behavior semantics shift but the code is identical. -->

<!-- Shape-drift scenarios + remediation (if checkpoint signals shape-drift): -->

Scenario A: `roundStatus` renamed to `roundState`
  Remediate:
    Locate: `this.#renderRoundStatus(data?.roundStatus);`
    Replace: `this.#renderRoundStatus(data?.roundState);  // backend shipped as roundState`

Scenario B: `winningSubbucket` renamed to `winningSub`
  Remediate:
    Locate in #renderBucketTable: `data?.winningSubbucket ?? null`
    Replace: `data?.winningSub ?? null  // backend shipped as winningSub`

Scenario C: `effectiveAmount` / `weightedAmount` renamed (e.g., `effective` + `weighted`)
  Remediate in #renderStatsRow:
    Locate: `data.effectiveAmount && data.effectiveAmount !== '0'`
    Replace: `data.effective && data.effective !== '0'`
    Same for weightedAmount.

Scenario D: `payoutAmount` renamed to `payout`
  Remediate in #renderPayout:
    Locate: `data.payoutAmount && data.payoutAmount !== '0'`
    Replace: `data.payout && data.payout !== '0'`

Scenario E: `bucket` / `subbucket` renamed (e.g., `bucketIndex` + `subBucketIndex`)
  Remediate in #renderStatsRow + #renderBucketTable + #renderPayout:
    Same pattern; find data.bucket references and swap to new name.

<!-- Side-quest pattern (3 atomic commits on database/main mirroring INTEG-05) -->

Commit 1: feat(api): add GET /player/:address/decimator endpoint (INTEG-03)
  - database/src/api/routes/player.ts -- add handler per INTEG-03-SPEC.md "Proposed Backend Implementation" (~110 LOC including day-resolution + 5-table joins + 2-path payout resolution)
  - database/src/api/schemas/player.ts -- add decimatorQuerySchema + decimatorPlayerResponseSchema
  - Imports: decimatorBurns, decimatorBucketTotals, decimatorWinningSubbuckets, decimatorRounds, decimatorClaims tables + eq, and, lte, sql from drizzle

Commit 2: docs(openapi): document /player/:address/decimator (INTEG-03)
  - database/src/api/openapi.json -- add endpoint definition with response schema
  - Follow existing pattern from /player/:address/baf (Phase 54 INTEG-05) + /player/:address/tickets/by-trait (Phase 52 INTEG-01)

Commit 3: test(api): cover INTEG-03 happy path, bucket null, empty level, not_eligible, closed, day-scoped
  - database/src/api/__tests__/player-decimator.test.ts (new vitest file)
  - Tests: player with burn + open round returns bucket 5-12 + subbucket; player with burn + closed+won returns winningSubbucket + non-zero payoutAmount; player with burn + closed+lost returns payoutAmount='0'; player without burn returns bucket=null; level with no decimator round returns roundStatus='not_eligible'; day-scoping via ?day=M respects blockNumber <= endBlock filter

Precedents:
- INTEG-02 (Phase 51): d135605 feat, dab5adf docs, 64fe8db test
- INTEG-01 (Phase 52): a46fdcb feat, e130547 docs, 9988887 test
- INTEG-05 (Phase 54): a0d4e69 feat, 6392541 docs, 08ef417 test

INTEG-03 is slightly more complex than INTEG-05 (5 tables joined + 2-path payout resolution via decimator_claims.ethAmount OR derived pool*playerBurn/totalBurn; day-scoping; roundStatus 3-way derivation). Budget 10-15 minutes for careful implementation.
</interfaces>
</context>

<read_first>
- .planning/phases/55-decimator/55-CONTEXT.md (D-09 INTEG-03 requirement + D-10 non-participant handling; hard-gate)
- .planning/phases/55-decimator/55-RESEARCH.md Section 8 (INTEG-03 Endpoint Spec Draft lines 372-727; the authoritative contract)
- .planning/phases/55-decimator/INTEG-03-SPEC.md (Wave 0 output; the exact spec the backend reads)
- .planning/phases/55-decimator/55-02-PLAN.md Task 1 action section (how Wave 1 wrote #refetchLevel with safe-degrade stub; Wave 2 surgically adjusts the comment semantics)
- .planning/phases/55-decimator/55-RESEARCH.md Section 14 Q8 lines 1720-1724 (Wave 2 shape-drift remediation pattern -- identical to INTEG-05's Wave 2 approach)
- play/components/decimator-panel.js (the Wave 1 file; locate #refetchLevel() method)
- play/app/__tests__/play-decimator-panel.test.js (all assertions must stay green after this wave)
- .planning/phases/54-coinflip-baf/54-03-PLAN.md Task 1 (CHECKPOINT template; direct clone for Phase 55 INTEG-03 gate)
- .planning/phases/52-tickets-packs-jackpot/52-03-PLAN.md Task 1 (secondary checkpoint template; INTEG-01 verification recipe)
- .planning/phases/51-profile-quests/51-03-wave2-backend-wiring-hard-gated-PLAN.md Task 1 (tertiary checkpoint template; INTEG-02 precedent)
- /home/zak/Dev/PurgeGame/database/src/api/routes/player.ts (check for existing INTEG-01 + INTEG-05 handlers; INTEG-03 handler places immediately after INTEG-05 handler)
- /home/zak/Dev/PurgeGame/database/src/api/schemas/player.ts (where decimatorQuerySchema + decimatorPlayerResponseSchema append)
- /home/zak/Dev/PurgeGame/database/src/db/schema/decimator.ts (ALL five decimator tables: decimator_burns, decimator_bucket_totals, decimator_winning_subbuckets, decimator_rounds, decimator_claims; verified indexed per RESEARCH Section 7)
- CLAUDE.md (no em dashes, no emojis)
</read_first>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1 (CHECKPOINT): Verify INTEG-03 endpoint ships before Wave 2 merge</name>
  <what-built>
    Wave 0 (Plan 55-01) wrote INTEG-03-SPEC.md into the Phase 55 directory with the full contract: path + query schema, response shape, roundStatus derivation logic, proposed Fastify handler + Zod schema TypeScript, acceptance criteria, 3-commit timeline.

    Wave 1 (Plan 55-02) shipped play/components/decimator-panel.js with a 404-tolerant INTEG-03 fetch path: #refetchLevel() calls `/player/${addr}/decimator?level=${level}` (optional `&day=${day}`) but tolerates any non-OK response by rendering the safe-degrade state (bucket table without aria-current, stats row '--' placeholders, payout pill hidden). The ok-path already does the right thing defensively (calls #renderBucketTable + #renderStatsRow + #renderPayout + #renderRoundStatus with live response data).

    Wave 2 flips the semantics: the endpoint is expected to ship, so any non-OK response is now a real error (not an absent endpoint). The comment in #refetchLevel() may need adjustment from "Wave 1 safe-degrade stub" language to "INTEG-03 error path" language. If the shipped response shape drifts from INTEG-03-SPEC.md, targeted field-name swaps are required in the render helpers.

    This checkpoint enforces the hard gate from CONTEXT.md D-09 and the Timeline section of INTEG-03-SPEC.md: "DECIMATOR-02/03/04 hydration lands only after the database team confirms the endpoint is shipped."

    Before proceeding, the user (or the agent after switching repos) must confirm the INTEG-03 endpoint is live.

    The checkpoint supports three terminal states:
    - `endpoint-shipped` -- the database repo has shipped the endpoint per spec (3 atomic commits; vitest pass); Wave 2 Task 2 proceeds with comment adjustment.
    - `endpoint-shipped shape-drift -- [field renames]` -- the endpoint is shipped but with renamed fields; Task 2 performs targeted field-name swaps in the render helpers.
    - `deferred -- [reason]` -- the endpoint is not yet ready; Wave 2 remains blocked. The user switches repos, implements per the spec, and returns.
  </what-built>
  <how-to-verify>
    **Setup: confirm database-side readiness**

    1. Confirm the 3 atomic commits are on database/main. From any shell:
       ```
       cd /home/zak/Dev/PurgeGame/database
       git log --oneline --all | head -20
       ```
       Expect 3 recent commits mirroring INTEG-05 (Phase 54) + INTEG-02 (Phase 51) + INTEG-01 (Phase 52) shape:
       - feat(api): add GET /player/:address/decimator endpoint (INTEG-03)
       - docs(openapi): document /player/:address/decimator (INTEG-03)
       - test(api): cover INTEG-03 happy path, bucket null, empty level, not_eligible, closed, day-scoped

    2. Verify the new vitest file passes:
       ```
       cd /home/zak/Dev/PurgeGame/database
       npx vitest run src/api/__tests__/player-decimator.test.ts 2>&1 | tail -20
       ```
       Expect all tests green.

    3. Start (or verify) the Fastify dev server at http://localhost:3000.

    4. Query available players to pick a test address:
       ```
       curl -s http://localhost:3000/replay/players | python3 -c "import json,sys; d=json.load(sys.stdin); print('\\n'.join(d['players'][:5]))"
       ```

    5. Probe the INTEG-03 endpoint with a player + level that has indexed decimator data. Use the sim's available levels as a starting point. For a known-populated level (e.g., any level where burns exist per `decimator_burns` table):
       ```
       PLAYER=0xfabb0ac9d68b0b445fb7357272ff202c5651694a
       LEVEL=5
       curl -s "http://localhost:3000/player/$PLAYER/decimator?level=$LEVEL" | python3 -m json.tool
       ```

    6. Verify the response includes the nine top-level keys per INTEG-03-SPEC.md:
       - [ ] `level` (echo of query `level`)
       - [ ] `player` (echo of path `:address`, lowercased)
       - [ ] `bucket` (integer 2-12 or null if no burn)
       - [ ] `subbucket` (integer 0-to-bucket-1 or null if no burn)
       - [ ] `effectiveAmount` (wei-encoded BURNIE decimal string; "0" if no burn)
       - [ ] `weightedAmount` (wei-encoded BURNIE decimal string; "0" if no burn)
       - [ ] `winningSubbucket` (integer or null)
       - [ ] `payoutAmount` (wei-encoded ETH decimal string; "0" if no win)
       - [ ] `roundStatus` (one of "open" | "closed" | "not_eligible")

    7. Spot-check edge cases:

       **Edge: player burned at level with round open**
       ```
       curl -s "http://localhost:3000/player/$PLAYER/decimator?level=5" | python3 -m json.tool
       ```
       Expect: `bucket: 5-12`, `subbucket: 0 to bucket-1`, `effectiveAmount != "0"`, `weightedAmount != "0"`, `winningSubbucket: null`, `payoutAmount: "0"`, `roundStatus: "open"` (if round not yet resolved).

       **Edge: player burned at level with round closed (win or lose)**
       Pick a level where `decimator_rounds.resolved = true`:
       ```
       curl -s "http://localhost:3000/player/$PLAYER/decimator?level=3" | python3 -m json.tool
       ```
       Expect: `winningSubbucket != null`, `roundStatus: "closed"`. If `subbucket === winningSubbucket` then `payoutAmount != "0"`; else `payoutAmount: "0"`.

       **Edge: player has no burn at this level**
       ```
       SOME_PLAYER=0x71be63f3384f5fb98995898a86b02fb2426c5788
       curl -s "http://localhost:3000/player/$SOME_PLAYER/decimator?level=100" | python3 -m json.tool
       ```
       Expect: `bucket: null`, `subbucket: null`, `effectiveAmount: "0"`, `weightedAmount: "0"`, `payoutAmount: "0"`. `roundStatus` = `"not_eligible"` if no round exists at level 100, else `"open"` or `"closed"` depending on state.

       **Edge: centennial level (level % 100 === 0) bucket range check**
       If a decimator round exists at level 100, verify `bucket` is in range [2, 12] (not just [5, 12]):
       ```
       curl -s "http://localhost:3000/player/$PLAYER/decimator?level=100" | python3 -m json.tool
       ```
       Expected: bucket can be 2, 3, or 4 at level 100 (not just 5+ as at normal levels).

       **Edge: missing level query param (400)**
       ```
       curl -s "http://localhost:3000/player/$PLAYER/decimator" -w "\n%{http_code}\n"
       ```
       Expect: HTTP 400 with Zod validation error JSON.

       **Edge: day-scoped historical query**
       ```
       curl -s "http://localhost:3000/player/$PLAYER/decimator?level=5&day=1" | python3 -m json.tool
       ```
       Expect: full payload with block-scoped data (state as of end-of-day-1 block).

       **Edge: day that doesn't exist (404)**
       ```
       curl -s "http://localhost:3000/player/$PLAYER/decimator?level=5&day=99999" -w "\n%{http_code}\n"
       ```
       Expect: HTTP 404 with `day_not_found` message.

    8. Confirm the response matches the Wave 1 decimator-panel render helper expectations:
       - `data.bucket` is number (2-12) or null
       - `data.subbucket` is number (0 to bucket-1) or null
       - `data.effectiveAmount` is decimal string (wei-scale BURNIE)
       - `data.weightedAmount` is decimal string (wei-scale BURNIE)
       - `data.winningSubbucket` is number or null
       - `data.payoutAmount` is decimal string (wei-scale ETH)
       - `data.roundStatus` is one of the 3 enum values

    9. If the shipped shape DIFFERS from INTEG-03-SPEC.md (e.g., `winningSub` instead of `winningSubbucket`, or `roundState` instead of `roundStatus`, or `bucketIndex` instead of `bucket`):
       - Document the drift in the resume signal: `endpoint-shipped shape-drift -- [field renames]`
       - Wave 2 Task 2 will adjust the #renderBucketTable + #renderStatsRow + #renderPayout + #renderRoundStatus calls to match.

    **Resume options:**

    - If all checks pass and the shipped shape matches the spec, resume with signal `endpoint-shipped` (optionally paste the 3 commit hashes from database/main).
    - If the shipped shape drifts from the spec, resume with `endpoint-shipped shape-drift -- [field renames]` so Task 2 adjusts the render calls.
    - If the endpoint is NOT ready, resume with `deferred -- [reason]` (e.g., `deferred -- database-side not implemented; switching to /database/ to ship`). Wave 2 stays blocked until the endpoint lands.

    **Estimated time:** 10-15 minutes for the side-quest (3 commits on database/main per RESEARCH Section 8 lines 700-711 precedent estimate); plus 3-5 minutes for the curl verification of all edge cases.
  </how-to-verify>
  <resume-signal>Type "endpoint-shipped" (optionally append " shape-drift -- [field renames]" if INTEG-03 ships with a different response shape, or paste the 3 commit hashes from database/main), or "deferred -- [reason]".</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Surgical edit to #refetchLevel() comments + adjust render helpers if INTEG-03 ships with shape drift</name>
  <files>play/components/decimator-panel.js</files>
  <read_first>
    - play/components/decimator-panel.js (Wave 1 file; locate #refetchLevel() method; confirm the Wave-1 safe-degrade comment is the only edit in the standard case)
    - Task 1 checkpoint resume signal (pass with/without shape-drift, OR deferred -- if deferred, Task 2 is skipped entirely)
    - .planning/phases/55-decimator/INTEG-03-SPEC.md (the spec; if the shipped shape differs, re-read the original field names to understand which fields map to the new names)
    - play/app/__tests__/play-decimator-panel.test.js (all assertions must stay green; Wave 2 turns the INTEG-03-dependent live-data assertions green too)
    - .planning/phases/54-coinflip-baf/54-03-PLAN.md Task 2 (Phase 54 precedent for this exact shape; clone the action approach)
  </read_first>
  <action>
Wave 2's real code change is minimal. The Wave 1 #refetchLevel() was written defensively to handle BOTH a 404 (silent safe-degrade fallback) AND a successful response (parse + render all four helpers). In Wave 2, the only things that may change are:

1. The comment inside the `if (!res.ok)` branch is adjusted to reflect the new meaning (no longer "Wave 1 safe-degrade stub pre-backend"; now "INTEG-03 error path 400/500").
2. IF the Task 1 resume signal included `shape-drift`, the #renderBucketTable / #renderStatsRow / #renderPayout / #renderRoundStatus call sites are adjusted to match the shipped field names.
3. Everything else stays unchanged (connectedCallback, disconnectedCallback, #refetchPlayer, all other render helpers, TEMPLATE, inline bucketRange + formatTimeMultiplier, customElements.define).

## Standard case (no shape drift) -- comment-only edit

Use the Edit tool. Locate the exact text in `play/components/decimator-panel.js`:

```javascript
    if (!res.ok) {
      // INTEG-03 safe-degrade stub (Wave 1 pre-backend or Wave 2 error path):
      // render bucket table WITHOUT aria-current, clear stats row placeholders,
      // hide payout pill. Wave 2 on 200 response upgrades these to live data.
      this.#renderBucketTable(level, null);
      this.#renderStatsRow(null);
      this.#renderPayout(null, level);
      return;
    }
```

Replace with:

```javascript
    if (!res.ok) {
      // INTEG-03 error path (400/500): fall back to safe-degrade rendering so
      // the panel degrades gracefully. The bucket table still shows the correct
      // range (per contract-truth bucketRange(level)) just without aria-current.
      // Note: INTEG-03 returns 200 even when the player has no burn at this
      // level (bucket=null case); only true errors reach this branch post-Wave-2.
      this.#renderBucketTable(level, null);
      this.#renderStatsRow(null);
      this.#renderPayout(null, level);
      return;
    }
```

That is the only text change in the standard case. The ok-path at lines below it already handles the live response correctly (parses JSON, passes `data` to all four render helpers).

## Shape-drift case (resume signal included `shape-drift -- [field renames]`)

If the database repo shipped INTEG-03 with renamed fields (e.g., `roundState` instead of `roundStatus`, or `winningSub` instead of `winningSubbucket`), apply targeted adjustments as follows:

### Example: if `roundStatus` was renamed to `roundState`:

Locate:
```javascript
    this.#renderRoundStatus(data?.roundStatus);
```

Replace:
```javascript
    this.#renderRoundStatus(data?.roundState);  // backend shipped as roundState
```

AND locate in `#renderPayout`:
```javascript
  if (data.roundStatus === 'not_eligible') { ... }
  if (data.roundStatus === 'open') { ... }
```

Replace with `data.roundState === 'not_eligible'` etc. (all `data.roundStatus` references in #renderPayout).

### Example: if `winningSubbucket` was renamed to `winningSub`:

Locate in #renderBucketTable:
```javascript
  const winningSub     = data?.winningSubbucket ?? null;
```

Replace:
```javascript
  const winningSub     = data?.winningSub ?? null;  // backend shipped as winningSub
```

### Example: if `effectiveAmount` / `weightedAmount` renamed to `effective` / `weighted`:

Locate in #renderStatsRow:
```javascript
  this.#setText('effective',
    data.effectiveAmount && data.effectiveAmount !== '0'
      ? `${formatBurnie(data.effectiveAmount)} BURNIE`
      : '0');
  this.#setText('weighted',
    data.weightedAmount && data.weightedAmount !== '0'
      ? `${formatBurnie(data.weightedAmount)} BURNIE`
      : '0');
```

Replace `data.effectiveAmount` with `data.effective` and `data.weightedAmount` with `data.weighted` (add comment noting the shipped name).

### Example: if `payoutAmount` renamed to `payout`:

Locate in #renderPayout:
```javascript
  if (data.payoutAmount && data.payoutAmount !== '0') { ... }
```

Replace `data.payoutAmount` with `data.payout` (all references in #renderPayout).

### Example: if `bucket` / `subbucket` renamed:

Rare; would cascade into #renderStatsRow + #renderBucketTable + #renderPayout. Apply the same find-and-swap pattern to all `data.bucket` and `data.subbucket` references.

Apply the same pattern to any other field renames. Add an inline comment (`// backend shipped as ...`) for each swapped reference so future readers can trace the drift.

## Skip case (Task 1 resolved as `deferred`)

If Task 1 resume signal was `deferred -- [reason]`, Task 2 is SKIPPED entirely. The Wave 1 decimator-panel.js remains unchanged; the 404-tolerant safe-degrade continues to work. Phase 55 cannot close out DECIMATOR-02 + 03 full + 04 validation until the endpoint ships in a future cycle. Record the deferral in the 55-03-SUMMARY.md and escalate to the user.

## Self-check after edit (when Task 1 was endpoint-shipped)

- `play/components/decimator-panel.js` LOC within 2-5 lines of Wave 1 final (comment-block adjustment only; possibly 5-10 line drift if shape-drift required multiple field swaps).
- The phrase "Wave 1 pre-backend" or "safe-degrade stub (Wave 1 ..." is GONE from the file.
- The phrase "INTEG-03 error path (400/500)" is present (or matching updated comment).
- The #refetchLevel() method still calls `this.#renderBucketTable(level, data)` + `this.#renderStatsRow(data)` + `this.#renderPayout(data, level)` + `this.#renderRoundStatus(data?.roundStatus)` (or the shape-drift-adjusted names).
- The ok-path unchanged from Wave 1 (it was already correct).
- Contract-truth bucketRange(level) helper UNCHANGED (no drift-induced edits here; it depends only on level).
- `node --test play/app/__tests__/play-decimator-panel.test.js` all green (the INTEG-03-dependent Wave 0 assertions that were amber turn green post-Wave-2).
- `node --test play/app/__tests__/play-shell-01.test.js` green.
- All prior phase tests green (13 play/ test files total: 12 prior + 1 play-decimator-panel).

## Live-browser verification (brief manual check after code edit)

Open http://localhost:8080/play/ after both website + database dev servers are running. Pick a player + level known to have decimator data. Scrub the day scrubber to a day where the level was burned. The bucket table should show the player's bucket row highlighted via aria-current. Scrub to a level where the round is closed AND the player won -- the payout pill should show "You won X.XX ETH" with green accent. Scrub to a level where the player lost -- "Not your subbucket" with muted color. Scrub to a level with no decimator round -- "No decimator round at level N" neutral.

If aria-current is missing or the payout pill shows the wrong state, inspect the DevTools Network tab for the `/player/${addr}/decimator?level=N` request. Verify the response shape matches INTEG-03-SPEC.md; if it drifts, return to Task 2 with the shape-drift adjustments.
  </action>
  <verify>
    <automated>node --check play/components/decimator-panel.js && node --test play/app/__tests__/play-decimator-panel.test.js && node --test play/app/__tests__/play-shell-01.test.js && node --test play/app/__tests__/play-coinflip-panel.test.js && node --test play/app/__tests__/play-baf-panel.test.js && node --test play/app/__tests__/play-profile-panel.test.js && node --test play/app/__tests__/play-tickets-panel.test.js && node --test play/app/__tests__/play-packs-panel.test.js && node --test play/app/__tests__/play-jackpot-wrapper.test.js && node --test play/app/__tests__/play-jackpot-shell01-regression.test.js && node --test play/app/__tests__/play-panel-stubs.test.js && node --test play/app/__tests__/play-route-structure.test.js && node --test play/app/__tests__/play-main-bootstrap.test.js && node --test play/app/__tests__/play-purchase-panel.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check play/components/decimator-panel.js` exits 0
    - `wc -l play/components/decimator-panel.js` reports >= 280 (Wave 1 baseline preserved; minor drift acceptable)
    - `! grep -q "Wave 1 pre-backend" play/components/decimator-panel.js` exits 0 (stub comment language removed)
    - `! grep -q "safe-degrade stub (Wave 1" play/components/decimator-panel.js` exits 0 (stub comment language removed)
    - `grep -q "INTEG-03 error path" play/components/decimator-panel.js` exits 0 (new comment in place, OR matching updated comment reflecting post-Wave-2 semantics)
    - `grep -c "this.#renderBucketTable(level, data)" play/components/decimator-panel.js` returns 1 (ok-path still calls render with data)
    - `grep -c "this.#renderStatsRow(data)" play/components/decimator-panel.js` returns 1 (ok-path preserved)
    - `grep -c "this.#renderPayout(data, level)" play/components/decimator-panel.js` returns 1 (ok-path preserved)
    - `grep -cE "this\\.#renderRoundStatus\\(data\\?\\.\\w+\\)" play/components/decimator-panel.js` returns 1 (ok-path renders round status; field name may drift)
    - `grep -c "/player/.*?/decimator\\?level=" play/components/decimator-panel.js` returns >= 1 (fetch URL unchanged)
    - `grep -c "#decimatorLevelFetchId" play/components/decimator-panel.js` returns >= 3 (stale-guard preserved)
    - `grep -c "formatBurnie" play/components/decimator-panel.js` returns >= 2 (score rendering preserved)
    - `grep -c "formatEth" play/components/decimator-panel.js` returns >= 1 (payout render preserved)
    - `grep -q "DECIMATOR_BUCKET_BASE = 12" play/components/decimator-panel.js` exits 0 (contract-truth preserved)
    - `grep -q "bucketRange" play/components/decimator-panel.js` exits 0 (helper preserved)
    - `! grep -q "levelStartTime\\|hours-remaining" play/components/decimator-panel.js` exits 0 (Pitfall 4 preserved)
    - `node --test play/app/__tests__/play-decimator-panel.test.js` exits 0 (all Wave 0 assertions green now)
    - `node --test play/app/__tests__/play-shell-01.test.js` exits 0 (recursive scan still green)
    - All 12 prior play/ test files continue to pass (play-coinflip-panel, play-baf-panel, play-profile-panel, play-tickets-panel, play-packs-panel, play-jackpot-wrapper, play-jackpot-shell01-regression, play-panel-stubs, play-route-structure, play-main-bootstrap, play-purchase-panel, plus the Phase 54 + 55 additions)
    - Live-browser UAT observation: aria-current highlight appears on player's bucket row; payout pill renders correct 5-state; terminal section visible when player has terminal burns; window badge upgrades to 3-state (OPEN/CLOSED/NOT ELIGIBLE)
    - `! grep -q $'\u2014' play/components/decimator-panel.js` exits 0 (no em dashes)
  </acceptance_criteria>
  <done>
    `play/components/decimator-panel.js` #refetchLevel() comment block is updated from Wave-1 stub language ("safe-degrade stub (Wave 1 pre-backend...)") to Wave-2 "error path (400/500)" language. If the shipped INTEG-03 response schema drifted from INTEG-03-SPEC.md, the #renderBucketTable / #renderStatsRow / #renderPayout / #renderRoundStatus field references are adjusted to match the shipped field names with inline `// backend shipped as ...` comments. The ok-path behavior is unchanged (it was already defensive-correct in Wave 1). All 13 play/ test suites continue to pass, and the INTEG-03-dependent assertions that were amber in Wave 1 turn GREEN in Wave 2. DECIMATOR-02 is fully validated: aria-current highlights the player's bucket row when they burned at the level. DECIMATOR-03 full is validated: stats row shows populated effective + weighted BURNIE values. DECIMATOR-04 is validated: payout pill state machine covers all 5 states (closed+won / closed+lost / closed+no-burn / open / not_eligible). Window badge upgrades from binary to 3-state via #renderRoundStatus from INTEG-03. SHELL-01 recursive guardrail continues to hold (14 FORBIDDEN entries; no new forbidden imports).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| INTEG-03 response -> decimator-panel render helpers | Wave 2 binds to the shipped response shape. If the shipped shape differs from INTEG-03-SPEC.md, the shape-drift branch in Task 2 action re-maps field names. All render helpers remain textContent-based (no XSS surface). |
| database/main commits -> solo-dev verification | Checkpoint requires the user to confirm 3 atomic commits landed (feat + docs + test) mirroring Phase 51+52+54 precedent; no external team attests. Repudiation-accept per prior precedent. |
| /home/zak/Dev/PurgeGame/database vitest -> endpoint correctness | Commit 3 of the side-quest adds vitest tests covering happy path + bucket null + empty level + not_eligible + closed + day-scoped; tests pass before the endpoint is considered shipped. |
| decimator-panel error path -> user-visible degradation | If INTEG-03 returns 500 post-ship, the panel falls back to safe-degrade (bucket table without aria-current, stats row placeholders, payout hidden). The window badge + context row + terminal section remain functional (sourced from extended-player). Error is silent (no console throw that surfaces to the user) per T-55-13. |
| Contract-truth bucketRange(level) -> bucket table structure | Invariant preserved: bucket table renders 5-12 on normal levels and 2-12 on centennial levels regardless of what INTEG-03 returns. If backend somehow returns bucket=1 or bucket>12, the table still renders correct contract-verified range and simply doesn't highlight any row (that response would be contract-invalid and surface as an upstream bug, not a frontend issue). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-20 | Tampering | INTEG-03 response before it reaches render helpers | mitigate | #decimatorLevelFetchId stale-guard with 2 token checks (post-fetch + post-json) catches interleaved responses during rapid scrub. Pattern inherited from Wave 1 / baf-panel precedent. |
| T-55-21 | Spoofing | INTEG-03 roundStatus field spoofed with unknown enum value | mitigate | #renderRoundStatus LABELS map has only open/closed/not_eligible keys; unknown falls to `'--'` text + empty data-status attribute (no CSS match = base pill styling). Fails safe. |
| T-55-22 | Spoofing | Compromised backend returns HTML-shaped player or amount field | mitigate | All render helpers use textContent for dynamic writes. data.player is echoed via server path + addressParamSchema validation. data.bucket/subbucket/winningSubbucket are integers rendered via String(). data amounts go through formatBurnie/formatEth which emit plain-decimal strings. |
| T-55-23 | Information disclosure | INTEG-03 error paths emit status codes via console | accept | Console warnings are dev-mode debug; DOM fallback text is generic (safe-degrade placeholders). No PII leaked. Same posture as T-54-23. |
| T-55-24 | Denial of Service | Rapid level-scrub triggers N INTEG-03 fetches | accept | #decimatorLevelFetchId counter invalidates stale responses but does not rate-limit outbound requests. Web scale: each response is < 1KB. No dedup helper needed per RESEARCH Section 10. |
| T-55-25 | Elevation of Privilege | Shape-drift fix applied without re-running test suite | mitigate | Task 2 acceptance criteria run all 13 play/ test files; any regression in existing tests (e.g., #refetchLevel identifier removed or key render helper call removed) fails CI before commit. |
| T-55-26 | Repudiation | Database-repo INTEG-03 commits as proof of delivery | accept | Task 1 checkpoint requires git log verification against the 3-atomic-commit shape; solo-dev self-coordination per Phase 51+52+54 precedent. |
| T-55-27 | Tampering | Wave 2 edit accidentally breaks Wave 1 safe-degrade branch | mitigate | The surgical edit only touches comment text inside the `if (!res.ok)` branch; the `#renderBucketTable(level, null); #renderStatsRow(null); #renderPayout(null, level); return;` logic stays intact. Acceptance criteria grep for the presence of `INTEG-03 error path` comment AND absence of "Wave 1 pre-backend" language. |
| T-55-28 | Tampering | Shape-drift adjustment breaks the contract-truth bucketRange | mitigate | bucketRange(level) helper is pure-derivation (depends only on level); shape-drift in INTEG-03 response cannot affect it. Acceptance criteria grep `DECIMATOR_BUCKET_BASE = 12` to confirm preservation. |
</threat_model>

<verification>
- Task 1 checkpoint pauses until INTEG-03 is confirmed live via curl + vitest (or is deferred with a clear reason).
- Task 2 performs 1-5 surgical edits (1 comment swap in the standard case; 2-5 field-name swaps if shape-drift was flagged).
- After Task 2, all Wave 0 test assertions turn green (including the previously-amber INTEG-03-dependent ones).
- Wave 1 safe-degrade behavior continues to work on error paths (400/500).
- Contract-truth bucketRange(level) helper unchanged.
- Time-remaining drop (Pitfall 4) preserved.
- SHELL-01 guardrail stays green (14 FORBIDDEN entries); no new play/ imports.
- Phase 50 + 51 + 52 + 53 + 54 + 55-01 + 55-02 tests continue to pass.
- The dev browser at http://localhost:8080/play/ renders the decimator panel with:
  (a) window badge 3-state (OPEN green / CLOSED dim / NOT ELIGIBLE muted);
  (b) activity score cross-reference updating on day-scrub;
  (c) 4-cell stats row populated from INTEG-03 (formatBurnie for effective + weighted);
  (d) bucket table with player's row aria-current highlight (5-12 normal / 2-12 centennial);
  (e) payout pill state machine (5 states);
  (f) terminal sub-section conditional on terminal burns.
- INTEG-03 side-quest is recorded in 55-03-SUMMARY.md with 3 database/main commit hashes.
</verification>

<success_criteria>
- [ ] Task 1 checkpoint resolved with `endpoint-shipped` (optionally with shape-drift markers) OR `deferred` with clear rationale
- [ ] INTEG-03 endpoint is reachable at `http://localhost:3000/player/{addr}/decimator?level=N[&day=M]` and returns the INTEG-03-SPEC.md-compliant JSON shape (or a documented-drift shape)
- [ ] Database-side vitest passes for the new endpoint (6+ test scenarios: happy path + bucket null + empty level + not_eligible + closed + day-scoped)
- [ ] `play/components/decimator-panel.js` #refetchLevel() has its Wave-1 stub comment replaced with Wave-2 "INTEG-03 error path" comment
- [ ] IF shape-drift: #renderBucketTable + #renderStatsRow + #renderPayout + #renderRoundStatus field references updated to match shipped schema
- [ ] All 13 play/ test files pass: play-decimator-panel, play-coinflip-panel, play-baf-panel, play-tickets-panel, play-packs-panel, play-jackpot-wrapper, play-jackpot-shell01-regression, play-shell-01, play-panel-stubs, play-route-structure, play-main-bootstrap, play-profile-panel, play-purchase-panel
- [ ] DECIMATOR-02 validated: bucket table shows player's bucket row highlighted via aria-current when bucket != null
- [ ] DECIMATOR-03 full validated: stats row shows populated effective + weighted values via formatBurnie
- [ ] DECIMATOR-04 validated: payout pill state machine renders all 5 states correctly
- [ ] DECIMATOR-01 window badge upgrades from binary (Wave 1) to 3-state (Wave 2 via INTEG-03 roundStatus)
- [ ] Contract-truth bucketRange(level) helper UNCHANGED; literals 12 + 5 + 2 preserved (Pitfall 1 override holds)
- [ ] Time-remaining remains DROPPED (Pitfall 4); no levelStartTime references (Wave 1 baseline preserved)
- [ ] SHELL-01 recursive guardrail stays green with 14 FORBIDDEN entries
- [ ] Phase 55 requirement matrix updated in 55-03-SUMMARY.md: DECIMATOR-01 validated (Wave 1 binary + Wave 2 3-state); DECIMATOR-03 validated (partial Wave 1 + full Wave 2); DECIMATOR-05 validated (Wave 1); DECIMATOR-02 validated (Wave 2); DECIMATOR-04 validated (Wave 2); INTEG-03 shipped (Wave 2)
</success_criteria>

<output>
After completion, create `.planning/phases/55-decimator/55-03-SUMMARY.md` per the standard template, including:
- INTEG-03 side-quest execution record: 3 commit hashes on database/main (format: feat + docs + test mirroring Phase 51 INTEG-02 / Phase 52 INTEG-01 / Phase 54 INTEG-05)
- Database-side test pass: vitest green for the new happy path + bucket null + empty level + not_eligible + closed + day-scoped fixtures
- Website-side test pass: 13/13 play/ test files green (288 prior + ~37 Phase 55 = ~325 total assertions)
- Files modified: 1 (decimator-panel.js)
- LOC delta: ~1-5 lines changed in standard case; ~5-15 lines if shape-drift
- Phase 55 exit state: ready for Wave 3 (manual UAT or deferral per Phase 50+51+52+53+54 chain)
- REQUIREMENTS.md updates: INTEG-03 flipped from [ ] Pending to [x] Validated (2026-04-24); DECIMATOR-01 + 02 + 03 + 04 + 05 all flipped to [x] Validated; Traceability table rows updated; footer timestamp updated
- Contract-truth preservation verified: literals DECIMATOR_BUCKET_BASE=12, DECIMATOR_MIN_BUCKET_NORMAL=5, DECIMATOR_MIN_BUCKET_100=2 present; 1-8 encoding absent; levelStartTime absent
</output>

## Plan Summary

**Objective:** Wave 2 -- wire the shipped INTEG-03 endpoint into decimator-panel for full DECIMATOR-02 + DECIMATOR-03 full + DECIMATOR-04 validation; hard-gated on database-side delivery.

**Tasks:**
1. (CHECKPOINT) Verify INTEG-03 endpoint live at `localhost:3000/player/{addr}/decimator?level=` with happy path + edge cases (bucket null + empty level + not_eligible + closed + day-scoped + centennial bucket range); confirm 3 atomic commits on database/main; resume with `endpoint-shipped` (+/- shape-drift markers) or `deferred`
2. Surgical comment edit to #refetchLevel() in decimator-panel.js (rename "Wave 1 pre-backend safe-degrade stub" language to "INTEG-03 error path (400/500)"); if shape-drift was flagged, adjust #renderBucketTable + #renderStatsRow + #renderPayout + #renderRoundStatus field references to match shipped schema

**Verify chain:**
- Task 1 blocks until backend ships (3 atomic commits on database/main expected)
- Task 2 is a 1-5 line comment edit (possibly 2-5 field name swaps); preserves all Wave 1 render helpers + stale-guards + defensive ok-path + contract-truth bucketRange
- After Task 2, all 13 play/ test files pass (INTEG-03-dependent Wave 0 assertions turn green)
- REQUIREMENTS.md updates in 55-03-SUMMARY.md flip INTEG-03 to [x] Validated + all 5 DECIMATOR requirements to [x]

**Context cost estimate:** ~10-15% for Task 2 (small surgical edit + test re-runs); checkpoint is interactive so no consumption beyond what the user provides during the database side-quest.

**Duration:** Task 1 blocks on database side-quest (variable; 10-15 min per Phase 54 INTEG-05 precedent which is comparable scope). Task 2 runs in 2-3 minutes. Live-browser verification adds 3-5 minutes.
</content>
</invoke>